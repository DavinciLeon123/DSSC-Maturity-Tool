---
phase: 14-scoring-engine-replacement
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/services/scoring_engine.py
  - config/scoring/mami-scoring.json
  - config/mami-framework.json
  - backend/app/main.py
  - backend/app/core/deps.py
  - backend/app/services/mami_config.py
  - backend/tests/benchmark/test_scoring_regression.py
  - backend/tests/perf/test_scoring_perf.py
  - backend/tests/test_health.py
  - backend/tests/conftest.py
  - backend/tests/test_zen_removed.py
  - backend/Dockerfile
  - docs/api/openapi.json
autonomous: true
requirements: [SCOR-03]
security_asvs_level: 1
security_block_on: high

must_haves:
  truths:
    - "After this plan, no ZEN/MoSCoW symbol remains anywhere in backend/app or config/: the zen import, get_zen_engine, get_mami_config, load_mami_config, get_scoring_dir, scoring_engine, zen.ZenEngine, app.state.zen_engine, and app.state.mami_config are all gone (SCOR-03, D-01), verified by test_zen_removed.py's static scan."
    - "The zen-engine dependency is absent from backend/pyproject.toml and backend/uv.lock; the app imports and the suite runs without the package installed (SCOR-03, D-01)."
    - "config/scoring/mami-scoring.json and config/mami-framework.json no longer exist in the repo (SCOR-03, D-01)."
    - "backend/app/services/scoring_engine.py no longer exists (SCOR-03, D-01)."
    - "mami_config.py's load_dssc_questionnaire_config and the legacy load_questionnaire_config/load_questionnaire_configs loaders SURVIVE; only load_mami_config and get_scoring_dir are removed (D-01)."
    - "The legacy scoring tests test_scoring_regression.py and test_scoring_perf.py are deleted, and conftest.py's mami_codes/make_answers fixtures (their only consumers) are removed along with the load_mami_config import (D-08)."
    - "test_health.py no longer asserts app.state.mami_config/zen_engine; it asserts the surviving app.state.dssc_questionnaire_config instead (D-08)."
    - "docs/api/openapi.json is regenerated from the FastAPI app and git-diff-clean, capturing the /score, /report/data, and /admin/heatmap response-model changes from Plans 14-02/14-03 (CLAUDE.md docs-freshness gate)."
  artifacts:
    - backend/tests/test_zen_removed.py
    - docs/api/openapi.json
  key_links:
    - "This plan runs only after Plans 14-02 and 14-03 have removed every endpoint dependency on get_zen_engine/get_mami_config, so deleting those deps.py functions and the main.py lifespan wiring cannot break app import."
    - "openapi.json regeneration is centralized here (not in 14-02/14-03) to avoid a same-wave write conflict on that single file."
  prohibitions:
    - "Must not leave any zen import, zen-engine dependency entry, or MAMI/scoring config file anywhere in the repo."
    - "Must not dual-maintain or deprecate-in-place the ZEN engine — it is deleted outright (SCOR-03)."
    - "Must not delete or alter load_dssc_questionnaire_config or the legacy load_questionnaire_config/load_questionnaire_configs loaders in mami_config.py — only load_mami_config and get_scoring_dir are removed."
    - "Must not change the Dockerfile's FROM python:3.13-slim base image or UV_PYTHON_PREFERENCE=only-system — only correct the now-inaccurate zen-engine comment (Pitfall 5)."
    - "Must not write test_zen_removed.py so that it scans backend/tests (it would self-match its own search tokens); scope the scan to backend/app and config, and build search tokens from parts (Phase 13 test_evidence_removed.py precedent)."
---

<!-- planner-discipline-allow: import zen -->
<!-- planner-discipline-allow: get_zen_engine -->
<!-- planner-discipline-allow: get_mami_config -->
<!-- planner-discipline-allow: load_mami_config -->
<!-- planner-discipline-allow: get_scoring_dir -->
<!-- planner-discipline-allow: zen.ZenEngine -->
<!-- planner-discipline-allow: scoring_engine -->
<!-- planner-discipline-allow: zen-engine -->

<objective>
Complete the ZEN/MoSCoW removal (SCOR-03, D-01/D-08): delete the `zen-engine` package, `scoring_engine.py`, the two MAMI config files, the `app.state` lifespan wiring, the `get_zen_engine`/`get_mami_config` dependencies, `mami_config.py`'s `load_mami_config`/`get_scoring_dir`, the legacy scoring perf/benchmark tests and their conftest fixtures; update `test_health.py`; add a static regression test (`test_zen_removed.py`) that locks the removal in place; and regenerate `docs/api/openapi.json` to capture all response-model changes from this phase.

Purpose: This is the final wave — it removes the now-orphaned ZEN/MoSCoW code and dependency that Plans 14-02/14-03 already stopped importing, and makes the removal regression-proof and the OpenAPI doc fresh so CI's docs-freshness gate passes.
Output: deletions across app/config/tests, updated `main.py`/`deps.py`/`mami_config.py`/`test_health.py`/`conftest.py`/`Dockerfile`, new `test_zen_removed.py`, regenerated `openapi.json`.

Depends on Plans 14-02 AND 14-03 — both must have removed all endpoint-level `get_zen_engine`/`get_mami_config` usage first, or deleting those definitions here breaks app import.
</objective>

<artifacts_this_phase_produces>
This plan produces:
- New static regression test `backend/tests/test_zen_removed.py` (SCOR-03) — mirrors Phase 13's `test_evidence_removed.py` AST-walk/substring-scan pattern.
- Regenerated `docs/api/openapi.json` reflecting the phase's response-model changes.
- Trimmed `mami_config.py` (surviving: `load_dssc_questionnaire_config`, `load_questionnaire_config`, `load_questionnaire_configs`).
- Deletions: `scoring_engine.py`, `config/scoring/mami-scoring.json`, `config/mami-framework.json`, `test_scoring_regression.py`, `test_scoring_perf.py`, the `zen-engine` dependency, and the `app.state` ZEN wiring / deps functions / conftest fixtures.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-scoring-engine-replacement/14-CONTEXT.md
@.planning/phases/14-scoring-engine-replacement/14-RESEARCH.md
@.planning/phases/14-scoring-engine-replacement/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the ZEN/MoSCoW subsystem (package, service, config, wiring, deps, loaders)</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/app/services/scoring_engine.py, config/scoring/mami-scoring.json, config/mami-framework.json, backend/app/main.py, backend/app/core/deps.py, backend/app/services/mami_config.py, backend/Dockerfile</files>
  <read_first>
    - backend/app/main.py — the `import zen` (~line 3), the `app.state.mami_config = load_mami_config()` line (~28), the `scoring_dir`/`loader`/`app.state.zen_engine = zen.ZenEngine(...)` block (~38-44), and the mami_config import block (~17-23) — noting which loaders to KEEP.
    - backend/app/core/deps.py — the `import zen` (line 1) and the two dependency functions (~42-49); the surviving `get_dssc_questionnaire_config` (~62-67) is the template to leave untouched.
    - backend/app/services/mami_config.py — `load_mami_config` (~11-14) and `get_scoring_dir` (~39-41) are the deletion targets; `load_dssc_questionnaire_config` (~32-36) and the legacy loaders must be preserved.
    - backend/Dockerfile — line ~10, the stale comment that cites zen-engine as the reason for the Python 3.13 pin (Pitfall 5).
    - .planning/phases/14-scoring-engine-replacement/14-RESEARCH.md — "Removing the dependency correctly" (uv remove zen-engine, NOT `uv remove zen`), the exact main.py/deps.py lines to remove, and Pitfalls 5 and 6.
    - .planning/phases/14-scoring-engine-replacement/14-PATTERNS.md — the "MODIFIED IN PLACE" sections for main.py, deps.py, mami_config.py.
  </read_first>
  <action>
    Perform the removals. From `backend/` run `uv remove zen-engine` (the PyPI/pyproject package name is `zen-engine`, NOT the import name `zen`) to update pyproject.toml + uv.lock + the venv in one step; if the lock is not updated cleanly, follow with `uv lock`. Delete `backend/app/services/scoring_engine.py` via `git rm`. Delete the two config files via `git rm config/scoring/mami-scoring.json config/mami-framework.json` (using git rm removes the now-empty config/scoring directory from version control cleanly — Pitfall 6). In `backend/app/main.py`: remove the top-level zen import, the `app.state.mami_config = ...` assignment, and the entire `scoring_dir`/`loader`/`app.state.zen_engine = ...` block, and remove `load_mami_config`/`get_scoring_dir` from the mami_config import — KEEP `load_dssc_questionnaire_config` and the legacy `load_questionnaire_config`/`load_questionnaire_configs` imports and the surviving `app.state.dssc_questionnaire_config` wiring. In `backend/app/core/deps.py`: remove the top-level zen import and the two dependency functions that returned the ZEN engine and the MAMI config from app.state — KEEP `get_dssc_questionnaire_config`. In `backend/app/services/mami_config.py`: remove the MAMI-config loader function and the scoring-dir helper — KEEP `load_dssc_questionnaire_config`, `load_questionnaire_config`, `load_questionnaire_configs`. In `backend/Dockerfile`: edit only the comment that cites the removed package as the reason for the Python 3.13 pin (e.g. reword to "pinned for wheel-compatibility with native-extension dependencies") — do NOT change `FROM python:3.13-slim` or `UV_PYTHON_PREFERENCE=only-system`. Remove any other imports left orphaned by these deletions.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import app.main" && uv run python -c "from app.services.mami_config import load_dssc_questionnaire_config, load_questionnaire_config, load_questionnaire_configs" && uv run ruff check . && uv run mypy app --ignore-missing-imports</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run python -c "import app.main"` exits 0 (app imports with the package removed).
    - `grep -c 'zen-engine' backend/pyproject.toml` returns 0 and `grep -c 'zen-engine' backend/uv.lock` returns 0.
    - `backend/app/services/scoring_engine.py`, `config/scoring/mami-scoring.json`, and `config/mami-framework.json` no longer exist (`test ! -e` each).
    - `grep -c 'get_zen_engine\|get_mami_config' backend/app/core/deps.py` returns 0; `grep -c 'load_mami_config\|get_scoring_dir' backend/app/services/mami_config.py` returns 0.
    - The surviving loaders import successfully (the second python -c above exits 0).
    - `grep -c 'FROM python:3.13-slim' backend/Dockerfile` still returns 1 (base image unchanged).
    - ruff and mypy pass across the app.
  </acceptance_criteria>
  <done>The ZEN engine, its package, config files, service, lifespan wiring, dependencies, and MAMI loaders are removed; surviving DSSC/legacy loaders intact; app imports; ruff/mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy scoring tests and their fixtures; fix test_health assertions</name>
  <files>backend/tests/benchmark/test_scoring_regression.py, backend/tests/perf/test_scoring_perf.py, backend/tests/conftest.py, backend/tests/test_health.py</files>
  <read_first>
    - backend/tests/conftest.py — the module docstring (lines 1-16), the `from app.services.mami_config import load_mami_config` import (~line 22), and the `mami_codes` (~26) / `make_answers` (~32) fixtures to delete; the DB-backed fixtures below them must be preserved untouched.
    - backend/tests/test_health.py — lines ~13-14 asserting `client.app.state.mami_config is not None` / `client.app.state.zen_engine is not None`.
    - backend/tests/benchmark/test_scoring_regression.py and backend/tests/perf/test_scoring_perf.py — confirm they are the only consumers of mami_codes/make_answers before deleting the fixtures (RESEARCH confirms repo-wide grep: no other consumers).
    - .planning/phases/14-scoring-engine-replacement/14-CONTEXT.md — D-08 (leave a note that Phase 17 owns the equal-weight-scoring replacements for these deleted perf/benchmark tests).
  </read_first>
  <action>
    Delete `backend/tests/benchmark/test_scoring_regression.py` and `backend/tests/perf/test_scoring_perf.py` via `git rm`. In `backend/tests/conftest.py`: remove the `mami_codes` and `make_answers` fixtures entirely, remove the now-unused `from app.services.mami_config import load_mami_config` import, and update the module docstring's first fixture-family bullet so it no longer describes the deleted synthetic-scoring fixtures — leave the DB-backed fixture family and its docstring bullet intact. In `backend/tests/test_health.py`: remove the two assertions on `app.state.mami_config`/`app.state.zen_engine` and replace them with an assertion that `client.app.state.dssc_questionnaire_config is not None` (the surviving startup singleton). Record a short note in `.planning/phases/14-scoring-engine-replacement/deferred-items.md` (create/append) stating that Phase 17 (TEST-01) owns writing equal-weight-scoring perf/benchmark replacements for the two deleted tests, so the quiet perf/benchmark scoring coverage between Phase 14 and Phase 17 is intentional, not an oversight (D-08).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_health.py -x -q && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/benchmark/test_scoring_regression.py` and `backend/tests/perf/test_scoring_perf.py` no longer exist.
    - `grep -c 'mami_codes\|make_answers\|load_mami_config' backend/tests/conftest.py` returns 0.
    - `grep -c 'app.state.mami_config\|app.state.zen_engine' backend/tests/test_health.py` returns 0; test_health.py asserts `dssc_questionnaire_config is not None` (source assertion).
    - `pytest tests/test_health.py -x` passes.
    - `.planning/phases/14-scoring-engine-replacement/deferred-items.md` contains the Phase 17 perf/benchmark note.
    - `cd backend && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q` is green (no collection errors from orphaned fixtures/imports).
  </acceptance_criteria>
  <done>Legacy scoring tests and their fixtures are gone, test_health asserts the surviving singleton, the Phase 17 deferral is noted, and the quick suite collects and passes clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add the SCOR-03 static removal regression test and regenerate openapi.json</name>
  <files>backend/tests/test_zen_removed.py, docs/api/openapi.json</files>
  <read_first>
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration — Phase 13's `test_evidence_removed.py` (its AST-walk / substring-scan structure and the "class name built from parts to avoid self-matching" technique referenced in STATE.md) is the analog to mirror; locate it under backend/tests.
    - backend/scripts/export_openapi.py — the exact regeneration entrypoint (`uv run python scripts/export_openapi.py` writes docs/api/openapi.json with json.dumps(indent=2, sort_keys=True) + newline).
    - backend/app/main.py, backend/app/core/deps.py, backend/app/services/mami_config.py — the files whose post-removal state the static test validates.
  </read_first>
  <action>
    Create `backend/tests/test_zen_removed.py` mirroring Phase 13's `test_evidence_removed.py`. It must statically scan `backend/app/` and `config/` (NOT `backend/tests/`, to avoid self-matching its own search tokens) and assert the absence of every removed ZEN/MoSCoW symbol: the zen module import, the ZEN-engine dependency function, the MAMI-config dependency function, the MAMI-config loader function, the scoring-dir helper, the scoring_engine module reference, the ZenEngine constructor call, and the two `app.state` ZEN/MAMI attribute assignments. Build the search tokens from parts (string concatenation) so the test file does not match itself, following Phase 13's precedent. Additionally assert: `backend/app/services/scoring_engine.py`, `config/scoring/mami-scoring.json`, and `config/mami-framework.json` do not exist; and the `zen-engine` dependency string is absent from `backend/pyproject.toml`. Do NOT negative-grep the bare token `mami_config` (the module file survives with legitimate `load_dssc_questionnaire_config` — target only the specific removed symbols `load_mami_config`/`get_mami_config`/`app.state.mami_config`). Then regenerate the OpenAPI doc: from `backend/`, run `uv run python scripts/export_openapi.py`, which rewrites `docs/api/openapi.json` to reflect the new /score ScoreResponse+DimensionScore, the /report/data dimension_scores field, and the simplified AdminHeatmapResponse (and the removal of FindingRead) from Plans 14-02/14-03.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_zen_removed.py -x -q && uv run python scripts/export_openapi.py && git diff --exit-code -- docs/api/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_zen_removed.py -x` passes (all removed-symbol absence assertions green).
    - test_zen_removed.py scans backend/app and config only, not backend/tests, and builds its search tokens from parts (source assertion; no self-match).
    - `grep -c 'FindingRead\|"matrix"\|"topic_structure"' docs/api/openapi.json` reflects the removals (FindingRead component gone); `grep -c 'DimensionScore' docs/api/openapi.json` returns >0 (new component present).
    - After running `scripts/export_openapi.py`, `git diff --exit-code -- docs/api/openapi.json` exits 0 (doc is fresh — the committed file matches the regenerated schema; CI docs-freshness gate will pass).
    - `cd backend && uv run pytest tests/ -n auto -m "not perf" -q` (full staging-onward suite) is green.
  </acceptance_criteria>
  <done>The static removal test passes and locks SCOR-03 in place, and docs/api/openapi.json is regenerated and diff-clean, capturing all phase response-model changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/supply chain → dependency manifest | Removing a native-extension dependency (`zen-engine`) changes pyproject.toml/uv.lock; the lock must stay internally consistent and the app must run without the package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | orphaned ZEN references after removal | medium | mitigate | `test_zen_removed.py` statically asserts no ZEN/MoSCoW symbol, config file, or dependency entry survives — a regression-proof gate beyond ruff/mypy incidentally catching orphaned imports (RESEARCH SCOR-03 test map). |
| T-14-05 | Denial of Service | app startup after lifespan-wiring removal | medium | mitigate | `uv run python -c "import app.main"` and `pytest tests/test_health.py` confirm the app still starts and the surviving `dssc_questionnaire_config` singleton loads after the ZEN `app.state` wiring is removed. |
| T-14-SC | Tampering | dependency removal (zen-engine) | low | accept | This is a planned architectural REMOVAL, not an install — no package legitimacy checkpoint applies (RESEARCH Package Legitimacy Audit: `zen-engine` REMOVED, no legitimacy check needed for a removal). The SBOM regenerates automatically in CI per CLAUDE.md. |
</threat_model>

<verification>
- `cd backend && uv run python -c "import app.main"` exits 0 with `zen-engine` removed.
- `cd backend && uv run pytest tests/test_zen_removed.py tests/test_health.py -x -q` passes.
- `cd backend && uv run python scripts/export_openapi.py && git diff --exit-code -- docs/api/openapi.json` exits 0.
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports` clean.
- `cd backend && uv run pytest tests/ -n auto -m "not perf" -q` (full staging-onward suite) green.
</verification>

<success_criteria>
- GoRules ZEN Engine, its rule configs, the MAMI framework config, and MoSCoW-based scoring no longer exist anywhere in the codebase or dependency manifest (SCOR-03), locked in by test_zen_removed.py.
- The app imports and the full suite passes without the zen-engine package; openapi.json is fresh and diff-clean; the Phase 17 perf/benchmark deferral is documented.
</success_criteria>

<output>
Create `.planning/phases/14-scoring-engine-replacement/14-04-SUMMARY.md` when done.
</output>
