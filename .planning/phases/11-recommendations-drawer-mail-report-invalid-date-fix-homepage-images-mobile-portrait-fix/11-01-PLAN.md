---
phase: 11-recommendations-drawer-mail-report-invalid-date-fix-homepage-images-mobile-portrait-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/report_generator.py
  - frontend/src/routes/index.tsx
autonomous: true
requirements:
  - INVALID-DATE-FIX-01
  - HOMEPAGE-IMAGES-01
must_haves:
  truths:
    - "Report page shows a correctly-parsed date (no 'Invalid Date' on Railway/Linux)"
    - "Homepage displays the OBJECTS.svg geometric illustration on the right side of the hero"
    - "Homepage displays lines-top.svg decoration on the left edge of the upper page"
    - "Homepage displays lines-bottom.svg decoration on the left edge of the lower page"
  artifacts:
    - path: "backend/app/services/report_generator.py"
      provides: "ISO 8601 generated_at strings at both generation locations"
      contains: "isoformat() + \"Z\""
    - path: "frontend/src/routes/index.tsx"
      provides: "Homepage with three SVG decorations imported and placed"
      contains: "OBJECTS.svg"
  key_links:
    - from: "backend/app/services/report_generator.py"
      to: "frontend/src/routes/_app/report.tsx"
      via: "generated_at field in JSON response → new Date(...).toLocaleString()"
      pattern: "isoformat"
    - from: "frontend/src/assets/OBJECTS.svg"
      to: "frontend/src/routes/index.tsx"
      via: "import objectsSrc from '../assets/OBJECTS.svg'"
      pattern: "objectsSrc"
---

<objective>
Fix the Invalid Date bug (report page breaks on Railway/Linux due to non-ISO date strings) and add the three homepage SVG decorations per design.

Purpose: Unblocks the report date display on Railway. Completes the homepage visual design with the three decoration SVGs that already exist in assets but are not yet referenced.
Output: report_generator.py emitting ISO 8601 dates; index.tsx showing OBJECTS.svg, lines-top.svg, and lines-bottom.svg per the Homepage Design.png layout.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/app/services/report_generator.py
@frontend/src/routes/index.tsx
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From backend/app/services/report_generator.py (TWO locations to change):
- Line ~49 in generate_html_report() context dict:
  `"generated_at": datetime.utcnow().strftime("%Y-%m-%d %H:%M UTC"),`
- Line ~204 in generate_report_data() initiative dict:
  `"generated_at": datetime.utcnow().strftime("%Y-%m-%d %H:%M UTC"),`

Both must change to:
  `"generated_at": datetime.utcnow().isoformat() + "Z",`

From frontend/src/routes/index.tsx (existing import pattern to follow):
```typescript
import logoSrc from '../assets/logo-coe-dsc.svg';
// Pattern: import as URL string, use as <img src={...} />
// Do NOT use ?url or ?react suffixes — Vite handles SVG imports as URLs by default
```

Homepage layout structure (from reading index.tsx):
- `<main style={{ flex: 1 }}>` with `position: "relative"` needed for absolute-positioned lines
- Hero section: `<section style={{ background: 'linear-gradient(135deg, #06004f...)' }}>` with inner `<div style={{ maxWidth: '800px', margin: '0 auto' }}>`
- OBJECTS.svg goes in the hero section, positioned to the right of the headline text — use `position: "absolute"` on the right side of the hero, or a two-column flex layout within the hero
- lines-top.svg: absolutely positioned on left edge, upper half of page (z-index behind content)
- lines-bottom.svg: absolutely positioned on left edge, lower half of page (z-index behind content)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Invalid Date — ISO 8601 in report_generator.py</name>
  <files>backend/app/services/report_generator.py</files>
  <action>
    Find and replace BOTH occurrences of `datetime.utcnow().strftime("%Y-%m-%d %H:%M UTC")` with `datetime.utcnow().isoformat() + "Z"` in report_generator.py.

    Location 1: generate_html_report() — the context dict passed to the Jinja2 template (around line 49).
    Location 2: generate_report_data() — the initiative sub-dict in the JSON response (around line 204).

    The frontend already uses `new Date(data.initiative.generated_at).toLocaleString()` which works correctly with ISO 8601. No frontend change needed.

    Do NOT change the `generated_at=datetime.utcnow()` call in reports.py — that's a SQLAlchemy datetime object, not a string, and is correct.
  </action>
  <verify>
    <automated>grep -n "isoformat" backend/app/services/report_generator.py</automated>
  </verify>
  <done>Both strftime calls replaced with isoformat() + "Z". grep shows exactly 2 hits on isoformat in report_generator.py. No strftime remains in that file for generated_at.</done>
</task>

<task type="auto">
  <name>Task 2: Add SVG decorations to homepage</name>
  <files>frontend/src/routes/index.tsx</files>
  <action>
    Import the three SVG assets at the top of the file following the exact same pattern as the existing `logoSrc` import:

    ```typescript
    import objectsSrc from '../assets/OBJECTS.svg';
    import linesTopSrc from '../assets/lines-top.svg';
    import linesBottomSrc from '../assets/lines-bottom.svg';
    ```

    Place the SVGs per the Homepage Design.png layout:

    1. **OBJECTS.svg**: In the hero section, positioned to the right of the headline text. Change the inner hero div (currently `maxWidth: '800px', margin: '0 auto'`) to a flex row layout: left side holds the headline/CTA content (flex: 1), right side holds OBJECTS.svg (`<img src={objectsSrc} alt="" aria-hidden="true" style={{ flex: '0 0 auto', maxWidth: '420px', pointerEvents: 'none' }} />`). On mobile (narrow screens) stack vertically using `flexWrap: 'wrap'`.

    2. **lines-top.svg**: Add `position: "relative"` to the `<main>` element. Place `<img src={linesTopSrc} alt="" aria-hidden="true" style={{ position: 'absolute', left: 0, top: 0, zIndex: 0, pointerEvents: 'none', userSelect: 'none' }} />` as the first child inside `<main>`.

    3. **lines-bottom.svg**: Place `<img src={linesBottomSrc} alt="" aria-hidden="true" style={{ position: 'absolute', left: 0, bottom: 0, zIndex: 0, pointerEvents: 'none', userSelect: 'none' }} />` as the last child inside `<main>`.

    All existing homepage content sections remain unchanged — only the hero inner layout changes (flex row) and the three img elements are added. Ensure z-index on line decorations (0) is behind all section content (which should be `position: 'relative', zIndex: 1` or just rely on stacking context).
  </action>
  <verify>
    <automated>grep -c "objectsSrc\|linesTopSrc\|linesBottomSrc" frontend/src/routes/index.tsx</automated>
  </verify>
  <done>All three SVG imports present. All three `<img>` elements placed in the JSX (import count + usage count each 3). TypeScript build passes: `cd frontend && npx tsc --noEmit`.</done>
</task>

</tasks>

<verification>
- `grep -n "strftime" backend/app/services/report_generator.py` returns no results (all replaced)
- `grep -n "isoformat" backend/app/services/report_generator.py` shows exactly 2 hits
- `grep -c "objectsSrc\|linesTopSrc\|linesBottomSrc" frontend/src/routes/index.tsx` returns 6 (3 imports + 3 usages)
- TypeScript compilation passes: `cd "c:/Users/djlia/Desktop/MaMi Checker/frontend" && npx tsc --noEmit`
</verification>

<success_criteria>
- Report page on Railway/Linux shows a properly formatted date (e.g. "3/13/2026, 2:30:00 PM") instead of "Invalid Date"
- Homepage shows OBJECTS.svg on hero right side, lines-top.svg on upper-left edge, lines-bottom.svg on lower-left edge
- No TypeScript errors
- No other files modified beyond the two listed
</success_criteria>

<output>
After completion, create `.planning/phases/11-recommendations-drawer-mail-report-invalid-date-fix-homepage-images-mobile-portrait-fix/11-01-SUMMARY.md`
</output>
