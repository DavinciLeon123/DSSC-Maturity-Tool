---
phase: 13-new-questionnaire-config-schema-data-model-migration
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - backend/app/models/evidence.py
  - backend/app/schemas/evidence.py
  - backend/app/api/v1/evidence.py
  - frontend/src/lib/evidence.ts
  - frontend/src/components/questionnaire/EvidenceInput.tsx
  - backend/app/main.py
  - backend/app/db/base.py
  - backend/app/api/v1/admin.py
  - backend/app/api/v1/reports.py
  - backend/app/services/report_generator.py
  - backend/tests/factories.py
  - backend/tests/api/test_admin.py
  - backend/tests/api/test_reports.py
  - backend/tests/services/test_report_generator.py
  - backend/tests/api/test_evidence_removed.py
autonomous: true
requirements: [MIGR-02]
must_haves:
  truths:
    - "The evidence/URL-per-question subsystem no longer exists anywhere: no model, no schema, no API router, no frontend file (MIGR-02)"
    - "A grep for the evidence model class name across backend/app returns zero matches (MIGR-02)"
    - "Requests to the former /api/v1/initiatives/{id}/evidence routes return 404 (route gone), not 405 or 500 (MIGR-02)"
    - "The app imports and boots, and the existing Phase-12 suite (auth/admin/reports/report_generator) is green after evidence plumbing is stripped"
    - "Admin cascade-delete of an Initiative no longer attempts to delete evidence rows (the evidence table is gone) and still succeeds"
  artifacts:
    - "backend/tests/api/test_evidence_removed.py"
  key_links:
    - "Deleting the evidence model without first stripping its importers (admin.py, reports.py, report_generator.py, factories.py, test_admin.py) breaks `import app.main` at pytest collection — every test fails, not just evidence ones (RESEARCH Pitfall 3). All must change together in this plan."
    - "In this plan the answer model still has its OLD shape (mami_code / answer_value enum), so ZEN-based scoring in reports.py/admin.py still functions — only the evidence_by_code plumbing is removed. The answer-shape reshape is 13-03."
  prohibitions:
    - statement: "MUST NOT archive or migrate evidence data — it is dropped outright per D-11 (MIGR-01 names only initiative/answer data for preservation; evidence is a secondary free-text-URL feature with no scoring/compliance history)"
      verification: flagged-unverified
    - statement: "MUST NOT leave the evidence model class name in any comment or dead code inside backend/app — the MIGR-02 negative grep must return zero (a leftover reference in a comment silently fails the acceptance criterion)"
      verification: flagged-unverified
    - statement: "MUST NOT remove, disable, or rewrite the GoRules ZEN engine, config/mami-framework.json, or config/scoring/mami-scoring.json — that is SCOR-03 / Phase 14. This plan only removes evidence plumbing around those call sites (RESEARCH State of the Art / Deprecated)"
      verification: flagged-unverified
  assumptions:
    - "MIGR-02 (unclassified edge probe): the two frontend files (frontend/src/lib/evidence.ts, frontend/src/components/questionnaire/EvidenceInput.tsx) are confirmed orphaned (EvidenceInput.tsx not imported anywhere else per RESEARCH). Deleting them is a pure orphan-file removal, not new UI work — this is why the phase ran with --skip-ui. Surfaced explicitly rather than silently dropped."
---

<objective>
Remove the evidence/URL-per-question subsystem entirely (MIGR-02): delete the evidence model, schema, API router, and both frontend files, and surgically strip every evidence-plumbing reference from admin.py, reports.py, report_generator.py, db/base.py, main.py, and the four affected Phase-12 test files — leaving the app importable and the suite green.

Purpose: Leg B of the sequenced legs (RESEARCH), ordered BEFORE the answer-model reshape so that the answer table still carries its old shape here — meaning ZEN scoring in reports/admin keeps working and only evidence is removed, keeping each plan's exit green. This unblocks 13-03's model reshape (which then re-touches these same files for the new answer shape).
Output: evidence subsystem gone; a new static/route test proving its absence; the existing suite still green.
</objective>

<assumption_delta_decision>
noun: Assessment
decision: promote
rationale: "CONTEXT.md D-06 — Assessment becomes the primary answer-keying entity. Not touched in this plan (evidence-removal only); recorded per the phase-wide singular→plural transition. Schema lands in 13-03."
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-CONTEXT.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `backend/tests/api/test_evidence_removed.py` (NEW test file — MIGR-02 route+static absence check)
- Deletions (no new symbols): the evidence model, schema, API module, and two frontend files are removed.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Delete evidence files and remove its router + registry import</name>
  <files>backend/app/models/evidence.py, backend/app/schemas/evidence.py, backend/app/api/v1/evidence.py, frontend/src/lib/evidence.ts, frontend/src/components/questionnaire/EvidenceInput.tsx, backend/app/main.py, backend/app/db/base.py</files>
  <read_first>
    - backend/app/models/evidence.py (deletion target)
    - backend/app/schemas/evidence.py (deletion target)
    - backend/app/api/v1/evidence.py (deletion target — note its ownership-check pattern for reuse reference only)
    - frontend/src/lib/evidence.ts (deletion target)
    - frontend/src/components/questionnaire/EvidenceInput.tsx (deletion target, confirmed orphaned)
    - backend/app/main.py (evidence router import ~line 12 + include_router ~line 71)
    - backend/app/db/base.py (evidence model import for Alembic registry)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md (main.py + db/base.py Pattern Assignments)
  </read_first>
  <action>
    Delete these five files: backend/app/models/evidence.py, backend/app/schemas/evidence.py, backend/app/api/v1/evidence.py, frontend/src/lib/evidence.ts, frontend/src/components/questionnaire/EvidenceInput.tsx.

    In backend/app/main.py remove the evidence-router import line and its `app.include_router(...)` registration (per PATTERNS.md: import ~line 12, include ~line 71). Leave the lifespan config loads from 13-01 intact.

    In backend/app/db/base.py remove the evidence-model import line so Alembic autogenerate no longer references the deleted table.
    <!-- planner-discipline-allow: EvidenceURL -->
    Grep the whole backend/app tree for the evidence model class name and confirm zero remaining references after Task 2 completes (Task 2 handles the reports/admin/report_generator/test call sites). Do NOT leave the deleted class name in any comment or dead code.
  </action>
  <verify>
    <automated>cd backend && test ! -f app/models/evidence.py && test ! -f app/schemas/evidence.py && test ! -f app/api/v1/evidence.py && test ! -f ../frontend/src/lib/evidence.ts && test ! -f ../frontend/src/components/questionnaire/EvidenceInput.tsx && echo "deleted OK"</automated>
  </verify>
  <done>All five files removed; evidence router no longer imported/registered in main.py; evidence model import removed from db/base.py. (App may not yet import until Task 2 strips remaining importers.)</done>
</task>

<task type="auto">
  <name>Task 2: Strip evidence plumbing from admin/reports/report_generator and update Phase-12 tests</name>
  <files>backend/app/api/v1/admin.py, backend/app/api/v1/reports.py, backend/app/services/report_generator.py, backend/tests/factories.py, backend/tests/api/test_admin.py, backend/tests/api/test_reports.py, backend/tests/services/test_report_generator.py</files>
  <read_first>
    - backend/app/api/v1/admin.py (evidence import ~line 18; cascade-delete in _delete_initiative_children ~line 78; CSV export)
    - backend/app/api/v1/reports.py (evidence import ~line 16; ~5 near-identical evidence_by_code query/build blocks)
    - backend/app/services/report_generator.py (generate_html_report / generate_report_data / _build_findings_detail each take evidence_by_code)
    - backend/tests/factories.py (evidence import ~line 17; make_evidence ~lines 113-130)
    - backend/tests/api/test_admin.py (evidence import ~line 18; make_evidence import ~line 23; call sites ~lines 117,164; assertions ~lines 133,179)
    - backend/tests/api/test_reports.py (any evidence-dependent fixtures/assertions)
    - backend/tests/services/test_report_generator.py (evidence_by_code fixture usage)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md (admin/reports/report_generator + factories/test_admin Pattern Assignments — exact line references)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md (Pitfall 3 — the "smaller diff: drop the parameter" guidance)
  </read_first>
  <action>
    Remove all evidence-model imports and usages from backend/app/api/v1/admin.py, backend/app/api/v1/reports.py, and backend/app/services/report_generator.py.

    In report_generator.py: choose the smaller-diff option per RESEARCH Pitfall 3 — DROP the `evidence_by_code` parameter from generate_html_report, generate_report_data, and _build_findings_detail, and remove the per-finding `evidence` lookup/rendering. The report template must render without an evidence section.

    In reports.py: at each of the ~5 call sites, remove the evidence-model query, the evidence_by_code dict build, and the `evidence_by_code=` keyword argument passed into the report_generator calls. Do NOT touch the ZEN scoring calls themselves (answer.mami_code still exists in this plan — the answer reshape is 13-03).

    In admin.py: remove the evidence cascade-delete line from _delete_initiative_children and any evidence column/comment in the CSV export. Leave participant_type handling unchanged in this plan (nullability is 13-03).

    Update the four Phase-12 test files: backend/tests/factories.py (remove the evidence import and the make_evidence factory), backend/tests/api/test_admin.py (remove make_evidence imports, both call sites, and their evidence assertions — keep the rest of the cascade-delete test asserting non-evidence children still delete), backend/tests/api/test_reports.py and backend/tests/services/test_report_generator.py (remove evidence fixtures/assertions and the evidence_by_code argument from any direct report_generator calls).

    After edits, a grep of backend/app for the evidence model class name must return zero matches; do not leave the token in comments.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import app.main; print('import OK')" && uv run ruff check app && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q</automated>
  </verify>
  <done>App imports; ruff clean; full quick suite green with no evidence references remaining; grep for the evidence model class name across backend/app returns nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_evidence_removed.py — route absence + static absence (MIGR-02)</name>
  <files>backend/tests/api/test_evidence_removed.py</files>
  <read_first>
    - backend/tests/conftest.py (client / user_client fixtures)
    - backend/tests/api/test_auth.py (API test style)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-VALIDATION.md (MIGR-02 row + Wave 0 requirement for test_evidence_removed.py)
  </read_first>
  <behavior>
    - test_evidence_route_returns_404: a GET/POST to a former /api/v1/initiatives/{id}/evidence path returns 404 (route removed), never 405 or 500.
    - test_no_evidence_model_import: a static check (grep or ast walk over backend/app) finds zero references to the deleted evidence model class name.
  </behavior>
  <action>
    Create backend/tests/api/test_evidence_removed.py. Add test_evidence_route_returns_404 hitting a representative former evidence route through the test client and asserting HTTP 404. Add test_no_evidence_model_import that walks backend/app (or shells a grep) and asserts the deleted evidence model class name appears nowhere — this is the MIGR-02 static guarantee. Reference the class name only via a variable/constant built at runtime (e.g. compose it from parts) so the test file itself does not contain the literal that would make its own grep self-matching; alternatively scope the grep to exclude the tests directory.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_evidence_removed.py -x -q</automated>
  </verify>
  <done>Both tests pass; former evidence routes 404; no evidence-model reference survives in backend/app. Full quick suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → former /evidence routes | Previously accepted user-supplied URLs (SSRF surface); this plan removes the surface entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Information Disclosure / SSRF | former evidence URL-input endpoints | high | mitigate | Removed entirely (MIGR-02/D-11) — deleting the model, router, and frontend eliminates the user-supplied-URL attack surface that RESEARCH/REQUIREMENTS note as the SSRF concern. Verified by test_evidence_removed.py (404 + static-absence). |
| T-13-07 | Tampering | admin cascade-delete path | low | accept | Removing the evidence cascade-delete line cannot orphan evidence rows because the table itself is dropped in the 13-04 migration; interim (pre-migration) rows become unreferenced but harmless read-only data until the table drop. |
| T-13-SC | Tampering | package installs | n/a | accept | Zero new packages (RESEARCH Package Legitimacy Audit). No install task. |

Disposition note: T-13-06 is high but fully mitigated by removal (the control IS the removal), satisfying security_block_on=high.
</threat_model>

<verification>
- `uv run pytest tests/ -n auto -m "not perf and not benchmark" -q` green.
- `import app.main` succeeds.
- `grep -rE "class .*Evidence|evidence_url" backend/app` returns nothing meaningful (model/table gone); former evidence routes 404.
</verification>

<success_criteria>
- Evidence subsystem fully absent: model, schema, API, both frontend files deleted (MIGR-02).
- No evidence import breaks the app; suite green (answer model still old shape, ZEN scoring intact).
- test_evidence_removed.py proves 404 + static absence.
</success_criteria>

<output>
Create `.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-02-SUMMARY.md` when done.
</output>
