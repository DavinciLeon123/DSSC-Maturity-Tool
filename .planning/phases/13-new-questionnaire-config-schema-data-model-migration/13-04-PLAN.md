---
phase: 13-new-questionnaire-config-schema-data-model-migration
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py
  - backend/tests/migrations/__init__.py
  - backend/tests/migrations/test_v1_archive_migration.py
  - docs/api/openapi.json
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "A single hand-written Alembic migration chains from the current head h8c6d5e4f3a2 and, on upgrade, creates the assessment table, creates questionnaire_answer_v1_archive with the OLD answer shape, copies every existing questionnaire_answer row into the archive verbatim, adds initiative.schema_version (server_default 'v2'), tags initiatives that have archived answers as 'v1_legacy', drops the old questionnaire_answer, and recreates it with the new assessment_id/category_id/score shape (D-01/D-02/D-03/MIGR-01)"
    - "All pre-migration questionnaire_answer rows are preserved verbatim in questionnaire_answer_v1_archive after upgrade — same row count, same answer_value strings, same initiative_id linkage (MIGR-01)"
    - "The new questionnaire_answer.score column carries a DB-level CHECK constraint restricting values to 1-5 (security V5 defense-in-depth)"
    - "The migration uses explicit sa.String() for the archived answer_value column, never a native Postgres ENUM (RESEARCH Pitfall 1)"
    - "The archive table has NO foreign key to initiative, so it survives a later admin hard-delete of a legacy initiative (D-01/A2)"
    - "alembic upgrade head succeeds from an empty DB AND from a DB seeded with pre-migration-shaped rows, verified in an isolated testcontainer (not the shared session-scoped fixture) — this is the first migration-verification test this repo has ever had (RESEARCH Pitfall 2)"
    - "docs/api/openapi.json regenerated via scripts/export_openapi.py matches the committed file (docs-freshness CI gate passes)"
    - "The migration does NOT touch, drop, or migrate the compliance_report table (D-05, RESEARCH Pitfall 4) nor the evidence_url table beyond what 13-02 removed"
  artifacts:
    - "backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py"
    - "backend/tests/migrations/__init__.py"
    - "backend/tests/migrations/test_v1_archive_migration.py"
  key_links:
    - "conftest builds schema via create_all and bypasses Alembic — so this migration has zero coverage from the existing suite. The new test MUST run alembic.command.upgrade(config, 'head') against a fresh testcontainer, not reuse the session-scoped postgres_container/engine fixtures (those already ran create_all and are not a clean upgrade slate) (RESEARCH Pitfall 2, PATTERNS test-fixture note)."
    - "The seeded-DB test must insert OLD-shaped questionnaire_answer rows via raw SQL (the SQLModel model is already reshaped by 13-03, so it cannot express the old shape) BEFORE running upgrade, then assert archive row count/content and initiative.schema_version tagging."
    - "downgrade must be authored and reversible-enough to pass upgrade->downgrade->upgrade against a seeded container; document its lossiness for post-upgrade new-shape answers in the migration docstring (same lossiness precedent as f6a4b3c2d1e9)."
  prohibitions:
    - statement: "MUST NOT generate this migration via alembic revision --autogenerate — env.py sets no compare_type, so autogenerate cannot reliably detect the string-enum -> integer reshape; hand-write it with explicit op.execute() like every prior data-transforming migration in this repo (RESEARCH Anti-Patterns)"
      verification: flagged-unverified
    - statement: "MUST NOT drop, alter, or force-migrate the compliance_report table — leave it untouched (D-05, Pitfall 4); stale HTML referencing the old framework is expected and is Phase 16's concern"
      verification: flagged-unverified
    - statement: "MUST NOT delete or fail to copy any existing questionnaire_answer row — a post-upgrade archive row count that differs from the pre-upgrade live count is a MIGR-01 failure (never delete compliance-relevant data)"
      verification: flagged-unverified
    - statement: "MUST NOT add a cascade-delete or FK from the archive table to initiative (D-01/A2) — archived history must outlive the initiative row"
      verification: flagged-unverified
    - statement: "MUST NOT let the archive answer_value column be created as a native Postgres ENUM — explicit sa.String() only (Pitfall 1)"
      verification: flagged-unverified
    - statement: "MUST NOT run under the perf or benchmark pytest markers — the migration test belongs in the default fast gate so every PR runs it (RESEARCH Project Constraints); and MUST NOT reuse the shared session-scoped testcontainer fixture"
      verification: flagged-unverified
  assumptions:
    - "MIGR-01 real edge (RESEARCH Assumption Log, promoted here per spec-less probe fallback): an initiative with ZERO prior answers produces zero archive rows and is NOT tagged 'v1_legacy' (its schema_version stays 'v2' because the legacy-tag UPDATE keys off DISTINCT initiative_id present in the archive). This is correct: a legacy initiative with no answers is indistinguishable from a new one and needs no legacy handling. Asserted explicitly in the seeded test (an answerless initiative stays 'v2'). Surfaced, not silently dropped."
    - "New migration revision id: i9d7e6f5a4b3 (next in the repo's alphabetical h-prefix sequence; down_revision h8c6d5e4f3a2). If the executor's alembic revision command auto-generates a different hash, rename the file and set revision/down_revision accordingly — the chain-from-h8c6d5e4f3a2 requirement is what matters, not the literal id."
    - "MIGR-01 unclassified edge probe (null/empty input): running upgrade against a DB with an empty questionnaire_answer table succeeds and creates all new tables with zero copied rows (covered by the empty-DB upgrade test). Flagged and covered."
---

<objective>
Deliver the phase's core artifact: one hand-written Alembic migration implementing the archive-table-split (RESEARCH Pattern 2) chained from head h8c6d5e4f3a2, plus the first migration-verification test this repo has ever had (BLOCKING — schema-gate contribution), plus regeneration of docs/api/openapi.json for the docs-freshness CI gate.

Purpose: 13-03 defined the models so conftest's create_all-built test schema is correct, but create_all bypasses Alembic entirely (RESEARCH Pitfall 2) — meaning the actual production upgrade path (which runs at container startup) has zero coverage. This plan writes the migration AND proves it against a real Postgres testcontainer with seeded pre-migration data, satisfying MIGR-01's "preserved read-only, not deleted" guarantee. Without this the phase cannot be considered verified (schema-gate contribution).
Output: the migration file, tests/migrations/test_v1_archive_migration.py (isolated container), and an up-to-date openapi.json.
</objective>

<assumption_delta_decision>
noun: Assessment
decision: promote
rationale: "CONTEXT.md D-06 — the migration creates the assessment table and re-keys questionnaire_answer onto assessment_id, physically promoting Assessment to the primary answer-keying entity in the database. This plan is where the promotion becomes durable schema."
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-CONTEXT.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md
@.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py — the phase's core migration (NEW; revision id may be regenerated — see assumption)
- backend/tests/migrations/__init__.py + backend/tests/migrations/test_v1_archive_migration.py — first migration-verification test category in the repo (NEW)
- docs/api/openapi.json — regenerated to reflect reshaped/removed schemas (MODIFIED)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write the archive-table-split Alembic migration (chained from h8c6d5e4f3a2)</name>
  <files>backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py</files>
  <read_first>
    - backend/alembic/versions/h8c6d5e4f3a2_make_contact_fields_nullable.py (current head — revision to chain from; alter_column nullable precedent)
    - backend/alembic/versions/f6a4b3c2d1e9_dsi_sp_questionnaire_foundation.py (data-preserving op.execute UPDATE precedent)
    - backend/alembic/versions/b3f7c9a1d2e8_add_questionnaire_answers_table.py (original questionnaire_answer create_table — old column shape + the sa.String() answer_value choice to mirror in the archive)
    - backend/alembic/versions/e5f3a2b4c6d7_add_compliance_report_table.py (plain create_table style)
    - backend/alembic/env.py (confirm no compare_type; render_as_batch already set)
    - backend/app/models/assessment.py, backend/app/models/questionnaire.py (13-03 target shapes the new tables must match)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md (Pattern 2 code block — copy-paste-ready template; Pitfall 1)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md (migration Pattern Assignment)
  </read_first>
  <action>
    Create backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py with revision i9d7e6f5a4b3 and down_revision "h8c6d5e4f3a2". Hand-write upgrade() (no autogenerate) using RESEARCH Pattern 2 as the template, in this order:
    1. op.create_table("assessment", ...) matching the 13-03 Assessment model (id PK, initiative_id integer + FK to initiative.id, version integer, status String, created_at DateTime, submitted_at DateTime nullable).
    2. op.create_table("questionnaire_answer_v1_archive", ...) mirroring the OLD questionnaire_answer columns; answer_value as sa.String() (NOT sa.Enum — Pitfall 1); NO ForeignKeyConstraint to initiative (A2).
    3. op.execute INSERT INTO questionnaire_answer_v1_archive (...) SELECT ... FROM questionnaire_answer — copy every existing row verbatim (MIGR-01).
    4. op.add_column("initiative", sa.Column("schema_version", sa.String(), nullable=False, server_default="v2")).
    5. op.execute UPDATE initiative SET schema_version='v1_legacy' WHERE id IN (SELECT DISTINCT initiative_id FROM questionnaire_answer_v1_archive).
    6. op.alter_column to make initiative.participant_type and "user".participant_type nullable (D-12).
    7. op.drop_table("questionnaire_answer"); then op.create_table("questionnaire_answer", ...) with the NEW shape: id PK, assessment_id integer + FK to assessment.id, question_id String, category_id String, score Integer, answered_at/updated_at DateTime, UniqueConstraint(assessment_id, question_id) named uq_answer_per_question_v2, and a CheckConstraint("score >= 1 AND score <= 5", name="ck_answer_score_range") (security V5 defense-in-depth).
    Do NOT touch compliance_report or evidence_url (D-05/Pitfall 4; evidence handled in 13-02).

    Author downgrade() to reverse the operations: drop the new questionnaire_answer, recreate the old shape, copy rows back from the archive, drop the archive and assessment tables, drop schema_version, restore participant_type non-nullability. Document in the docstring that new-shape answers written after upgrade are lossy on downgrade (same accepted lossiness as f6a4b3c2d1e9).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; c=Config('alembic.ini'); s=ScriptDirectory.from_config(c); heads=s.get_heads(); rev=s.get_revision(heads[0]); assert len(heads)==1, ('multiple heads', heads); assert rev.down_revision=='h8c6d5e4f3a2' or 'h8c6d5e4f3a2' in (rev.down_revision or ''), rev.down_revision; print('single head chained from h8c6d5e4f3a2 OK')" && uv run ruff check alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py</automated>
  </verify>
  <done>Migration file exists, single alembic head, chains from h8c6d5e4f3a2; upgrade creates assessment + archive + reshaped answer with score CHECK and no archive FK; compliance_report untouched; downgrade authored; ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] Migration-verification test against an isolated testcontainer (MIGR-01)</name>
  <files>backend/tests/migrations/__init__.py, backend/tests/migrations/test_v1_archive_migration.py</files>
  <read_first>
    - backend/tests/conftest.py (postgres_container / engine session-scoped fixtures — reuse the container-spinning mechanics but NOT the shared fixtures)
    - backend/alembic/env.py (how alembic resolves the DB URL / sqlalchemy.url)
    - backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py (Task 1 output under test)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md (Pitfall 2 — the exact 3-part test spec)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-VALIDATION.md (Wave 0 requirement for tests/migrations/test_v1_archive_migration.py)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-PATTERNS.md (test-fixture Pattern Assignment — isolated scope, not shared session fixture)
  </read_first>
  <behavior>
    - Fresh empty DB: alembic upgrade head succeeds and all new tables (assessment, questionnaire_answer_v1_archive, reshaped questionnaire_answer) exist.
    - Seeded DB: after seeding N OLD-shaped questionnaire_answer rows across M initiatives (one initiative deliberately answerless) via raw SQL, upgrade copies exactly N rows into the archive with identical answer_value/initiative_id, tags the M-with-answers initiatives 'v1_legacy', and leaves the answerless initiative 'v2'.
    - Round-trip: upgrade -> downgrade -> upgrade succeeds without error.
  </behavior>
  <action>
    Create backend/tests/migrations/__init__.py and backend/tests/migrations/test_v1_archive_migration.py. Use testcontainers PostgresContainer directly with a function- or module-scoped fixture (NOT the shared session-scoped postgres_container/engine — those already ran create_all and are not a clean upgrade slate, RESEARCH Pitfall 2 / PATTERNS). Build an Alembic Config pointing sqlalchemy.url at the fresh container and run alembic.command.upgrade(config, "head") programmatically.

    Test 1 (empty DB): upgrade head from empty; assert assessment, questionnaire_answer_v1_archive, and the reshaped questionnaire_answer tables exist and the new answer table has assessment_id/category_id/score columns and no mami_code.

    Test 2 (seeded DB): before upgrade, create the pre-migration initiative + questionnaire_answer tables' OLD shape via raw SQL (the reshaped SQLModel cannot express the old shape) — OR run migrations up to h8c6d5e4f3a2 first, then insert OLD-shaped rows via raw SQL — and seed a known number of rows across multiple initiatives, including one initiative with zero answers. Run upgrade to head. Assert: archive row count equals seeded count; archived answer_value strings and initiative_id values match what was seeded; initiatives that had answers are tagged 'v1_legacy'; the answerless initiative remains 'v2' (the MIGR-01 zero-answers edge). 

    Test 3 (round-trip): upgrade head, then alembic.command.downgrade to the previous revision, then upgrade again — assert no exception. Keep the test fast (schema-only, tiny row counts) so it runs in the default -n auto "not perf and not benchmark" gate. Do NOT mark it perf/benchmark.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/migrations/test_v1_archive_migration.py -x -q</automated>
  </verify>
  <done>All three tests pass against a fresh isolated testcontainer; seeded-DB test proves row-count/content preservation and correct schema_version tagging including the answerless-initiative stays-'v2' edge; round-trip upgrade/downgrade/upgrade succeeds. This is the BLOCKING gate: the phase is not verified until it is green.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate docs/api/openapi.json (docs-freshness CI gate) + full quality gate</name>
  <files>docs/api/openapi.json</files>
  <read_first>
    - backend/scripts/export_openapi.py (the regeneration script the docs-freshness CI job runs)
    - docs/api/openapi.json (committed schema that will drift after the reshaped/removed schemas)
    - CLAUDE.md (docs-freshness gate + local quality gate commands)
    - .planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-RESEARCH.md (Project Constraints — docs-freshness is a required task; Runtime State Inventory build-artifacts row)
  </read_first>
  <action>
    Run the openapi export script (per CLAUDE.md: uv run python scripts/export_openapi.py) so docs/api/openapi.json reflects the removed evidence routes/schemas (13-02), the reshaped AnswerCreate/AnswerRead score field, and the nullable participant_type in InitiativeRead (13-03). Commit the regenerated file. Confirm the former evidence routes are absent from the regenerated spec.

    Then run the full local quality gate from CLAUDE.md to confirm the whole phase is green before the PR: ruff check --fix + ruff format, then ruff check + ruff format --check + mypy app --ignore-missing-imports + the full quick pytest suite.
  </action>
  <verify>
    <automated>cd backend && uv run python scripts/export_openapi.py && git diff --exit-code ../docs/api/openapi.json && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q</automated>
  </verify>
  <done>docs/api/openapi.json regenerated, committed, and `git diff --exit-code` clean (docs-freshness will pass); no evidence routes in the spec; ruff + mypy + full quick suite all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → alembic upgrade head (container startup) | The migration transforms production data; a bug silently loses or corrupts preserved v1 answers |
| new questionnaire_answer.score (DB layer) | Last line of defense for out-of-range scores if a future write path bypasses Pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Tampering / Denial of data integrity | the archive-split migration | high | mitigate | BLOCKING migration-verification test (Task 2) proves every pre-migration row is copied verbatim and legacy tagging is correct against a seeded real-Postgres testcontainer, with an upgrade/downgrade/upgrade round-trip — the phase is not verified until green (schema-gate contribution). |
| T-13-02b | Tampering | questionnaire_answer.score column | medium | mitigate | DB-level CHECK (score BETWEEN 1 AND 5) added in the create_table, complementing the Pydantic Field(ge=1, le=5) from 13-03 (defense-in-depth). |
| T-13-08b | Tampering | archive durability under admin delete | high | mitigate | No FK from the archive table to initiative (D-01/A2) — an admin hard-delete of a legacy initiative cannot cascade-remove preserved history; asserted structurally in the migration. |
| T-13-SC | Tampering | package installs | n/a | accept | Zero new packages (RESEARCH Package Legitimacy Audit). No install task. |

High-severity threats T-13-03 and T-13-08b are dispositioned mitigate with concrete, tested controls, satisfying security_block_on=high.
</threat_model>

<verification>
- `uv run pytest tests/migrations/test_v1_archive_migration.py -x -q` green (BLOCKING).
- Single alembic head chained from h8c6d5e4f3a2; `alembic upgrade head` then `alembic downgrade -1` both succeed against a seeded testcontainer.
- `uv run python scripts/export_openapi.py` + `git diff --exit-code docs/api/openapi.json` clean (docs-freshness).
- Full local quality gate green (ruff + mypy + quick pytest).
</verification>

<success_criteria>
- Hand-written archive-split migration chained from head, preserving all v1 answers read-only in the archive, tagging legacy initiatives, reshaping questionnaire_answer with a score CHECK, no archive FK, compliance_report untouched (MIGR-01, D-01/D-02/D-03/D-05, Pitfall 1/4).
- BLOCKING migration test proves preservation + tagging (incl. answerless-initiative edge) + round-trip against an isolated testcontainer (Pitfall 2).
- docs/api/openapi.json regenerated and diff-clean; full quality gate green.
</success_criteria>

<output>
Create `.planning/phases/13-new-questionnaire-config-schema-data-model-migration/13-04-SUMMARY.md` when done.
</output>
