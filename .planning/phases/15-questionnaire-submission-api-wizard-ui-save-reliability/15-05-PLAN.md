---
phase: 15-questionnaire-submission-api-wizard-ui-save-reliability
plan: 05
type: execute
wave: 2
depends_on: [15-02, 15-03]
files_modified:
  - frontend/src/lib/assessments.ts
  - frontend/src/routes/_app/assessments.tsx
  - frontend/src/routes/_app/dashboard.tsx
  - frontend/src/routeTree.gen.ts
autonomous: true
requirements: [HIST-02, HIST-01]
must_haves:
  truths:
    # HIST-02 history view (D-16/D-17)
    - "A new dedicated /assessments route (linked from the dashboard, not inlined into the initiative card) shows the assessment history view (D-17)"
    - "The history view shows (a) a list/table of past submitted versions — date, version #, overall average score, link to that version's report (D-16a)"
    - "The history view shows (b) a per-dimension comparison table across versions — rows = the 6 config dimension names, columns = each submitted version, cells = that version's score for that dimension; plain antd Table, no chart (D-16b)"
    - "The dashboard gains a 'View assessment history' secondary/link-style button beside the existing Start/Retake button (D-17)"
    # HIST-01 retake trigger (D-13/D-14)
    - "Starting a retake on a SUBMITTED initiative is an explicit action: a 'Start new assessment' button opens a confirmation dialog with the locked copy before navigating to /questionnaire (D-13)"
    - "The confirmation dialog copy makes clear this creates a new permanent version and that nothing carries over from the previous submitted assessment (D-13/D-14)"
    - "The first-ever draft case (no prior submission) still navigates straight to /questionnaire with no dialog — there is nothing to confirm overwriting yet (UI-SPEC Component Inventory)"
    # UI considerations (covered)
    - "History list empty state: heading 'No completed assessments yet' + the locked body copy when the endpoint returns [] (never a '(0)' heading)"
    - "History list loading: skeleton rows or centered spinner while the endpoint resolves; the table shell/headers may render immediately with skeleton placeholders"
    - "History list load error: 'Couldn't load your assessment history.' + an inline Retry button, same failed→retry pattern as autosave (#991B1B on #FEE2E2)"
    - "History list populated row shape: date, version #, overall average, link to that version's report (D-16a)"
    - "History list zero-one-many: heading count-suffix format 'Your assessment history (N)' reads correctly at 1 and many; N=0 is the empty state, not a '(0)' heading"
    - "Comparison-table dimension names: 6 fixed config category names render single-line, no wrap/truncate logic"
    - "Comparison-table populated: columns = submitted versions (oldest→newest or newest→oldest, plain-table discretion), rows = 6 fixed dimensions, cells = per-version score (D-16b)"
  key_links:
    - "assessments.ts fetchAssessmentHistory(initiativeId) is a thin api.get wrapper over GET /initiatives/{id}/assessments (15-02), typed to AssessmentSummary"
    - "The comparison table pivots the same AssessmentSummary[] response client-side (dimension_scores names are identical across versions from config)"
    - "The retake confirmation reuses admin.index.tsx's Modal.confirm({title,content,onOk}) call shape, not a hand-rolled Modal instance"
    - "The new /assessments route is registered in routeTree.gen.ts (manually updated before tsc build, per project convention)"
  backstops:
    - statement: "History comparison-table overflow: as the version count grows, use a horizontal-scroll container with the dimension-name column sticky/frozen left, so columns scroll rather than squeeze unreadably thin"
      verification: backstop
    - statement: "'Start new assessment' confirm action in-flight: disable the confirm button + show an inline spinner while the new-draft creation/navigation is pending, to prevent a double-click creating two drafts"
      verification: backstop
security_enforcement: enabled
---

<objective>
Build the HIST-02 history UI and the HIST-01 explicit-retake trigger: a new dedicated /assessments page (D-17) with a version list (D-16a) and a per-dimension comparison table (D-16b), a thin API wrapper for the 15-02 endpoint, and a dashboard "View assessment history" link plus a confirmed "Start new assessment" retake dialog (D-13/D-14).

Purpose: HIST-02 requires the user to view and compare past versions; HIST-01/D-13 requires retakes to be an explicit, confirmed, permanent action rather than the current implicit silent new-draft creation.
Output: assessments.ts lib, assessments.tsx route (+ routeTree registration), extended dashboard.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-CONTEXT.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: assessments.ts lib + /assessments history page (list + comparison table, HIST-02/D-16/D-17)</name>
  <files>frontend/src/lib/assessments.ts, frontend/src/routes/_app/assessments.tsx, frontend/src/routeTree.gen.ts</files>
  <read_first>
    - frontend/src/lib/questionnaire.ts (thin api-wrapper convention to mirror for the new lib; AnswerRead-style typing)
    - frontend/src/routes/_app/admin.index.tsx (message.useMessage() line 38, antd Table dataSource+columns convention, Card/Alert composition, Link usage)
    - frontend/src/routes/_app/report.tsx (how a report link/route is shaped, for the per-version "view report" link-out)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"Greenfield History Endpoint" (AssessmentSummary shape: id, version, submitted_at, overall_average, dimension_scores)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Copywriting Contract (empty/error/heading copy), §UI Considerations (loading/empty/error/populated/overflow rows), §Visual Hierarchy
    - frontend/src/routeTree.gen.ts (existing route registration structure to extend for the new /assessments route)
  </read_first>
  <action>
    Create frontend/src/lib/assessments.ts: export an AssessmentSummary type (id, version, submitted_at, overall_average, dimension_scores: {category_id; name; score}[]) and fetchAssessmentHistory(initiativeId): Promise<AssessmentSummary[]> as a thin api.get("/initiatives/{id}/assessments") wrapper.
    Create frontend/src/routes/_app/assessments.tsx (createFileRoute "/_app/assessments"): fetch the current initiative then its history. Render two stacked sections per Visual Hierarchy — (1) primary version-list antd Table (columns: submitted date, version #, overall average, a link-out to that version's report) with the heading "Your assessment history (N)"; (2) secondary per-dimension comparison antd Table (rows = the 6 dimension names from dimension_scores, columns = each version, cells = that version's score) shown once there is >=1 version. Implement loading (skeleton/spinner), empty ("No completed assessments yet" + locked body, only when [] — no "(0)" heading), and error ("Couldn't load your assessment history." + inline Retry) states with the exact UI-SPEC copy and the #991B1B-on-#FEE2E2 error convention. Register the new route in routeTree.gen.ts (manual update before tsc, per project convention). Use literal theme hex, not CSS vars.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/lib/assessments.ts src/routes/_app/assessments.tsx</automated>
  </verify>
  <acceptance_criteria>
    - /assessments route resolves; routeTree.gen.ts includes it and tsc is green
    - Version-list table shows date/version/overall-average/report-link; heading is "Your assessment history (N)"; N=0 renders the empty state (no "(0)")
    - Comparison table pivots dimension_scores: 6 dimension-name rows, one column per version, score cells
    - Loading and error (with Retry) states use the UI-SPEC copy + #991B1B/#FEE2E2 convention; eslint clean
  </acceptance_criteria>
  <done>The history page renders the version list and per-dimension comparison from the 15-02 endpoint with correct empty/loading/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard history link (D-17) + explicit confirmed retake dialog (D-13/D-14)</name>
  <files>frontend/src/routes/_app/dashboard.tsx</files>
  <read_first>
    - frontend/src/routes/_app/dashboard.tsx (current initiative-card view + Start/Retake button + inline Alert error convention)
    - frontend/src/routes/_app/admin.index.tsx (Modal.confirm({title,content,onOk}) call shape at line 119 to copy)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Copywriting Contract ("Start new assessment" dialog title/body/confirm/cancel, "View assessment history" link), §Component Inventory (first-ever-draft skips the dialog)
  </read_first>
  <action>
    In dashboard.tsx add a "View assessment history" secondary/link-style antd Button beside the existing Start/Retake Questionnaire button that navigates to /assessments (D-17). Change the Start/Retake handler: when initiative.status === "submitted", open Modal.confirm (copying admin.index.tsx's call shape) with the locked copy — title "Start a new assessment?", body "This creates a new, permanent version in your history. Your previous submitted assessment stays unchanged, and you'll answer all 52 questions again from scratch — nothing carries over.", confirm "Start new assessment", cancel "Cancel" — and only navigate to /questionnaire onOk (D-13/D-14). When there is no prior submission (first-ever draft), navigate straight through with no dialog. Keep the existing inline Alert error convention. Use literal theme hex, not CSS vars.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/routes/_app/dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard shows a "View assessment history" link/button navigating to /assessments
    - On a submitted initiative, Start/Retake opens Modal.confirm with the exact locked title/body/confirm/cancel copy and only navigates on confirm
    - First-ever-draft (unsubmitted) case navigates straight to /questionnaire with no dialog
    - tsc + eslint clean
  </acceptance_criteria>
  <done>Retaking is an explicit, confirmed, permanence-communicating action, and the dashboard links to the history view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → history API | The history page consumes an owner-scoped endpoint; the client must not assume access to arbitrary initiative ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Information Disclosure (IDOR) | /assessments consuming GET /initiatives/{id}/assessments | high | mitigate | Access control is enforced server-side (15-02: ownership re-derivation → 403/404); the page only ever requests the current user's own initiative id and surfaces the server's 403/404 rather than assuming access — no client-side authorization is trusted as the control |
| T-15-SC | Tampering | npm installs | low | accept | No new npm packages introduced (antd Table/Modal already installed) — no install task, no legitimacy checkpoint |
</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit` → green (route registered)
- `cd frontend && npx eslint src/lib/assessments.ts src/routes/_app/assessments.tsx src/routes/_app/dashboard.tsx` → clean
- `cd frontend && npx vitest run` → existing suite green
- Manual UAT (per 15-VALIDATION.md): retake confirmation dialog + blank-draft behavior; history list + comparison table across multiple versions
</verification>

<success_criteria>
The user can open a dedicated history page listing their submitted versions with a per-dimension comparison table, and retaking requires an explicit confirmation that communicates permanence and no carry-over.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file `frontend/src/lib/assessments.ts` (`fetchAssessmentHistory`, `AssessmentSummary` type)
- New route `frontend/src/routes/_app/assessments.tsx` (history list + comparison table) + `routeTree.gen.ts` registration
- Extended `frontend/src/routes/_app/dashboard.tsx` (history link + confirmed retake dialog)

<output>
Create `.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-05-SUMMARY.md` when done
</output>
