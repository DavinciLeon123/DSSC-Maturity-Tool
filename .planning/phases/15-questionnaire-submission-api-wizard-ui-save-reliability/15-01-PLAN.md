---
phase: 15-questionnaire-submission-api-wizard-ui-save-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/assessment.py
  - backend/alembic/versions/j1a2b3c4d5e6_assessment_version_lastviewed.py
  - backend/app/api/v1/questionnaire.py
  - backend/app/schemas/questionnaire.py
  - backend/tests/api/test_questionnaire_answers.py
  - backend/tests/schemas/test_questionnaire_schemas.py
  - backend/tests/migrations/test_assessment_version_migration.py
autonomous: true
requirements: [HIST-01, SAVE-03, SAVE-04]
must_haves:
  truths:
    # HIST-01 / D-15 — version increment
    - "Starting a new draft Assessment after a prior version was submitted assigns version = max(existing versions for that initiative) + 1, never a hardcoded 1 (D-15)"
    - "A retake draft is created blank — no answers are copied forward from the previous submitted assessment; version increment naturally yields an empty new draft (D-14)"
    # SAVE-03 edge coverage (probe rows 11-14)
    - "SAVE-03 boundary: with the per-user limit at 120/minute, a full 52-question session (each answer + up to 3 retries) stays well under the ceiling; the key function is exercised on every answer-save request (D-16 discretion: 120/minute per RESEARCH A1)"
    - "SAVE-03 precision: get_user_or_ip_key returns a deterministic exact string user:<email> for a valid Bearer token — distinct users never collide onto one rate-limit bucket, no truncation of the sub claim"
    - "SAVE-03 idempotency: calling get_user_or_ip_key twice for the same token returns an identical key; two saves from one user both count against that single per-user bucket"
    - "SAVE-04/D-08 backend: last_viewed_category_id persists on the draft Assessment row and survives a hard refresh / new tab (server-side, not client memory)"
    - "SAVE-04/D-08 backend: a dedicated PATCH .../last-viewed-category endpoint updates Assessment.last_viewed_category_id UNCONDITIONALLY (no answer required) — so a user who navigates to a category and views it without answering still resumes there, capturing last-VIEWED not last-ANSWERED (D-08, RESEARCH Open Question 1)"
  artifacts:
    - "backend/app/models/assessment.py — Assessment gains last_viewed_category_id: str | None and __table_args__ UniqueConstraint on (initiative_id, version)"
    - "backend/alembic/versions/<new>.py — hand-written migration: (initiative_id, version) unique constraint + nullable last_viewed_category_id column on assessment"
    - "backend/app/api/v1/questionnaire.py — get_user_or_ip_key key function; version-increment in _get_or_create_draft_assessment; dedicated PATCH .../last-viewed-category endpoint that writes last_viewed_category_id unconditionally"
    - "backend/app/schemas/questionnaire.py — LastViewedCategoryUpdate request schema (category_id: str) for the dedicated last-viewed-category endpoint"
    - "backend/tests/migrations/test_assessment_version_migration.py — upgrade/downgrade verification against seeded data"
  key_links:
    - "questionnaire.py imports app.core.security.decode_access_token (reused, not a second JWT parser) for the rate-limit key function"
    - "version-increment IntegrityError-catch-and-requery is guarded by the new (initiative_id, version) unique constraint declared on the model AND in the migration"
  prohibitions:
    - "Must NOT copy or pre-fill answers from a previously submitted assessment into a new retake draft — a silent carry-forward could mask an unreviewed question as already answered"
    - "Must NOT overwrite or mutate a previously submitted Assessment row when starting or saving a retake — prior versions are immutable history"
  flagged_edge_assumptions:
    - "[EDGE UNRESOLVED — flagged per specless-fallback §A, NOT auto-resolved] HIST-01 unclassified probe: review manually — the edge probe could not classify HIST-01; the version-increment race (concurrent first-answer PUTs of a new retake) is mitigated by the (initiative_id,version) constraint + requery, but the exact concurrent-double-click ordering guarantee is surfaced here as an open item rather than asserted"
security_enforcement: enabled
---

<objective>
Add the backend data-integrity spine for versioned retakes and reliable saves: (1) HIST-01/D-15 — compute and increment Assessment.version on draft creation instead of always defaulting to 1; (2) SAVE-03 — replace the IP-keyed answer-save rate limit with a per-authenticated-user key function; (3) SAVE-04/D-08 — persist "last viewed category" server-side via a dedicated PATCH endpoint that writes it unconditionally (independent of answer saves) so a hard-refresh resumes at the last category the user was VIEWING, not merely the last one they answered in. All three require a single hand-written Alembic migration (the [BLOCKING] task) that adds a (initiative_id, version) unique constraint and a nullable last_viewed_category_id column to the assessment table.

Purpose: These are the write-side invariants everything else in the phase depends on — the version number must be assigned correctly and race-safely, and the rate limit must not throttle a user's own legitimate autosave burst.
Output: Modified Assessment model, one new migration, modified questionnaire.py, and Wave-0 unit/migration tests for the new logic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-CONTEXT.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Assessment model change + hand-written Alembic migration (D-08, Pitfall 4)</name>
  <files>backend/app/models/assessment.py, backend/alembic/versions/j1a2b3c4d5e6_assessment_version_lastviewed.py, backend/tests/migrations/test_assessment_version_migration.py</files>
  <read_first>
    - backend/app/models/assessment.py (the model being modified)
    - backend/alembic/versions/i9d7e6f5a4b3_questionnaire_v2_schema_migration.py (Phase 13's hand-written precedent — revision/down_revision chaining, op.create_unique_constraint / op.add_column style, lossy-downgrade documentation convention; this is the current head, revision "i9d7e6f5a4b3")
    - backend/tests/migrations/test_v1_archive_migration.py (isolated per-test PostgresContainer + alembic.command.upgrade/downgrade pattern to mirror)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"D-15: Version Increment Logic" and §"D-08" (migration note: bundle both changes in one migration, hand-written not autogenerate)
  </read_first>
  <action>
    On the Assessment SQLModel (backend/app/models/assessment.py): add field last_viewed_category_id: str | None = Field(default=None) (D-08); add __table_args__ = (UniqueConstraint("initiative_id", "version", name="uq_assessment_version_per_initiative"),) importing UniqueConstraint from sqlalchemy so create_all()-based test DBs enforce it too (the existing draft-uniqueness index is migration-only; this constraint must be model-declared to be testable). Do NOT change existing fields.
    Hand-write a new Alembic migration file with revision id j1a2b3c4d5e6 and down_revision "i9d7e6f5a4b3" (the current head). upgrade(): op.add_column on assessment adding last_viewed_category_id as sa.String(), nullable=True; op.create_unique_constraint("uq_assessment_version_per_initiative", "assessment", ["initiative_id", "version"]). downgrade(): reverse both (drop constraint then drop column). Do NOT use autogenerate — write it by hand per the i9d7e6f5a4b3 precedent. Document downgrade behavior in the module docstring.
    Run the migration against a throwaway Postgres to prove it applies: use the isolated-per-test PostgresContainer pattern from test_v1_archive_migration.py, calling alembic.command.upgrade to head then downgrade one step then upgrade again. Add test_assessment_version_migration.py asserting: empty-DB upgrade creates the column + constraint; a second Assessment row with a duplicate (initiative_id, version) raises IntegrityError; upgrade/downgrade/upgrade round-trip succeeds.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/migrations/test_assessment_version_migration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with revision "j1a2b3c4d5e6", down_revision "i9d7e6f5a4b3"; `cd backend && uv run alembic heads` shows a single head
    - Assessment model has last_viewed_category_id and the uq_assessment_version_per_initiative UniqueConstraint in __table_args__
    - test_assessment_version_migration.py passes: duplicate (initiative_id, version) insert raises IntegrityError; round-trip upgrade/downgrade/upgrade succeeds
  </acceptance_criteria>
  <done>Migration written by hand, applied and reversed cleanly against a real Postgres, and the model enforces the version-uniqueness constraint in both prod (migration) and test (create_all) paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Version-increment (D-15/HIST-01), per-user rate-limit key (SAVE-03), dedicated last-viewed-category endpoint (D-08)</name>
  <files>backend/app/api/v1/questionnaire.py, backend/app/schemas/questionnaire.py</files>
  <read_first>
    - backend/app/api/v1/questionnaire.py (the file being modified — _get_or_create_draft_assessment lines 41-87, upsert_answer lines 90-181, limiter line 19/93)
    - backend/app/core/security.py (decode_access_token — reuse verbatim, do NOT write a second JWT parser)
    - backend/app/schemas/questionnaire.py (AnswerCreate/AnswerRead precedent — add the new LastViewedCategoryUpdate here in the same style)
    - backend/app/api/v1/questionnaire.py get_answers (lines 184-213) — the ownership-check + draft-lookup pattern the new PATCH endpoint must mirror
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"D-15: Version Increment Logic" (exact modified function), §"Pitfall 1" (key function shape), §"SAVE-03: Per-User Rate Limiting", §"D-08" and §"Open Questions" #1 (dedicated endpoint recommendation over piggyback)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md §questionnaire.py (imports, race-safe pattern, key-function snippet)
  </read_first>
  <behavior>
    - get_user_or_ip_key(request): valid Bearer token → returns "user:<sub>" where sub is the JWT email; missing/malformed/expired token → falls back to get_remote_address(request)
    - _get_or_create_draft_assessment: when no draft exists, next version = (max version across ALL assessments for the initiative, or 0) + 1; on IntegrityError from the (initiative_id, version) constraint, rollback + requery the draft and return it, else re-raise
    - PATCH .../last-viewed-category: for an owned initiative, resolves the draft Assessment and sets last_viewed_category_id = body.category_id UNCONDITIONALLY (no answer needed), commits, returns the updated value; foreign initiative → 403, missing → 404 (mirrors get_answers ownership logic). This is the exact-resume write path — it captures last-VIEWED, not last-ANSWERED (D-08, RESEARCH Open Question 1)
  </behavior>
  <action>
    Add import: from app.core.security import decode_access_token; from sqlalchemy import func.
    Define module-level get_user_or_ip_key(request: Request) -> str per RESEARCH Pitfall 1: strip the "Bearer " prefix from the Authorization header, call decode_access_token, return f"user:{sub}" if a sub is returned, else get_remote_address(request). Replace limiter = Limiter(key_func=get_remote_address) with key_func=get_user_or_ip_key. Raise the decorator ceiling from 60/minute to 120/minute on upsert_answer (per RESEARCH A1 — a legitimate 52-answer session plus retries stays well under this; note the [ASSUMED] value inline).
    In _get_or_create_draft_assessment: before constructing the new Assessment, query func.max(Assessment.version) filtered by initiative_id, compute next_version = (max or 0) + 1, and pass version=next_version to Assessment(...). Keep the existing IntegrityError-catch-and-requery block exactly; it now also defends the new (initiative_id, version) constraint (D-15/Pitfall 4).
    D-08 (dedicated endpoint, per RESEARCH Open Question 1's resolution): add a LastViewedCategoryUpdate schema (category_id: str) to backend/app/schemas/questionnaire.py, then add a new route PATCH /questionnaire/initiatives/{initiative_id}/last-viewed-category to questionnaire.py. It re-derives ownership exactly like get_answers (404 if no initiative, 403 if not current_user's), resolves the draft Assessment via _get_or_create_draft_assessment, sets last_viewed_category_id = body.category_id unconditionally, commits, and returns the updated value (e.g. {"last_viewed_category_id": ...}). Apply @limiter.limit with the same per-user get_user_or_ip_key at a generous ceiling (e.g. 120/minute) since it fires on every category change. Do NOT gate the write on an answer existing — a bare navigation must persist. Do NOT piggyback the last-viewed write onto upsert_answer (the answer-save path stays unchanged for last-viewed) — the dedicated endpoint is the single, exact write path so a navigate-without-answer + refresh resumes correctly (removes the approximation the piggyback-only approach would leave).
    Do NOT change the ownership check, the config cross-validation, or the pg_insert upsert shape of upsert_answer.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q</automated>
  </verify>
  <acceptance_criteria>
    - get_user_or_ip_key returns "user:<email>" for a token minted by create_access_token and an IP string for a request with no/garbled Authorization header
    - After submitting version 1 and starting a fresh draft, the new draft's version == 2 (never 1)
    - upsert_answer decorator reads @limiter.limit("120/minute"); limiter uses key_func=get_user_or_ip_key
    - A PATCH .../last-viewed-category route exists and updates Assessment.last_viewed_category_id unconditionally (no answer required); upsert_answer contains no last_viewed_category_id write (single dedicated write path)
    - ruff/mypy clean: `cd backend && uv run ruff check . && uv run mypy app --ignore-missing-imports`
  </acceptance_criteria>
  <done>Version numbers increment race-safely, the rate limit is keyed per user (falling back to IP for unauthenticated requests), and last-viewed-category is written via a dedicated PATCH endpoint on every category change (exact resume, not an approximation).</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 tests — version increment + key function + update version==1 assertions (SAVE-03, HIST-01)</name>
  <files>backend/tests/api/test_questionnaire_answers.py, backend/tests/schemas/test_questionnaire_schemas.py</files>
  <read_first>
    - backend/tests/api/test_questionnaire_answers.py (existing characterization tests + _login helper + make_user/make_initiative factories)
    - backend/tests/schemas/test_questionnaire_schemas.py (any assertion hardcoding version == 1)
    - backend/app/api/v1/questionnaire.py (get_user_or_ip_key signature, as modified in Task 2)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-VALIDATION.md §"Wave 0 Requirements" (exact cases required)
    - config/dssc-questionnaire.json (question/category ids: q-1-1..q-6-8, cat-1..cat-6; scores 1-5) — for building a submitted assessment if needed
  </read_first>
  <action>
    Add to test_questionnaire_answers.py: a test marked so `-k rate_limit` selects it, calling get_user_or_ip_key directly (not via HTTP) with (a) a Request stub carrying a real Bearer token from create_access_token → asserts key == "user:<email>", and (b) a Request stub with no Authorization header → asserts it falls back to an IP key. Add a test selectable by `-k version_increment`: create an initiative, save one answer (draft version 1), flip that assessment to submitted, then save a new answer → assert a second draft exists with version == 2 and the first row is untouched.
    Add a test selectable by `-k last_viewed`: create an initiative with a draft assessment but save NO answers, PATCH .../last-viewed-category with a category_id → assert the draft Assessment.last_viewed_category_id equals that category_id (proves the write is unconditional / independent of any answer save, closing the D-08 last-VIEWED-vs-last-ANSWERED gap); also assert a foreign user's PATCH returns 403.
    Update any existing assertion in test_questionnaire_answers.py / test_questionnaire_schemas.py that assumes version == 1 unconditionally so it reflects the increment logic. Do NOT weaken the existing ownership/lazy-create characterization tests.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_questionnaire_answers.py -k "rate_limit or version_increment or last_viewed" -x -q && cd backend && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/api/test_questionnaire_answers.py -k rate_limit -x` passes (direct unit test of the key function, per RESEARCH Pitfall 1)
    - `pytest tests/api/test_questionnaire_answers.py -k version_increment -x` passes (asserts version == 2 on retake)
    - `pytest tests/api/test_questionnaire_answers.py -k last_viewed -x` passes (PATCH updates last_viewed_category_id with zero answers saved; foreign user → 403)
    - Full quick suite green; no existing test left asserting an unconditional version == 1
  </acceptance_criteria>
  <done>The new key function and version-increment logic have direct automated coverage and the existing suite is updated to the new behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → answer-save API | Authenticated PUT crosses here; JWT in Authorization header is the identity source |
| application → database | Version assignment + uniqueness invariant enforced at the DB layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Spoofing / DoS | get_user_or_ip_key rate-limit key | medium | mitigate | Key derived from a signature-verified JWT (decode_access_token validates the signature before returning sub), never a spoofable raw header; unauthenticated/forged tokens fall back to IP keying, so a fresh-key-per-request bypass is not possible |
| T-15-03 | Tampering (data integrity) | Assessment version assignment | medium | mitigate | New (initiative_id, version) unique constraint (model __table_args__ + migration) + IntegrityError-catch-and-requery in _get_or_create_draft_assessment prevents duplicate/skipped versions under a concurrent retake race (Pitfall 4) |
| T-15-SC | Tampering | pip installs | low | accept | No new packages introduced this phase (RESEARCH Package Legitimacy Audit = N/A); decode_access_token/func are already-installed PyJWT/SQLAlchemy — no install task, no legitimacy checkpoint required |
</threat_model>

<verification>
- `cd backend && uv run alembic heads` → single head (j1a2b3c4d5e6)
- `cd backend && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q` → green
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports` → clean
- **Flagged-edge sign-off (must_haves.flagged_edge_assumptions):** before writing 15-01-SUMMARY.md, the executor must explicitly record a human-visible disposition for the flagged HIST-01 concurrent-retake-race edge item (the `[EDGE UNRESOLVED]` entry) — either "verified acceptable (constraint + requery covers it)" or "carry forward as a known open item" — so it gets a conscious sign-off rather than being silently dropped.
</verification>

<success_criteria>
Retaking assigns a strictly-incrementing version race-safely; the answer-save rate limit is per-user; last-viewed-category is persisted server-side; migration applies and reverses cleanly.
</success_criteria>

## Artifacts this phase produces (this plan)

- New migration `backend/alembic/versions/j1a2b3c4d5e6_assessment_version_lastviewed.py`
- New DB column `assessment.last_viewed_category_id` (nullable String)
- New DB constraint `uq_assessment_version_per_initiative` on `assessment(initiative_id, version)`
- New function `get_user_or_ip_key` in `backend/app/api/v1/questionnaire.py`
- New route `PATCH /questionnaire/initiatives/{initiative_id}/last-viewed-category` (unconditional last-viewed write) + `LastViewedCategoryUpdate` schema
- Modified `_get_or_create_draft_assessment` (version-increment) behavior
- New test file `backend/tests/migrations/test_assessment_version_migration.py`

<output>
Create `.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-01-SUMMARY.md` when done
</output>
