---
phase: 15-questionnaire-submission-api-wizard-ui-save-reliability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/questionnaire.ts
  - frontend/src/hooks/useDebouncedSave.ts
  - frontend/src/components/questionnaire/AnswerButtonGroup.tsx
  - frontend/src/components/questionnaire/QuestionCard.tsx
  - frontend/src/components/questionnaire/StepPills.tsx
  - frontend/src/components/questionnaire/ContextCallout.tsx
  - frontend/src/components/questionnaire/FollowupPanel.tsx
autonomous: true
requirements: [QSTN-02, SAVE-01, SAVE-02]
must_haves:
  truths:
    # QSTN-02 edge coverage (probe rows 1-5) + UI considerations
    - "QSTN-02 boundary: each question renders exactly 5 options mapped to scores 1..5 (both extremes present); no 6th option and no score 0/null-score option is ever rendered"
    - "QSTN-02 adjacency: the 5 radio circles sit in one horizontal row with even spacing (sm/8px gap) and do not overlap or collide at the 1100px-max wizard card width"
    - "QSTN-02 empty: a question with no saved answer renders all 5 circles unselected (value null); config always supplies exactly 5 options so the empty-options case cannot occur"
    - "QSTN-02 ordering: options render in config array order (score 1 leftmost → score 5 rightmost), stable across renders"
    - "QSTN-02 precision: the captured value is the exact integer score from the selected option's config {label, score} pair — no rounding or coercion at capture"
    - "QSTN-02 UI long-text (option labels): config-driven labels wrap to up to 2 lines under/beside each circle and are never ellipsis-truncated (truncation could hide a maturity-level's meaning)"
    - "QSTN-02 UI long-text (question text): question text wraps normally at 16px/1.5 line-height with no max-line clamp"
    - "D-04 progress: StepPills shows a category stepper AND an overall 'N of 52 answered' counter derived from config, never a hardcoded 52"
    # SAVE-01 edge coverage (probe rows 6-8)
    - "SAVE-01 adjacency: useDebouncedSave keys its debounce timer per question_id, so answering question B does not cancel or reset question A's pending save"
    - "SAVE-01 empty: selecting one answer schedules exactly one save; no selection fires no save (there is no deselect on a radio scale)"
    - "SAVE-01 ordering: changing the same question twice within the debounce window results in only the latest value being saved (last-write-wins per question)"
    # SAVE-02 edge coverage
    - "SAVE-02: the retry-with-backoff state machine performs 3 automatic attempts (~1s/2s/4s) then transitions to a terminal failed state; a 429 routes to a distinct non-escalating rate-limited state and does not consume an auto-retry attempt (RESEARCH Pattern 3)"
  artifacts:
    - "frontend/src/lib/questionnaire.ts — rewritten types (AnswerOption/Question/QuestionnaireConfig) + saveAnswer/fetchAnswers/fetchQuestionnaireConfig retyped + flushAnswerBeacon (fetch keepalive)"
    - "frontend/src/hooks/useDebouncedSave.ts — per-question debounce timer with schedule/flush/flushAll + retry-with-backoff state machine"
    - "frontend/src/components/questionnaire/AnswerButtonGroup.tsx — rebuilt as horizontal RadioScale (5 config-driven circles, 44px hit target)"
    - "frontend/src/components/questionnaire/QuestionCard.tsx — rebuilt: question text + RadioScale only (followup branch removed)"
    - "frontend/src/components/questionnaire/StepPills.tsx — extended with 'N of 52 answered' counter"
  key_links:
    - "RadioScale option list is question.options ?? config.default_options — config is the source of truth, never a hardcoded array"
    - "flushAnswerBeacon uses native fetch with keepalive:true and an Authorization header (NOT sendBeacon — it cannot carry the header/PUT), bypassing the shared axios instance (D-07)"
    - "ContextCallout.tsx and FollowupPanel.tsx are deleted (orphaned by the new config schema — no props source remains)"
  prohibitions:
    - "Must NOT re-introduce var(--color-navy)/var(--border-radius-sm) CSS-custom-property usage — use the literal theme.ts hex (#06004f navy, #399e5a green, 8px/16px radius) to stop the pre-existing token drift (UI-SPEC Known token drift)"
security_enforcement: enabled
---

<objective>
Rebuild the wizard's answer plumbing as one cohesive unit sharing the new type contract: the QSTN-02 horizontal 5-circle radio scale (config-driven labels → scores 1-5), the per-question debounced save + retry-with-backoff hook (SAVE-01/SAVE-02), and the supporting question card / progress components. Also delete the two components orphaned by the new config schema.

Purpose: These are the shared primitives WizardPage (15-04) and the history page (15-05) build on; they must all agree on the Phase-13 backend shape (question_id/category_id/score:1-5), which the current pre-Phase-13 types (YES/NOT_THERE_YET/mami_code) do not.
Output: Rewritten questionnaire.ts, new useDebouncedSave hook, rebuilt RadioScale/QuestionCard/StepPills, deleted ContextCallout/FollowupPanel.

Note on compile state: this plan intentionally leaves WizardPage.tsx (rebuilt in 15-04) referencing the old removed types, so project-wide `tsc -b` is NOT green until 15-04 lands. This plan's automated gate is eslint on the changed files + the existing Vitest suite; the full tsc gate is the Wave-2 exit criterion in 15-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-CONTEXT.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite questionnaire.ts types + API wrappers + flushAnswerBeacon (D-07), new assessments-agnostic; new useDebouncedSave hook (SAVE-01/SAVE-02)</name>
  <files>frontend/src/lib/questionnaire.ts, frontend/src/hooks/useDebouncedSave.ts</files>
  <read_first>
    - frontend/src/lib/questionnaire.ts (current file — keep the thin api-wrapper convention, replace the type shapes)
    - frontend/src/lib/api.ts (shared axios instance + base URL / auth header convention)
    - config/dssc-questionnaire.json (authoritative shape: version, default_options[{label,score}], categories[{id,name,questions[{id,category_id,text,options?}]}])
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"Pattern 1" (interfaces), §"Pattern 2" (useDebouncedSave), §"Pattern 3" (retry state machine), §"D-07" (flushOnUnload / fetch keepalive)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Copywriting Contract (badge states) and §Color (amber vs red)
  </read_first>
  <action>
    Rewrite questionnaire.ts: export interface AnswerOption {label: string; score: number}; Question {id; category_id; text; options?: AnswerOption[]}; QuestionnaireConfig {version; default_options: AnswerOption[]; categories: {id; name; questions: Question[]}[]}; AnswerRead matching the backend (id, assessment_id, question_id, category_id, score, answered_at, updated_at). Retype fetchQuestionnaireConfig(): Promise<QuestionnaireConfig>, fetchAnswers(initiativeId): Promise<AnswerRead[]>, saveAnswer(initiativeId, questionId, {question_id, category_id, score}) — thin wrappers over the shared api axios instance. Remove all old types (AnswerValue, mami_code, Followup, Topic, old Category/Question). Add flushAnswerBeacon(initiativeId, questionId, categoryId, score): native fetch to the PUT answers endpoint with keepalive:true, Content-Type application/json, and Authorization Bearer header read from the auth store — NOT sendBeacon, NOT the axios instance (D-07); .catch swallow (tab is closing).
    Create hooks/useDebouncedSave.ts per RESEARCH Pattern 2 + Pattern 3: schedule(questionId, score) sets a per-questionId setTimeout (~1500ms, splitting the 1-2s D-05 range); flush(questionId) sends the latest pending value; flushAll() flushes every pending question (returns a Promise for D-06 Next/Back). Wrap each save in the Pattern 3 retry ladder (RETRY_DELAYS_MS = [1000,2000,4000], 3 attempts) exposing a state callback ("saving"|"retrying"|"saved"|"failed"|"rate-limited"); a 429 goes straight to "rate-limited" (non-escalating), other errors escalate through the ladder to terminal "failed". Use useRef for the per-question timer/pending maps so re-renders do not reset timers.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/lib/questionnaire.ts src/hooks/useDebouncedSave.ts</automated>
  </verify>
  <acceptance_criteria>
    - questionnaire.ts exports AnswerOption/Question/QuestionnaireConfig with no residual mami_code/AnswerValue/Followup/Topic references (grep clean, excluding comments)
    - flushAnswerBeacon calls native fetch with keepalive:true and an Authorization header; does NOT use navigator.sendBeacon
    - useDebouncedSave keys timers per question_id and implements the 3-attempt [1000,2000,4000] ladder with a distinct non-escalating 429 path
    - eslint clean on both files
  </acceptance_criteria>
  <done>Types match the live backend; the debounce+retry hook exists with per-question timers and the correct backoff/429 semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AnswerButtonGroup → horizontal RadioScale + rebuild QuestionCard + extend StepPills (QSTN-02, D-04)</name>
  <files>frontend/src/components/questionnaire/AnswerButtonGroup.tsx, frontend/src/components/questionnaire/QuestionCard.tsx, frontend/src/components/questionnaire/StepPills.tsx</files>
  <read_first>
    - frontend/src/components/questionnaire/AnswerButtonGroup.tsx (existing selected/unselected visual language to preserve — filled circle + border swap)
    - frontend/src/components/questionnaire/QuestionCard.tsx (existing card layout; the followup branch to remove)
    - frontend/src/components/questionnaire/StepPills.tsx (existing category stepper to extend, not replace)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Spacing Scale (44px hit-target exception; visible circle 28-32px), §Color, §Typography, §Component Inventory
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"Pattern 1"
  </read_first>
  <action>
    Rebuild AnswerButtonGroup.tsx as a horizontal RadioScale: render options = question.options ?? config.default_options in a flex row, each option a column of (a 44px-hit-target button role="radio" aria-checked with a visible 28-32px circle inside) + its config label below/beside. Selected-but-unsaved uses navy #06004f border+fill; selected-and-saved-confirmed uses green #399e5a (state passed in from the parent); unselected stays neutral/white with a subtle border. Labels wrap to up to 2 lines, never ellipsis. onChange(score) fires the integer score. Use literal theme hex, not CSS vars.
    Rebuild QuestionCard.tsx: render question.text (16px/1.5, wrap normally) + the RadioScale; drop the showFollowup/FollowupPanel branch and the ContextCallout usage entirely — a question is now just text + scale.
    Extend StepPills.tsx: keep the numbered-circle category stepper (checkmark on complete), and add a small "N of 52 answered" counter (D-04) near the top, where the total (52) is derived from config (sum of questions across categories), never hardcoded. Keep the existing visual language.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/components/questionnaire/AnswerButtonGroup.tsx src/components/questionnaire/QuestionCard.tsx src/components/questionnaire/StepPills.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RadioScale renders exactly 5 options from config in a horizontal row, each with a >=44px hit target and a config-driven wrapping label; option order follows config (score 1 → 5)
    - QuestionCard renders only question text + RadioScale (no followup/context-callout imports remain)
    - StepPills shows both the category stepper and a config-derived "N of 52 answered" counter
    - No var(--color-*)/var(--border-radius-*) usages introduced; eslint clean
  </acceptance_criteria>
  <done>The QSTN-02 radio scale and progress components are rebuilt config-driven, horizontal, and within the 2-weight/theme-hex UI contract.</done>
</task>

<task type="auto">
  <name>Task 3: Delete orphaned ContextCallout and FollowupPanel components</name>
  <files>frontend/src/components/questionnaire/ContextCallout.tsx, frontend/src/components/questionnaire/FollowupPanel.tsx</files>
  <read_first>
    - frontend/src/components/questionnaire/ContextCallout.tsx and FollowupPanel.tsx (confirm no remaining importers after Task 2)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Component Inventory ("Drop" list)
  </read_first>
  <action>
    Delete ContextCallout.tsx and FollowupPanel.tsx (git rm). These are orphaned by the new config schema (no context_text/context_image or followup fields exist anymore). Confirm via grep that QuestionCard (rebuilt in Task 2) no longer imports them; do NOT touch FindingsPanel.tsx (pre-existing orphan, out of this phase's file list per UI-SPEC).
  </action>
  <verify>
    <automated>cd frontend && ! grep -rn "ContextCallout\|FollowupPanel" src --include=*.tsx --include=*.ts | grep -v '^Binary'</automated>
  </verify>
  <acceptance_criteria>
    - Both files removed from the working tree
    - No remaining import or JSX reference to ContextCallout or FollowupPanel anywhere in src
    - FindingsPanel.tsx untouched
  </acceptance_criteria>
  <done>The two schema-orphaned components are gone with no dangling references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → answer-save API (unload path) | flushAnswerBeacon sends an authenticated PUT that must outlive the page context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Information Disclosure | flushAnswerBeacon token handling | high | mitigate | Uses fetch(keepalive:true) with the JWT in an Authorization header — never a token-in-URL/query-string workaround (which sendBeacon would have forced); the token never appears in the URL, browser history, or access logs (RESEARCH D-07) |
| T-15-SC | Tampering | npm installs | low | accept | No new npm packages introduced (RESEARCH: debounce/retry/keepalive all hand-rolled with built-ins; no lodash/use-debounce added) — no install task, no legitimacy checkpoint required |
</threat_model>

<verification>
- `cd frontend && npx eslint src/lib/questionnaire.ts src/hooks/useDebouncedSave.ts src/components/questionnaire/` → clean
- `cd frontend && npx vitest run` → existing TopNav suite still green (no new frontend tests this phase — deferred to Phase 17 per CONTEXT.md boundary)
- Full `cd frontend && npx tsc -b --noEmit` green is the Wave-2 exit gate (15-04), not this plan's gate (see objective note)
</verification>

<success_criteria>
The wizard answer primitives (types, debounce+retry hook, radio scale, question card, progress) match the Phase-13 backend shape and the UI contract; orphaned components are removed.
</success_criteria>

## Artifacts this phase produces (this plan)

- Rewritten `frontend/src/lib/questionnaire.ts` (new `AnswerOption`/`Question`/`QuestionnaireConfig` types, `flushAnswerBeacon`)
- New hook `frontend/src/hooks/useDebouncedSave.ts`
- Rebuilt `AnswerButtonGroup.tsx` (RadioScale), `QuestionCard.tsx`, extended `StepPills.tsx`
- Deleted `ContextCallout.tsx`, `FollowupPanel.tsx`

<output>
Create `.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-03-SUMMARY.md` when done
</output>
