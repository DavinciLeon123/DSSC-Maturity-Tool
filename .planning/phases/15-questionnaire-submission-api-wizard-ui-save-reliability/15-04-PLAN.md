---
phase: 15-questionnaire-submission-api-wizard-ui-save-reliability
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-03]
files_modified:
  - frontend/src/components/questionnaire/WizardPage.tsx
  - frontend/src/routes/_app/questionnaire.tsx
autonomous: true
requirements: [SAVE-01, SAVE-02, SAVE-04, HIST-01]
must_haves:
  truths:
    # Wizard flow / navigation decisions
    - "Wizard paginates one category per page (6 pages, 8-9 questions each) — no topic sub-pagination, matching the flat new config (D-01)"
    - "Within a category page questions can be answered in any order; Next is enabled only once every question on that page has an answer (D-02)"
    - "Back navigation is free — the user can return to any earlier category and change an already-saved answer before submission (D-03)"
    # SAVE-01 integration
    - "Selecting an answer schedules a debounced save ~1.5s later via useDebouncedSave, without requiring Next/Back (SAVE-01/D-05)"
    - "Clicking Next or Back calls flushAll() to send any pending debounced save immediately before navigating (D-06)"
    # SAVE-02 / retry / blocking
    - "On save failure the AutosaveBadge shows transient amber 'Save failed — retrying' during the 3 auto attempts, then terminal red 'Save failed' with a 'Retry save' button (D-09)"
    - "A terminal-failed save blocks Next AND Submit until it succeeds; there is no dismiss/continue-anyway path (D-10/D-11/D-12)"
    - "The final-category CTA reads 'Submit assessment →' and Submit composes with (does not replace) the backend 422 completion gate (D-12)"
    # SAVE-04 / resume
    - "A beforeunload handler fires flushAnswerBeacon (fetch keepalive) for any still-pending answer before the tab closes/refreshes (D-07/SAVE-04)"
    - "On return after tab close/hard-refresh the wizard resumes on the last-viewed category (from Assessment.last_viewed_category_id) with all saved answers pre-filled, and writes last-viewed-category on each category change (D-08)"
    # UI considerations (covered)
    - "Wizard initial mount shows a full-panel loading state until config + saved answers + last-viewed-category all resolve (never category 1 then a jump-cut to the real position)"
    - "Wizard mount fetch error uses the same #991B1B-on-#FEE2E2 banner + Retry convention as autosave failures (no separate error language)"
    - "The 'partial' page state keeps Next visually disabled (rgba(6,0,79,0.05) bg / rgba(6,0,79,0.3) text) until every question on the page is answered — no extra 'X remaining' copy beyond the overall counter"
    - "Autosave loading uses the existing 'Saving...' badge; confirmed uses green 'Saved ✓' (unchanged copy)"
  key_links:
    - "WizardPage consumes useDebouncedSave (15-03) for scheduling, flushAll for Next/Back, and flushAnswerBeacon (15-03) for the beforeunload listener"
    - "isNextDisabled gains badgeState === terminal-failed (NOT the transient retrying/rate-limited states) so only a real terminal failure blocks navigation"
    - "AutosaveBadge is extended (not replaced) to distinguish transient amber from terminal red within the existing failed state (D-09)"
  prohibitions:
    - "Must NOT allow Next or Submit to proceed while any answer save is in a terminal-failed state — retry is the only way past a failed save, even for a demo (D-10/D-11/D-12, strict reading of SAVE-02)"
    - "Must NOT await an async save inside the beforeunload handler — it must synchronously fire flushAnswerBeacon (keepalive), since browsers do not wait for in-flight promises on unload (RESEARCH Pitfall 2)"
    - "Must NOT batch multiple answers into one beforeunload keepalive payload (RESEARCH Pitfall 3 — keep it one small per-question request)"
  backstops:
    - statement: "SAVE-02 concurrency: if a save is in flight and the user re-answers the same question, the newer debounced save supersedes the older and terminal-failure still blocks navigation so no divergent parallel state can be committed"
      verification: backstop
  flagged_edge_assumptions:
    - "[EDGE UNRESOLVED — flagged per specless-fallback §A, NOT auto-resolved] SAVE-04 unclassified probe: review manually — beforeunload/keepalive is best-effort (bfcache disabled, may not fire for OS-killed mobile tabs per RESEARCH D-07 note); the exact guarantee when the OS kills a backgrounded tab before the debounce fires is surfaced here as an open item, not asserted. Manual UAT (VALIDATION.md) is the check."
security_enforcement: enabled
---

<objective>
Rebuild WizardPage.tsx end-to-end for the new schema and the save-reliability contract: per-answer debounced autosave (SAVE-01/D-05), real retry-with-backoff wired into the AutosaveBadge with terminal-failure blocking of Next/Submit (SAVE-02/D-09/D-10/D-11/D-12), a beforeunload keepalive flush (SAVE-04/D-07), resume-at-last-viewed-category (D-08), one-category-per-page navigation (D-01/D-02/D-03), and the "Submit assessment →" rename. Wire the questionnaire route to supply the resume position and saved answers.

Purpose: This is the user-facing heart of the phase — where "answers save reliably in the background" and "no silent lost save" actually become true.
Output: Rebuilt WizardPage.tsx, updated questionnaire.tsx route; project-wide tsc green (Wave-2 exit gate for the frontend rebuild).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-CONTEXT.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md
@.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md
@frontend/src/lib/questionnaire.ts
@frontend/src/hooks/useDebouncedSave.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild WizardPage — category-per-page nav (D-01/02/03), debounced save + Next/Back flush (SAVE-01/D-05/D-06), retry + terminal-block (SAVE-02/D-09/10/11/12)</name>
  <files>frontend/src/components/questionnaire/WizardPage.tsx</files>
  <read_first>
    - frontend/src/components/questionnaire/WizardPage.tsx (current file — AutosaveBadge lines 19-82, useMutation onError 429 branch lines 136-173, isNextDisabled lines 211-213, nav-button style lines 606-643, submitted-confirmation lines 347-421)
    - frontend/src/lib/questionnaire.ts (new types + saveAnswer + flushAnswerBeacon, from 15-03)
    - frontend/src/hooks/useDebouncedSave.ts (schedule/flush/flushAll + retry state, from 15-03)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §Copywriting Contract, §Color (amber vs red), §Visual Hierarchy, §Component Inventory (extend AutosaveBadge, not replace)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-PATTERNS.md §WizardPage.tsx (which existing pieces carry over)
  </read_first>
  <action>
    Rebuild WizardPage to iterate the flat new config: state categoryIndex only (no topicIndex); currentCategory = config.categories[categoryIndex]; render every question in that category via the rebuilt QuestionCard + RadioScale. Initialize localAnswers from savedAnswers (question_id → score). handleAnswerChange sets local state and calls useDebouncedSave.schedule(questionId, score). isNextDisabled = not-every-question-on-page-answered (D-02) OR badge is in terminal-failed state (D-10). Back is free (D-03), disabled only on the first category. handleNext/handleBack await flushAll() before changing categoryIndex (D-06). Final category CTA reads "Submit assessment →" (D-12 rename from Finish); Submit calls the existing /initiatives/{id}/submit and is blocked while any save is terminal-failed or pending (D-12), on top of the backend 422 gate.
    Extend AutosaveBadge to distinguish transient (amber #F59E0B "Save failed — retrying" / "Too many saves — slow down") from terminal (red #991B1B "Save failed") within the failed concept, and render a "Retry save" button + an inline "This answer didn't save. Retry before continuing." banner next to the blocked Next/Submit on terminal failure (UI-SPEC copy). Wire the hook's retry state into badgeState; 429 → rate-limited (transient, self-clears), other exhausted failures → terminal. Keep the submitted-confirmation view; update its copy per UI-SPEC. Use literal theme hex, no CSS vars.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/components/questionnaire/WizardPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WizardPage compiles against the new questionnaire.ts types; project-wide `tsc -b --noEmit` is green (Wave-2 frontend gate)
    - Selecting an answer schedules a debounced save; Next/Back call flushAll() first
    - Terminal-failed badge state disables Next and Submit and renders a "Retry save" button; transient retrying/rate-limited states do NOT disable Next
    - Final CTA reads "Submit assessment →"; no CSS-var color usages introduced; eslint clean
  </acceptance_criteria>
  <done>The wizard saves per-answer with visible retry, blocks navigation on unrecoverable failure with no override, and navigates one category per page.</done>
</task>

<task type="auto">
  <name>Task 2: beforeunload keepalive flush (SAVE-04/D-07) + resume-at-last-viewed-category (D-08) + mount loading/error states</name>
  <files>frontend/src/components/questionnaire/WizardPage.tsx, frontend/src/routes/_app/questionnaire.tsx</files>
  <read_first>
    - frontend/src/routes/_app/questionnaire.tsx (current route wrapping WizardPage — how it fetches config/answers)
    - frontend/src/lib/questionnaire.ts (flushAnswerBeacon signature, fetchAnswers, fetchQuestionnaireConfig)
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-RESEARCH.md §"D-07: beforeunload Flush Mechanism" (the flushOnUnload/useEffect listener), §"D-08" (last-viewed column), §"Pitfall 2/3"
    - .planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-UI-SPEC.md §UI Considerations (loading/error mount rows)
  </read_first>
  <action>
    In WizardPage add a useEffect that registers a window "beforeunload" listener firing flushAnswerBeacon (keepalive) synchronously for each answer still in the hook's pending map (D-07/SAVE-04) — do NOT await, do NOT batch (Pitfall 2/3); remove the old useRef unmount fire-and-forget save (superseded). Initialize categoryIndex from a lastViewedCategoryId prop (mapped to its index; default 0 if null) so the wizard resumes where the user was (D-08); write last-viewed-category to the backend whenever categoryIndex changes (via the answer-save piggyback field from 15-01, and/or a lightweight save call on category change) so a refresh with no new answer still resumes correctly.
    In questionnaire.tsx: fetch config + saved answers + the initiative's last_viewed_category_id, show a full-panel loading state until ALL resolve (never render category 1 then jump), and on fetch error render the shared #991B1B-on-#FEE2E2 banner + Retry (same convention as autosave). Pass savedAnswers and lastViewedCategoryId into WizardPage.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit && npx eslint src/components/questionnaire/WizardPage.tsx src/routes/_app/questionnaire.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A "beforeunload" listener calls flushAnswerBeacon (keepalive) per pending answer, never awaits, never batches; the old unmount useRef save is removed
    - WizardPage starts on the last-viewed category (via prop) and updates last-viewed-category on category change
    - questionnaire.tsx shows a single full-panel loading state until config + answers + last-viewed resolve, and a Retry error banner on failure
    - tsc + eslint clean
  </acceptance_criteria>
  <done>Closing/refreshing the tab does not lose the last answer, and returning resumes at the correct category with a clean loading/error mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → answer-save API (unload) | beforeunload flush sends an authenticated PUT that must survive page teardown |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Information Disclosure | beforeunload flush | high | mitigate | The unload flush uses flushAnswerBeacon (fetch keepalive) with the JWT in an Authorization header — the token is never placed in the URL/query string, so it cannot leak into browser history or access logs (RESEARCH D-07) |
| T-15-SC | Tampering | npm installs | low | accept | No new npm packages introduced (debounce/retry/keepalive use React + fetch built-ins) — no install task, no legitimacy checkpoint |
</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit` → green (project-wide; this is the Wave-2 frontend exit gate that 15-03 deferred)
- `cd frontend && npx eslint src/components/questionnaire/WizardPage.tsx src/routes/_app/questionnaire.tsx` → clean
- `cd frontend && npx vitest run` → existing suite green
- Manual UAT (per 15-VALIDATION.md): radio-scale render, ~1.5s autosave, failed-save block+retry, beforeunload survival, resume-at-category
</verification>

<success_criteria>
Answers autosave in the background with visible, blocking failure handling; tab close/refresh does not lose the last answer; the user resumes at their last-viewed category with answers pre-filled.
</success_criteria>

## Artifacts this phase produces (this plan)

- Rebuilt `frontend/src/components/questionnaire/WizardPage.tsx` (debounced save, retry+terminal-block, beforeunload keepalive, resume)
- Updated `frontend/src/routes/_app/questionnaire.tsx` (loading/error mount, passes savedAnswers + lastViewedCategoryId)

<output>
Create `.planning/phases/15-questionnaire-submission-api-wizard-ui-save-reliability/15-04-SUMMARY.md` when done
</output>
