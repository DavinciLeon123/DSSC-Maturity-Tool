---
phase: 16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping
plan: 06
type: execute
wave: 3
depends_on: ["16.2-02", "16.2-03", "16.2-05"]
files_modified: []
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5]

must_haves:
  truths:
    - "A human has visually confirmed the homepage/About/wizard changes against a real running app, not just typecheck/lint — colors, gradients, font, and interactive flows (welcome-screen refresh behavior in particular) can only be verified by looking at rendered output"
  artifacts: []
  key_links: []
---

<objective>
Get human sign-off that all 5 of Phase 16.2's success criteria hold on a real running app,
closing out the phase — everything automatable (typecheck, lint, build, config/copy grep checks,
backend config regression tests) was already done in Waves 1-2; this plan is the one thing that
cannot be automated: does it actually look and behave right.

Purpose: this phase is almost entirely visual/UX content (colors, gradients, font, copy, and one
genuinely stateful interaction — the welcome-screen refresh-persistence behavior from RESEARCH.md
Pitfall 1). Static checks (grep, typecheck) prove the code contains the right strings and compiles,
but cannot prove a gradient renders correctly, that text is legible, or that a page reload
actually re-shows the welcome screen. Matches the precedent set by Phase 16 (16-05) and Phase 16.1,
both of which ended in a human-verify checkpoint for the same category of change.
Output: explicit user confirmation (or a list of concrete visual/behavioral gaps to fix) before
this phase is marked complete.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-CONTEXT.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-SOURCE-CONTENT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human visual + behavioral confirmation of all 5 success criteria</name>
  <what-built>
Font/color/logo foundation app-wide (16.2-01); new DSMA homepage + footer copy and dssc.eu-style
hero/footer color treatment (16.2-02); new About page copy and an app-wide dssc.eu gradient
background applied via the shared authenticated layout (16.2-03); config-driven per-dimension
intro text + 16-subsection question grouping (16.2-04); a pre-question welcome screen gated on the
existing fresh-draft/resume signal, a dimension-intro box, and subsection-grouped question
rendering in the wizard (16.2-05).
  </what-built>
  <how-to-verify>
Launch the app locally (frontend `npm run dev` against a running backend, or the deployed
Integration environment if preferred) and check each of the following:

**1. Homepage (`/`) — success criterion 1**
- Hero shows "DSMA – Data Spaces Maturity Assessment" as the heading, the new subheading text, and
  "Start the assessment" as the primary CTA; "Create an account" secondary CTA is present and
  legible.
- Hero background is a subtle white-to-green-to-blue diagonal wash, not a solid blue block; heading
  text is near-black (ink), not white.
- Logo in the header is the real DSSC logo (not the Phase 16.1 placeholder) at roughly 75px tall;
  hero shows the color logo variant (not white-on-white).
- "How it works" step 1 reads "Register your data space" / "Create an account and register your
  Data Space (DS)."; steps 2/3 bodies match the new copy; closing banner reads "The DSMA gives you
  an immediate overview of your current maturity level." with the new supporting sentence (no
  mention of "six categories" or "maturity radar").
- Footer background is dark navy, logo is ~50px tall (white variant), tagline reads "Data Space
  Support Center – Tool Powered by the Centre of Excellence for Data Sharing and Cloud".
- Whole page renders in the Jost font (visually distinct from the old Open Sans — check via
  browser devtools computed font-family if unsure).

**2. About page (`/about`, logged in) — success criterion 2**
- Heading reads "About Data Space Maturity Assessment Tool"; body is exactly 2 paragraphs matching
  the new copy (CEN/CLC/TS 18331:2026 standard; Co-created by the Dutch Centre of Excellence...).
- Text is ink-colored, not the old bright blue.
- Background behind the content (and behind every other logged-in page — dashboard, wizard, admin)
  now shows the subtle dssc.eu gradient wash instead of the old flat green tint — confirm this is
  visible on at least one other authenticated page too, since this was an intentional app-wide
  change.

**3. Assessment welcome screen — success criterion 3**
- Start a brand-new assessment (new account/initiative, or use "Retake" if available): the welcome
  screen appears first, with all 5 sections (Welcome / What you will assess / What you will
  receive / Before you start / Time required) and a "Begin assessment" button — not a category
  page, and not counted in any "Question X of Y" indicator.
- **Critical regression check (Pitfall 1):** while the welcome screen is showing, refresh the
  browser tab (before clicking "Begin"). Confirm the welcome screen still shows after reload — if
  it jumps straight to the Governance category instead, this is a blocking gap (the
  `saveLastViewedCategory` guard from 16.2-05 Task 1 is not working).
- Click "Begin assessment": lands on Governance category 1. Confirm there is no "back" control that
  returns to the welcome screen from Governance (or any later category).
- Answer a question, refresh the page again: confirm you resume on the correct category — not the
  welcome screen.

**4. Per-dimension intro text — success criterion 4**
- On each of the 6 dimension pages (Governance, Business, Legal, Interoperability, Control over
  Data & Trust, Value Creation), confirm a visually distinct intro box appears near the top with
  that dimension's own paragraph (per `16.2-SOURCE-CONTENT.md` §3), styled differently from the
  question cards below it (muted color / smaller text), and that it's still showing if you navigate
  away and back to that category.

**5. Question subsection grouping — success criterion 5**
- On each dimension page, confirm the questions are visibly grouped under small uppercase
  sub-header labels matching `16.2-SOURCE-CONTENT.md` §5 (e.g. Governance shows "Governance
  Framework Establishment" above Q1-4 and "Participation Management" above Q5-8), that the labels
  don't visually compete with the dimension's own `<h3>` title, and that "Question X of Y" still
  counts across the whole dimension (not reset per subsection).

Report back "approved" if all 5 areas check out, or describe exactly which item(s) failed (page +
what you saw vs. expected) so a gap-closure plan can be scoped precisely.
  </how-to-verify>
  <action>Human verification step — follow the numbered checks in &lt;how-to-verify&gt; above against a real running app (local dev server or the deployed Integration environment). No code changes are made in this task.</action>
  <verify>Manually exercise all 5 checklist areas above (homepage, About page, welcome screen incl. the Pitfall-1 refresh regression check, dimension intro text, subsection grouping). Confirm each passes.</verify>
  <done>All 5 success-criteria areas confirmed working on a real running app; reviewer types "approved".</done>
  <resume-signal>Reply "approved" to close out Phase 16.2, or describe the specific visual/behavioral issues found (which page, what's wrong) for a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
This plan's only task is the human-verify checkpoint above — there is no additional automated
verification beyond what Waves 1-2 already ran (typecheck/lint/build/backend config tests).
</verification>

<success_criteria>
The user has explicitly confirmed all 5 of Phase 16.2's success criteria hold against a real
running app. Phase 16.2 can then be marked complete in ROADMAP.md/STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-06-SUMMARY.md`
</output>
