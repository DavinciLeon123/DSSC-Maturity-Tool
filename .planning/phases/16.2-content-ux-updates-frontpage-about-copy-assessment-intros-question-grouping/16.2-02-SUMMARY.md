---
phase: 16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping
plan: 02
subsystem: ui
tags: [react, tanstack-router, dssc-rebrand, css-in-js]

# Dependency graph
requires:
  - phase: 16.2-01
    provides: Jost font stack + --dssc-* CSS custom property token set (ink, fg-2, hero-gradient, accent-gradient, bg-dark) in globals.css, color/white logo PNGs already dssc.eu-accurate
provides:
  - Homepage (frontend/src/routes/index.tsx) rebranded with verbatim DSMA copy (hero, how-it-works, closing banner) and dssc.eu visual treatment (gradient-wash hero, ink text, color logo, gradient-clip H1 accent)
  - Footer.tsx rebranded with dssc.eu dark-navy background, 50px logo, new tagline copy
affects: [16.2-03, 16.2-05, 16.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Gradient-clip text accent (WebkitBackgroundClip/backgroundClip: 'text' + transparent color) applied inline via style={{}}, matching this codebase's established no-CSS-module convention"

key-files:
  created: []
  modified:
    - frontend/src/routes/index.tsx
    - frontend/src/components/layout/Footer.tsx

key-decisions:
  - "Wrapped the ENTIRE h1 text ('DSMA – Data Spaces Maturity Assessment') in the gradient-clip span, not just the 'Data Spaces Maturity Assessment' substring the plan's <interfaces> text suggested — a tag boundary between plain text and a nested span would have broken the plan's own literal-string grep verification and must_haves artifact 'contains' check, both of which require the full phrase contiguous in the raw source. Prioritized the plan's explicit machine-checked verification over the softer 'reasonable substring' wording."

requirements-completed: [SC1]

# Metrics
duration: 15min
completed: 2026-08-04
---

# Phase 16.2 Plan 02: Homepage + Footer DSMA Rebrand Summary

**Homepage hero/how-it-works/closing-banner rewritten with verbatim DSMA copy and dssc.eu's gradient-wash-on-white visual language (replacing the old solid-blue MAMI hero); Footer switched to dssc.eu's dark-navy background with the new tagline.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-08-04T07:15:00+02:00 (approx.)
- **Completed:** 2026-08-04T07:30:04+02:00
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- Homepage hero now shows "DSMA – Data Spaces Maturity Assessment" (gradient-clip accent), the new subheading, and "Start the assessment" CTA, on a white/paper background with dssc.eu's subtle green→blue diagonal gradient wash instead of a flat blue block
- Hero logo swapped from the white variant to the color variant (required once the background went light); heading/body text recolored to dssc.eu ink (`#1c2025`) / fg-2 (`#3d444b`)
- "How it works" step 1/2 copy updated verbatim (step 3 titles unchanged per the mapping table); closing banner h2/p rewritten verbatim, dropping the old six-category/maturity-radar sentence entirely, and its background swapped to the same gradient wash for visual consistency
- Footer background changed from bright blue (`#008ecf`) to dssc.eu dark navy (`#0f1c2b`), logo resized 32px→50px, and tagline replaced with "Data Space Support Center – Tool Powered by the Centre of Excellence for Data Sharing and Cloud" at the locked `rgba(255,255,255,0.85)` text opacity

## Task Commits

Each task was committed atomically:

1. **Task 1: Rework index.tsx — hero/how-it-works/closing-banner copy + dssc.eu color treatment** - `91692fd` (feat)
2. **Task 2: Rework Footer.tsx — dark-navy background, logo sizing, new tagline** - `d2ac640` (feat)

**Plan metadata:** (this commit) `docs(16.2-02): complete homepage + footer DSMA rebrand plan`

## Files Created/Modified
- `frontend/src/routes/index.tsx` - Homepage: DSMA hero copy, gradient-wash background, ink text, color hero logo, gradient-clip H1 accent, restyled secondary CTA, updated how-it-works/closing-banner copy and background, 75px header logo
- `frontend/src/components/layout/Footer.tsx` - Dark-navy (#0f1c2b) footer background, 50px logo, new tagline copy at 0.85 text opacity

## Decisions Made
- Wrapped the full h1 text in the gradient-clip span rather than only "Data Spaces Maturity Assessment" (see key-decisions above) — the plan's own `<verification>` block requires `grep -n "DSMA – Data Spaces Maturity Assessment"` to match the raw file, which is only possible if the phrase is contiguous (no tag boundary splitting it). This still satisfies the plan's stated goal ("dssc.eu itself does this" gradient-clip effect on the H1) and its `must_haves.artifacts[0].contains` check exactly.
- Removed the now-unused `logoWhite` import from `index.tsx` after swapping the hero logo to `logoSrc` — leaving it would have failed the `tsc`/`eslint` unused-import check (Rule 3, blocking).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed unused `logoWhite` import in index.tsx**
- **Found during:** Task 1 (hero logo swap)
- **Issue:** Plan's step 2 says to swap the hero `<img>` from `logoWhite` to `logoSrc`; after that swap `logoWhite` had zero remaining references in the file, which `eslint`/`tsc` would flag
- **Fix:** Deleted the `import logoWhite from '../assets/logo-dssc-white.png';` line
- **Files modified:** frontend/src/routes/index.tsx
- **Verification:** `npm run typecheck` and `npm run lint` both pass clean
- **Committed in:** `91692fd` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary mechanical cleanup from the plan's own instructed logo swap. No scope creep.

## Issues Encountered
None beyond the gradient-clip span-scope decision documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Homepage and Footer fully match the dssc.eu-aligned copy/visual spec locked in `16.2-CONTEXT.md`; Phase 16.2 success criterion 1 satisfied
- Ran concurrently with 16.2-03 (about.tsx/_app.tsx) and 16.2-05 (WizardPage.tsx, new WelcomeScreen.tsx/SubsectionLabel.tsx) in the same working tree; this plan's two commits (`91692fd`, `d2ac640`) were verified via `git show --stat` to contain only their intended files — no git-index race like Wave 1 hit this time
- `npm run typecheck`, `npm run lint`, and `npm run build` all pass clean on the final tree state for this plan's files
- No blockers for 16.2-06 or downstream phases

---
*Phase: 16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping*
*Completed: 2026-08-04*

## Self-Check: PASSED

- FOUND: frontend/src/routes/index.tsx
- FOUND: frontend/src/components/layout/Footer.tsx
- FOUND: .planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-02-SUMMARY.md
- FOUND: 91692fd (Task 1 commit)
- FOUND: d2ac640 (Task 2 commit)
