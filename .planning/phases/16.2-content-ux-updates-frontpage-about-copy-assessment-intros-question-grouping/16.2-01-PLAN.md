---
phase: 16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/globals.css
  - frontend/src/lib/theme.ts
  - frontend/src/assets/fonts/Jost-VariableFont_wght.ttf
  - frontend/src/assets/logo-dssc-color.png
  - frontend/src/assets/logo-dssc-white.png
  - frontend/src/components/layout/Footer.tsx
  - frontend/src/components/layout/TopNav.tsx
  - frontend/src/components/questionnaire/AnswerButtonGroup.tsx
  - frontend/src/components/questionnaire/StepPills.tsx
  - frontend/src/components/questionnaire/WizardPage.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/_app/about.tsx
  - frontend/src/routes/_app/admin.heatmap.tsx
  - frontend/src/routes/_app/admin.index.tsx
  - frontend/src/routes/_app/assessments.tsx
  - frontend/src/routes/_app/dashboard.tsx
  - frontend/src/routes/_app/initiative.tsx
  - frontend/src/routes/_app/questionnaire.tsx
  - frontend/src/routes/_app/report.tsx
  - frontend/src/routes/_auth/forgot-password.tsx
  - frontend/src/routes/_auth/login.tsx
  - frontend/src/routes/_auth/register.tsx
  - frontend/src/routes/_auth/reset-password.tsx
autonomous: true
requirements: [SC1, SC2]

must_haves:
  truths:
    - "Every page in the app renders text in the Jost font family, self-hosted (not pulled from Google Fonts CDN)"
    - "The real DSSC logo (pulled from dssc.eu) appears wherever the app shows a logo, not the Phase 16.1 placeholder"
    - "The shared TopNav header logo (visible on every authenticated page, not just the homepage) renders at dssc.eu's 75px sizing, not the old 36px"
    - "No component in the app still requests 'Open Sans' as its font-family"
  artifacts:
    - path: "frontend/src/assets/fonts/Jost-VariableFont_wght.ttf"
      provides: "Self-hosted variable font file, served from the Vite asset pipeline"
    - path: "frontend/src/styles/globals.css"
      provides: "@font-face for Jost (font-weight 100 900) + --dssc-* CSS custom properties (ink, paper, bg-dark, hero gradient, accent gradient); Google Fonts @import removed"
      contains: "@font-face"
    - path: "frontend/src/lib/theme.ts"
      provides: "antd ThemeConfig fontFamily updated to the Jost stack"
      contains: "Jost"
  key_links:
    - from: "frontend/src/styles/globals.css"
      to: "frontend/src/assets/fonts/Jost-VariableFont_wght.ttf"
      via: "@font-face src: url(...) — relative import resolved by Vite's asset pipeline"
      pattern: "url\\(.*Jost-VariableFont"
    - from: "the 18 mechanically-edited ts/tsx files"
      to: "--font-family / Jost stack"
      via: "literal string replace of the old 'Open Sans', sans-serif value"
      pattern: "'Jost', 'Helvetica Neue', Arial, sans-serif"
---

<objective>
Establish the app-wide visual foundation this phase's content work builds on: self-hosted Jost
(replacing the Google-Fonts-CDN Open Sans/Jost mix), the real DSSC brand color tokens/gradients
from dssc.eu, and the actual DSSC logo assets — landing this first so every later plan in this
phase (Homepage, About, Wizard) edits files that already carry the new font/logo/tokens instead of
each plan reinventing the same swap.

Purpose: CONTEXT.md locks "Jost app-wide" and "centralize colors into shared tokens" as decisions
that span the whole app, not just the two pages named in this phase's title — landing this as a
dedicated Wave-1 foundation (rather than duplicating it inside the Homepage/About/Wizard plans)
avoids the same ~126-occurrence mechanical edit being redone three times and avoids file-ownership
conflicts between plans that would otherwise all touch WizardPage.tsx/index.tsx/about.tsx at once.
Output: globals.css/theme.ts carrying the real dssc.eu tokens; the Jost .ttf self-hosted; both logo
PNGs replaced with the real dssc.eu-sourced assets; zero remaining 'Open Sans' font-family
references anywhere in frontend/src.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-CONTEXT.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-RESEARCH.md

Reference assets (already downloaded to this phase's directory, verbatim copies of the live
dssc.eu assets — use as-is, do not re-fetch or re-derive):
- `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/reference-assets/Jost-VariableFont_wght.ttf`
- `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/reference-assets/dssc-logo-color.png`
- `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/reference-assets/dssc-logo-white.png`
</context>

<interfaces>
Current `frontend/src/styles/globals.css` (full file, 22 lines):
```css
@import url('https://fonts.googleapis.com/css2?family=Jost:wght@500;600;700&family=Open+Sans:wght@300;400;600;700&display=swap');

:root {
  --color-navy:       #008ecf;
  --color-green:      #76b82a;
  --color-blue:       #008ecf;
  --color-text-gray:  #4A495B;
  --color-bg-light:   #F5F5F8;
  --font-family:      'Open Sans', sans-serif;
  --border-radius-sm: 6px;
  --border-radius-lg: 30px;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: var(--font-family);
  color: var(--color-text-gray);
  background: var(--color-bg-light);
  font-size: 16px;
  line-height: 22px;
}
```

Current `frontend/src/lib/theme.ts` (full file, 37 lines) — `fontFamily` and `colorText` are the
two lines this plan changes; everything else (colorPrimary/colorSuccess/borderRadius/components
block) is untouched:
```ts
export const mamiTheme: ThemeConfig = {
  token: {
    colorPrimary: '#008ecf',
    colorSuccess: '#76b82a',
    colorLink: '#76b82a',
    fontFamily: "'Open Sans', -apple-system, BlinkMacSystemFont, sans-serif",
    borderRadius: 8,
    borderRadiusLG: 16,
    colorBgLayout: '#ffffff',
    colorBgContainer: '#ffffff',
    colorText: '#008ecf',
  },
  ...
};
```

Confirmed via repo-wide grep (2026-08-03): every one of the other 18 files below contains the
literal substring `'Open Sans', sans-serif` (single-quoted font name, always followed by exactly
`, sans-serif`) one or more times, in `fontFamily: "'Open Sans', sans-serif"` or similar inline
`style={{}}` attributes — no other spacing/quoting variant exists in this set:
`frontend/src/components/layout/Footer.tsx`, `TopNav.tsx`,
`frontend/src/components/questionnaire/AnswerButtonGroup.tsx`, `StepPills.tsx`, `WizardPage.tsx`,
`frontend/src/routes/index.tsx`, `frontend/src/routes/_app/about.tsx`, `admin.heatmap.tsx`,
`admin.index.tsx`, `assessments.tsx`, `dashboard.tsx`, `initiative.tsx`, `questionnaire.tsx`,
`report.tsx`, `frontend/src/routes/_auth/forgot-password.tsx`, `login.tsx`, `register.tsx`,
`reset-password.tsx`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Self-host Jost, add dssc.eu color/gradient tokens, replace logo assets, resize TopNav logo</name>
  <files>frontend/src/styles/globals.css, frontend/src/lib/theme.ts, frontend/src/assets/fonts/Jost-VariableFont_wght.ttf, frontend/src/assets/logo-dssc-color.png, frontend/src/assets/logo-dssc-white.png, frontend/src/components/layout/TopNav.tsx</files>
  <action>
1. Create `frontend/src/assets/fonts/` and copy
   `.planning/phases/16.2-.../reference-assets/Jost-VariableFont_wght.ttf` into it verbatim (no
   re-encoding). This mirrors how the two existing logo PNGs are already imported in this repo — a
   plain asset under `src/assets/`, picked up automatically by Vite's asset pipeline with zero
   `vite.config.ts` changes.

2. Overwrite `frontend/src/assets/logo-dssc-color.png` and `frontend/src/assets/logo-dssc-white.png`
   with the reference-assets versions of the same names (same filename, new file content — every
   existing `import logoSrc from '../assets/logo-dssc-color.png'` / `logoWhite from
   '.../logo-dssc-white.png'` import site keeps working unmodified; this task does not touch any
   `.tsx` import statement).

3. Replace `frontend/src/styles/globals.css` in full with:
```css
@font-face {
  font-family: 'Jost';
  src: url('../assets/fonts/Jost-VariableFont_wght.ttf') format('truetype-variations');
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
}

:root {
  --color-navy:       #008ecf;
  --color-green:      #76b82a;
  --color-blue:       #008ecf;
  --color-text-gray:  #4A495B;
  --color-bg-light:   #F5F5F8;
  --font-family:      'Jost', 'Helvetica Neue', Arial, sans-serif;
  --border-radius-sm: 6px;
  --border-radius-lg: 30px;

  /* DSSC brand tokens, fetched directly from https://dssc.eu/'s compiled CSS (Phase 16.2) */
  --dssc-blue:   #008ecf;
  --dssc-green:  #76b82a;
  --dssc-indigo: #50568e;
  --dssc-yellow: #fbbc43;
  --dssc-slate:  #627179;
  --dssc-black:  #111315;
  --dssc-ink:    #1c2025;  /* main heading/body text — dssc.eu's "fg-1" */
  --dssc-fg-2:   #3d444b;  /* secondary/muted body text */
  --dssc-white:  #fff;
  --dssc-paper:  #fafbfc;
  --dssc-bg-dark: #0f1c2b; /* footer / dark-band background */
  --dssc-hero-gradient: linear-gradient(135deg, rgba(118,184,42,0.06) 0%, rgba(0,142,207,0.10) 60%, rgba(255,255,255,0) 100%);
  --dssc-accent-gradient: linear-gradient(135deg, var(--dssc-green) 0%, var(--dssc-blue) 100%);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: var(--font-family);
  color: var(--color-text-gray);
  background: var(--color-bg-light);
  font-size: 16px;
  line-height: 22px;
}
```
   Note the Google Fonts `@import` line (which pulled both Jost and Open Sans from Google's CDN) is
   deleted outright, not edited — self-hosting was the explicit locked decision specifically to
   drop the CDN dependency.

4. In `frontend/src/lib/theme.ts`, change only two lines inside `mamiTheme.token`:
   - `fontFamily: "'Open Sans', -apple-system, BlinkMacSystemFont, sans-serif"` →
     `fontFamily: "'Jost', 'Helvetica Neue', Arial, sans-serif"`
   - `colorText: '#008ecf'` → `colorText: '#1c2025'` — this is a deliberate discretionary call
     (flagged in CONTEXT.md as "Claude's Discretion: whether antd's ThemeConfig also gets
     updated"): `colorText` was blue app-wide, which predates this rebrand; aligning it to the new
     dssc.eu ink color is consistent with the locked "main heading/body text color: ink #1c2025"
     decision and the "centralize colors into shared tokens" decision. Do not change
     `colorPrimary`/`colorSuccess`/`colorLink`/`borderRadius*`/the `components` block — those are
     out of scope for this plan.
   Also update the file's header comment (currently "DSSC brand tokens: Blue #008ecf (primary),
   Green #76b82a (secondary), Jost+Open Sans typography") to drop the now-inaccurate "Open Sans"
   mention.

5. Resize the shared header logo in `frontend/src/components/layout/TopNav.tsx`. This component
   renders on every authenticated page (dashboard, wizard, admin, About, etc.) — the "logo same
   size as dssc.eu" locked decision (CONTEXT.md) applies here too, not just to the homepage's own
   header (Plan 02) and footer (Plan 02). Currently (line 52):
   ```tsx
   <img src={logoSrc} alt="DSSC" height={36} />
   ```
   Change to `height={75}` to match dssc.eu's own header logo sizing. The enclosing `<header>`
   (line 38-49) has a fixed `height: '64px'`, which is shorter than a 75px-tall logo and would
   visually clip/overflow it — bump that to `height: '96px'` in the same edit so the larger logo
   has comfortable vertical padding (matches the ~44px of padding-per-side dssc.eu's own 75px logo
   sits within its own taller header). Do not change any other style property on the header (padding,
   background, borderBottom, sticky positioning all stay as-is).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>globals.css has no Google Fonts @import, has a self-hosted @font-face for Jost, and defines the --dssc-* tokens; theme.ts's fontFamily is the Jost stack and colorText is #1c2025; both logo PNGs are the new dssc.eu-sourced files (byte content changed, filenames unchanged); TopNav.tsx's logo renders at 75px tall inside a 96px header; typecheck/lint both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical 'Open Sans' → Jost replace across the remaining 18 files</name>
  <files>frontend/src/components/layout/Footer.tsx, frontend/src/components/layout/TopNav.tsx, frontend/src/components/questionnaire/AnswerButtonGroup.tsx, frontend/src/components/questionnaire/StepPills.tsx, frontend/src/components/questionnaire/WizardPage.tsx, frontend/src/routes/index.tsx, frontend/src/routes/_app/about.tsx, frontend/src/routes/_app/admin.heatmap.tsx, frontend/src/routes/_app/admin.index.tsx, frontend/src/routes/_app/assessments.tsx, frontend/src/routes/_app/dashboard.tsx, frontend/src/routes/_app/initiative.tsx, frontend/src/routes/_app/questionnaire.tsx, frontend/src/routes/_app/report.tsx, frontend/src/routes/_auth/forgot-password.tsx, frontend/src/routes/_auth/login.tsx, frontend/src/routes/_auth/register.tsx, frontend/src/routes/_auth/reset-password.tsx</files>
  <action>
Do a literal, mechanical string replace of `'Open Sans', sans-serif` → `'Jost', 'Helvetica Neue',
Arial, sans-serif` across exactly the 18 files listed above (theme.ts was already handled in Task
1 — do not touch it again here). This is a pure find/replace with no logic changes: every
occurrence is inside an inline `fontFamily: "..."` style value.

Use a scripted replace (Bash tool — this environment's Bash runs Git Bash, so `sed -i` is
available) rather than 18 manual edits, e.g.:
```bash
for f in \
  frontend/src/components/layout/Footer.tsx \
  frontend/src/components/layout/TopNav.tsx \
  frontend/src/components/questionnaire/AnswerButtonGroup.tsx \
  frontend/src/components/questionnaire/StepPills.tsx \
  frontend/src/components/questionnaire/WizardPage.tsx \
  frontend/src/routes/index.tsx \
  frontend/src/routes/_app/about.tsx \
  frontend/src/routes/_app/admin.heatmap.tsx \
  frontend/src/routes/_app/admin.index.tsx \
  frontend/src/routes/_app/assessments.tsx \
  frontend/src/routes/_app/dashboard.tsx \
  frontend/src/routes/_app/initiative.tsx \
  frontend/src/routes/_app/questionnaire.tsx \
  frontend/src/routes/_app/report.tsx \
  frontend/src/routes/_auth/forgot-password.tsx \
  frontend/src/routes/_auth/login.tsx \
  frontend/src/routes/_auth/register.tsx \
  frontend/src/routes/_auth/reset-password.tsx ; do
  sed -i "s/'Open Sans', sans-serif/'Jost', 'Helvetica Neue', Arial, sans-serif/g" "$f"
done
```
After running the replace, grep the whole `frontend/src` tree for the literal string `Open Sans`
— it must return zero matches anywhere (not just in the 18 files above; this also confirms Task 1
left no stray reference in globals.css/theme.ts). Do not touch any non-font-family code in these
files — this task is a pure string substitution, no JSX structure or logic changes.
  </action>
  <verify>
    <automated>! grep -rn "Open Sans" frontend/src && cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Zero occurrences of the string "Open Sans" remain anywhere under frontend/src; every previously-affected file now requests the Jost stack; typecheck/lint both pass with no new errors.</done>
</task>

</tasks>

<verification>
- `grep -rn "Open Sans" frontend/src` returns no results.
- `frontend/src/styles/globals.css` contains `@font-face` and no `fonts.googleapis.com` reference.
- `cd frontend && npm run build` succeeds (confirms the font asset resolves through Vite's build, not just dev server).
</verification>

<success_criteria>
Every text-bearing element in the app is styled with the Jost font stack via either inheritance
(`body`/antd `ThemeConfig`) or an explicit inline override that itself now names Jost — never Open
Sans. The self-hosted `.ttf` is served from the app's own build output, not Google's CDN. Both logo
PNGs are the real dssc.eu-sourced assets. This unblocks Plans 02/03/05, which build their
copy/structure changes on top of these already-updated files.
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-01-SUMMARY.md`
</output>
