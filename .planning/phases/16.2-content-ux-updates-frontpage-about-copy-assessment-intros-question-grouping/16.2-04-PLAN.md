---
phase: 16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - config/dssc-questionnaire.json
  - frontend/src/lib/questionnaire.ts
autonomous: true
requirements: [SC4, SC5]

must_haves:
  truths:
    - "Every one of the 6 categories in the live questionnaire config carries its own intro paragraph, verbatim from the source PDF"
    - "Every one of the 6 categories carries an ordered list of named subsections whose counts sum exactly to that category's question count"
    - "Existing backend consumers of the config (scoring, report generation, the /questionnaire/config endpoint) are unaffected by the new fields — no response_model exists to break, and no consumer asserts a closed key set"
  artifacts:
    - path: "config/dssc-questionnaire.json"
      provides: "6 categories, each with an added `intro` string and `subsections` array ({name, count}[]); `questions` arrays and every existing key untouched"
      contains: "\"subsections\""
    - path: "frontend/src/lib/questionnaire.ts"
      provides: "Subsection interface + optional intro/subsections fields on Category, additive to the existing type"
      contains: "export interface Subsection"
  key_links:
    - from: "config/dssc-questionnaire.json"
      to: "frontend/src/lib/questionnaire.ts"
      via: "GET /questionnaire/config response shape — the raw JSON dict, unvalidated by any Pydantic response_model, deserialized into QuestionnaireConfig/Category on the frontend"
      pattern: "intro\\?:\\s*string"
---

<objective>
Extend the live questionnaire config with the two new content fields this phase's wizard UI work
depends on: a per-category `intro` paragraph (success criterion 4) and a per-category
`subsections` breakdown (success criterion 5) — landing this as a Wave-1 content/contract plan so
the Wizard UI plan (16.2-05) can build its rendering purely against an already-populated,
already-typed config.

Purpose: RESEARCH.md verified this is a safe, additive-only config change — `GET
/questionnaire/config` has no `response_model` (its OpenAPI schema is literally `{}`), so new keys
cannot change the docs-freshness-gated `openapi.json`, and every backend consumer (`mami_config.py`,
`dimension_scoring.py`, `report_generator.py`) accesses only specific known keys, never a closed
set. This plan is the "define contracts first" half of an interface-first split — 16.2-05
implements the consuming UI against the types/data this plan produces.
Output: `config/dssc-questionnaire.json` carrying real intro/subsections content for all 6
categories; `frontend/src/lib/questionnaire.ts` carrying the matching `Subsection` type.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-CONTEXT.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-RESEARCH.md
@.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-SOURCE-CONTENT.md

This plan has no dependency on 16.2-01 (Font/Color/Logo Foundation) — it touches neither
frontend-styled files nor any file in that plan's list, and can run in the same wave.
</context>

<interfaces>
Confirmed (2026-08-03, via direct read of `config/dssc-questionnaire.json`) the live per-category
question counts, in file order:

| `id` | `name` | question count |
|---|---|---|
| `cat-1` | Governance | 8 |
| `cat-2` | Business | 9 |
| `cat-3` | Legal | 6 |
| `cat-4` | Interoperability | 9 |
| `cat-5` | Control over Data & Trust | 9 |
| `cat-6` | Value Creation | 11 |

(8+9+6+9+9+11 = 52, matches the source doc.)

Current `frontend/src/lib/questionnaire.ts`'s relevant types (full file otherwise unchanged by this
plan — only `Category` gains two optional fields, and a new `Subsection` interface is added):
```typescript
export interface Category {
  id: string;
  name: string;
  questions: Question[];
}

export interface QuestionnaireConfig {
  version: string;
  default_options: AnswerOption[];
  categories: Category[];
}
```
Only `WizardPage.tsx` and `StepPills.tsx` import `Category` anywhere in the repo (confirmed via
grep) — `StepPills.tsx` reads only `.id`/`.name`/`.questions.length`, so adding optional fields is a
no-op there under `strict: true` TypeScript; `verbatimModuleSyntax: true` (in
`frontend/tsconfig.app.json`) means any file importing the new `Subsection` type must use
`import type { Subsection } from "../../lib/questionnaire";` (matching the existing convention
already used for `Category`/`Question` in `StepPills.tsx`).

Existing backend tests that must stay green after this change (neither asserts a closed key set):
`backend/tests/services/test_dssc_config.py` (`test_all_52_questions_present`,
`test_config_is_pure_data_no_hardcoded_labels`,
`test_served_content_is_byte_for_parsed_equal_to_raw_json_file`,
`test_load_raises_fast_on_zero_question_category`), `backend/tests/api/test_questionnaire.py`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add `intro` and `subsections` to every category in config/dssc-questionnaire.json</name>
  <files>config/dssc-questionnaire.json</files>
  <action>
For each of the 6 category objects in `config/dssc-questionnaire.json`'s `categories` array, add two
new keys — `intro` (string) and `subsections` (array of `{name, count}`) — immediately after each
category's existing `name` key and before its `questions` array. Do not reorder, rename, or remove
any existing key or any question. Use this exact content (verbatim from
`16.2-SOURCE-CONTENT.md` §3 for `intro`, §5 for `subsections` — do not paraphrase):

- **cat-1 (Governance)** — intro: "This section assesses how your data space is organized and
  managed. The questions focus on governance structures, decision-making, the rulebook,
  operational processes, participant management, and support mechanisms. Together, these elements
  demonstrate how effectively the data space can operate, onboard participants, manage change, and
  ensure long-term sustainability."
  subsections: `[{"name": "Governance Framework Establishment", "count": 4}, {"name": "Participation Management", "count": 4}]`

- **cat-2 (Business)** — intro: "This section evaluates the business foundations of your data
  space. The questions examine business goals, funding and revenue models, value propositions, use
  cases, participant engagement, data sharing activities, and the creation of value-added services.
  The objective is to understand whether the data space can deliver sustainable value to its
  participants over time."
  subsections: `[{"name": "Business Model Development", "count": 4}, {"name": "Use Case Development", "count": 5}]`

- **cat-3 (Legal)** — intro: "This section assesses the legal readiness of your data space. The
  questions focus on identifying applicable regulations, translating them into requirements,
  ensuring compliance, and establishing agreements that govern participation, data sharing, and
  service provision. Together, these elements help create a legally sound environment for trusted
  data exchange."
  subsections: `[{"name": "Regulatory Compliance", "count": 3}, {"name": "Contractual Framework", "count": 3}]`

- **cat-4 (Interoperability)** — intro: "This section evaluates how effectively participants can
  exchange and understand data. The questions cover data models, semantic standards, reference
  datasets, APIs, data exchange protocols, and mechanisms for provenance, observability, and
  traceability. These capabilities enable consistent, reliable, and scalable data sharing both
  within and across data spaces."
  subsections: `[{"name": "Shared Semantics", "count": 4}, {"name": "Interoperable Exchange", "count": 2}, {"name": "Provenance, Observability and Monitoring", "count": 3}]`

- **cat-5 (Control over Data & Trust)** — intro: "This section assesses the mechanisms that enable
  participants to share data with confidence while retaining control. The questions focus on
  digital identity, verifiable credentials, trust frameworks, compliance verification, registries,
  and the enforcement of access, usage, and consent policies. Together, these capabilities
  establish trust, security, and data sovereignty within the data space."
  subsections: `[{"name": "Identity Management and Attestation", "count": 3}, {"name": "Trust Framework", "count": 3}, {"name": "Access and Usage Policies Enforcement", "count": 3}]`

- **cat-6 (Value Creation)** — intro: "This section evaluates how your data space enables
  participants to discover, use, and create value from shared data. The questions focus on data and
  service descriptions, catalogues, value creation services, federation with other data spaces,
  participant adoption, data reuse, satisfaction, and the generation of new services and offerings.
  The goal is to understand the tangible value produced by the data space ecosystem."
  subsections: `[{"name": "Data, Services and Offerings Descriptions", "count": 1}, {"name": "Publication and Discovery", "count": 2}, {"name": "Value Creation Services", "count": 3}, {"name": "Adoption Level", "count": 5}]`

Each category's `subsections[].count` values must sum exactly to that category's existing question
count (4+4=8, 4+5=9, 3+3=6, 4+2+3=9, 3+3+3=9, 1+2+3+5=11 — matches the table in `<interfaces>`).
Validate the JSON parses after editing (e.g. `node -e "JSON.parse(require('fs').readFileSync('config/dssc-questionnaire.json','utf8'))"`).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/services/test_dssc_config.py tests/api/test_questionnaire.py -q</automated>
  </verify>
  <done>All 6 categories carry intro + subsections verbatim from the source doc; subsection counts sum correctly per category; existing backend config tests still pass unmodified (proving the change is additive-safe).</done>
</task>

<task type="auto">
  <name>Task 2: Add the Subsection type + optional Category fields to questionnaire.ts</name>
  <files>frontend/src/lib/questionnaire.ts</files>
  <action>
Add a new exported `Subsection` interface and two optional fields on the existing `Category`
interface, without touching any other type or function in this file:
```typescript
export interface Subsection {
  name: string;
  count: number;
}

export interface Category {
  id: string;
  name: string;
  intro?: string;
  subsections?: Subsection[];
  questions: Question[];
}
```
Do not make `intro`/`subsections` required — they must stay optional so any category (present or
future) without them still type-checks. Do not change `Question`, `AnswerOption`,
`QuestionnaireConfig`, or any of the exported functions (`fetchQuestionnaireConfig`,
`fetchAnswers`, `saveAnswer`, `fetchLastViewedCategory`, `saveLastViewedCategory`,
`flushAnswerBeacon`) in this file.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Subsection is exported from questionnaire.ts; Category has optional intro/subsections fields; typecheck passes with no new errors in StepPills.tsx or WizardPage.tsx (both already compile fine against the widened, backward-compatible type).</done>
</task>

</tasks>

<verification>
- `node -e "const c=require('./config/dssc-questionnaire.json'); c.categories.forEach(cat=>{const n=cat.subsections.reduce((s,x)=>s+x.count,0); if(n!==cat.questions.length) throw new Error(cat.id+' mismatch: '+n+' vs '+cat.questions.length)}); console.log('all category subsection counts match')"` prints the success message with no thrown error.
- `cd backend && uv run pytest tests/services/test_dssc_config.py tests/api/test_questionnaire.py -q` passes.
- `cd frontend && npm run typecheck` passes.
</verification>

<success_criteria>
`config/dssc-questionnaire.json` is the single source of truth for both the 6 dimension-intro
paragraphs and the 16 named subsections, with zero backend regressions; `questionnaire.ts` exposes
a typed, optional `Subsection`/`Category.intro`/`Category.subsections` contract ready for 16.2-05's
wizard UI to consume without needing any further config-shape exploration.
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-content-ux-updates-frontpage-about-copy-assessment-intros-question-grouping/16.2-04-SUMMARY.md`
</output>
