---
phase: 16-report-data-contract-dual-visualization-admin-aggregation
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - backend/app/services/admin_aggregation.py
  - backend/app/api/v1/admin.py
  - backend/tests/api/test_admin.py
autonomous: true
requirements: [ADMN-01]
must_haves:
  truths:
    - "GET /admin/heatmap returns org_average_scores (6 dimensions), org_radar_chart_svg, and a per-initiative list of rows (ADMN-01/D-07)"
    - "Each initiative contributes only its latest submitted assessment — highest version with status submitted, one row per initiative (D-08)"
    - "Initiatives with zero submitted assessments are excluded from the org average and appear in the per-initiative list with a no-data marker (has_data false) — never contributing a zero score (D-08)"
    - "When no initiative has any submitted assessment, org_radar_chart_svg is null (suppressed, not a zeroed degenerate hexagon) and org_average_scores is empty (RESEARCH Pitfall 5, ADMN-01 empty)"
    - "The org-average radar is produced by the same generate_radar_svg function used for individual reports, not a second implementation (D-01)"
    - "Each populated per-initiative row exposes the latest submitted assessment_id so the frontend can link to that initiative's report via ?assessment_id= (D-07)"
    - "Per-initiative rows with equal overall average sort stably (ADMN-01 ordering)"
  prohibitions:
    - "MUST NOT include non-submitted (draft) assessments in the org-wide average, and MUST NOT coerce a no-data initiative's scores to zero when averaging — no-data is not a score of zero (D-08)."
  artifacts:
    - backend/app/services/admin_aggregation.py (latest-submitted-per-initiative query + org average + empty guard)
    - backend/app/api/v1/admin.py (AdminAggregateResponse, AdminInitiativeAggregateRow, rebuilt /heatmap)
    - backend/tests/api/test_admin.py (aggregation + empty + exclusion cases)
  key_links:
    - "admin_aggregation reuses generate_radar_svg + get_maturity_band from report_generator (D-01 single implementation)"
---

<objective>
Rebuild the admin `/heatmap` endpoint (a fixed degraded stub since Phase 14) into a real cross-initiative aggregation over the new 6-category model: one org-wide averaged radar chart plus a per-initiative breakdown table, each initiative contributing only its latest submitted assessment, initiatives with no submitted data excluded from the average but still listed.

Purpose: ADMN-01 replaces the retired 4x3 topic heatmap with a 6-dimension aggregate view; D-07/D-08 fix the exact shape (both org radar and per-initiative rows, latest-submitted-only, zero-initiative safety).
Output: new admin_aggregation.py service; rewritten /heatmap with real response models; rewritten test_admin.py heatmap cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-CONTEXT.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-RESEARCH.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin_aggregation.py — latest-submitted-per-initiative + org average + empty guard</name>
  <files>backend/app/services/admin_aggregation.py</files>
  <read_first>
    - backend/app/api/v1/admin.py (list_initiatives L174-209 — the session.execute(text(...)) + result.mappings() raw-SQL join idiom to adapt; enum-safe raw-SQL rationale)
    - backend/app/models/assessment.py (Assessment.status submitted, version, dimension_scores JSONB)
    - backend/app/services/report_generator.py (generate_radar_svg, get_maturity_band from plan 16-01 — reuse, do not reimplement)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-RESEARCH.md (Pattern 3 LEFT JOIN LATERAL / DISTINCT ON latest-submitted query; Pitfall 5 zero-initiative guard)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md (admin.py section — zero-division/empty-aggregate guard, no analog exists, write as new code)
  </read_first>
  <behavior>
    - the latest-submitted query returns exactly one row per initiative (highest version submitted), including initiatives with zero submitted assessments (via LEFT JOIN, dimension_scores null for those)
    - build_admin_aggregate excludes null-snapshot rows from the org average
    - with 2 initiatives scoring [2,2,2,2,2,2] and [4,4,4,4,4,4], org average per dimension is 3.0
    - with zero submitted assessments org-wide, org_radar_chart_svg is None and org_average_scores is [] (no division by zero, no all-zero hexagon)
    - a draft-only initiative is present in the per-initiative rows with has_data False and contributes nothing to the average
  </behavior>
  <action>
    Create backend/app/services/admin_aggregation.py with a function build_admin_aggregate(session, config) -> dict. Use one raw SQL query adapting admin.py's session.execute(text(...)) + .mappings() idiom, per RESEARCH Pattern 3: LEFT JOIN LATERAL a subquery selecting the highest-version submitted assessment (a.status = 'submitted', ORDER BY a.version DESC, LIMIT 1) per initiative, so every initiative appears exactly once and those with no submitted assessment yield a null latest row (D-08). For each row build a per-initiative entry: initiative id, name, the latest submitted assessment_id (or None), the frozen dimension_scores (or None), overall_average (round(mean(scores), 2) or None), and has_data (bool). Compute the org average ONLY over rows whose dimension_scores is not None (RESEARCH Pitfall 5): filter first, and if the filtered set is empty set org_radar_chart_svg = None and org_average_scores = []; otherwise compute per-dimension mean across included initiatives in config category order (never coerce a missing initiative's scores to zero — no-data is not a score of zero, the plan's prohibition), then org_radar_chart_svg = generate_radar_svg(org_average_scores, config["maturity_bands"]) reusing the same function as individual reports (D-01). Sort per-initiative rows deterministically (e.g. by overall_average then name) so equal averages are stable (ADMN-01 ordering). Return {org_average_scores, org_radar_chart_svg, initiatives: [...]}.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_admin.py -q -k "aggreg or heatmap"</automated>
    <automated>cd backend && grep -n "def build_admin_aggregate" app/services/admin_aggregation.py && grep -c "generate_radar_svg" app/services/admin_aggregation.py</automated>
  </verify>
  <acceptance_criteria>
    - `build_admin_aggregate` returns a dict with keys org_average_scores, org_radar_chart_svg, initiatives
    - the latest-submitted query returns one row per initiative and includes draft-only/no-submission initiatives with a null latest assessment
    - org average is computed only over non-null snapshots; empty included-set yields org_radar_chart_svg None and org_average_scores []
    - `build_admin_aggregate` imports and calls `generate_radar_svg` (no second radar implementation)
  </acceptance_criteria>
  <done>admin_aggregation.py computes latest-submitted-per-initiative rows and a Pitfall-5-safe org average, reusing generate_radar_svg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite /admin/heatmap with real response models + test_admin.py</name>
  <files>backend/app/api/v1/admin.py, backend/tests/api/test_admin.py</files>
  <read_first>
    - backend/app/api/v1/admin.py (AdminHeatmapResponse stub L30-37 to replace; AdminInitiativeRow L54-61 shape to mirror; get_admin_heatmap L324-338 decorator + require_admin to keep; drop the unused type/request params)
    - backend/app/services/admin_aggregation.py (build_admin_aggregate from Task 1)
    - backend/tests/api/test_admin.py (current heatmap tests assert the fixed degraded stub — full rewrite needed)
    - backend/tests/factories.py (fixtures for building initiatives/assessments/answers in tests)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md (admin.py section — AdminAggregateResponse/AdminInitiativeAggregateRow response-model shapes)
  </read_first>
  <behavior>
    - GET /admin/heatmap (admin-authed) returns AdminAggregateResponse with org_average_scores, org_radar_chart_svg, and initiatives rows
    - each populated row has has_data True, dimension_scores, overall_average, and report_assessment_id
    - a draft-only initiative row has has_data False and null scores
    - zero-submission-org-wide returns org_radar_chart_svg null + empty org_average_scores
    - a non-admin caller gets 403 (require_admin unchanged)
  </behavior>
  <action>
    Replace AdminHeatmapResponse with real Pydantic models: AdminInitiativeAggregateRow {id: int, name: str, report_assessment_id: int | None, dimension_scores: list[dict] | None, overall_average: float | None, has_data: bool} and AdminAggregateResponse {org_average_scores: list[dict], org_radar_chart_svg: str | None, initiatives: list[AdminInitiativeAggregateRow]}. Rewrite get_admin_heatmap to keep the @router.get("/heatmap") path and Depends(require_admin), drop the unused type/request params (leftover from the deleted DSI/SP split), depend on get_dssc_questionnaire_config, call build_admin_aggregate(session, config), and return it mapped into AdminAggregateResponse. Remove the now-unused ComplianceReport import from admin.py if it is not referenced elsewhere in the file.

    Rewrite the heatmap tests in test_admin.py: build 2 initiatives with submitted assessments (distinct frozen dimension_scores) plus one draft-only initiative; assert org_average_scores is the per-dimension mean over the 2 submitted initiatives (e.g. all-2 and all-4 -> 3.0), org_radar_chart_svg is a non-empty svg string, the draft-only initiative appears with has_data False, and each populated row carries report_assessment_id. Add an empty-org test: no submitted assessments anywhere -> org_radar_chart_svg None and org_average_scores []. Keep a require_admin 403 test for a non-admin caller.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_admin.py -q</automated>
    <automated>cd backend && test "$(grep -c 'degraded=True' app/api/v1/admin.py)" = "0" && echo "stub removed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "degraded=True" app/api/v1/admin.py` returns 0 (the stub is gone)
    - `/admin/heatmap` returns AdminAggregateResponse; a draft-only initiative row has has_data False and null scores
    - a test asserts the org average equals the per-dimension mean over only submitted initiatives (all-2 + all-4 -> 3.0)
    - an empty-org test asserts org_radar_chart_svg None and org_average_scores []
    - a non-admin caller receives 403; `mypy`/`ruff` clean
  </acceptance_criteria>
  <done>/admin/heatmap returns the real 6-dimension aggregate; tests cover populated, draft-excluded, empty-org, and access-control cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> /admin/heatmap | admin-authed request reads across all initiatives' frozen scores |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Information Disclosure | cross-initiative aggregation exposes all initiatives' scores | low | accept | The endpoint is gated by require_admin (unchanged); org-wide visibility of scores is the intended admin capability (ADMN-01/D-07). |
| T-16-05 | Denial of Service | all-Python averaging over every initiative's JSONB snapshot | low | accept | Fine at expected initiative counts (tens); RESEARCH Assumption A3 documents the large-scale case as out of scope for this phase. |
| T-16-02 | Tampering (XSS) | org_radar_chart_svg | medium | mitigate | Same as the individual chart — only config category names + computed numeric averages flow into the SVG; no end-user free-text. |
| T-16-SC | Tampering | package installs | low | accept | No new package introduced this phase. |
</threat_model>

<verification>
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports`
- `cd backend && uv run pytest tests/api/test_admin.py -q`
- Postgres-backed testcontainer confirms the LATERAL/DISTINCT ON latest-submitted query returns one row per initiative
</verification>

<success_criteria>
- /admin/heatmap returns org radar + per-initiative rows for the 6-category model
- latest-submitted-only aggregation; no-data initiatives excluded from average, listed with has_data False
- zero-org-wide case suppresses the org radar (no degenerate hexagon)
</success_criteria>

## Artifacts this phase produces (this plan)
- module: backend/app/services/admin_aggregation.py (`build_admin_aggregate`)
- models: `AdminAggregateResponse`, `AdminInitiativeAggregateRow` (backend/app/api/v1/admin.py)
- rebuilt endpoint: `GET /admin/heatmap`

<output>
Create `.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-03-SUMMARY.md` when done
</output>
