---
phase: 16-report-data-contract-dual-visualization-admin-aggregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/dssc-questionnaire.json
  - backend/app/services/report_generator.py
  - backend/app/schemas/report.py
  - backend/tests/services/test_report_generator.py
autonomous: true
requirements: [RPRT-01, RPRT-02, RPRT-03]
must_haves:
  truths:
    - "config/dssc-questionnaire.json has a top-level maturity_bands key with exactly 3 bands: red {min 1.0, max 2.0, color #d64545, label Needs attention}, orange {min 2.0, max 3.5, color #e08e2b, label Developing}, green {min 3.5, max 5.0, color #399e5a, label Mature} (RPRT-03/D-05)"
    - "get_maturity_band is the single function mapping a score to a band; both build_priority_list and generate_radar_svg call it — no second inequality chain exists anywhere (RPRT-03)"
    - "A score exactly on a shared boundary belongs to the HIGHER band: 2.0 resolves to orange, 3.5 resolves to green (rule: min <= score < max, top band inclusive of its max) (RPRT-02 boundary/adjacency)"
    - "build_priority_list always returns exactly 6 rows sorted ascending by score, each row carrying category_id, name, score, band_id, band_label, band_color (RPRT-02/D-06)"
    - "When two dimensions have equal scores, priority-list ordering is stable by config category order (RPRT-02 ordering)"
    - "generate_radar_svg returns one <svg viewBox=...> string containing all 6 config category names as axis labels and one data polygon; the same function is reused verbatim for the admin org-average chart (RPRT-01/D-01/D-02)"
    - "Radar axes are placed in config category order, deterministic and stable (RPRT-01 ordering)"
    - "Priority-list and radar scores are rounded to 2 decimal places, reusing compute_dimension_scores' existing round(_, 2) precedent (RPRT-02 precision)"
    - "A submitted assessment always carries all 6 dimension scores (SCOR-04 completeness gate ran at submit time), so the priority list and radar are never empty or partial for a real report (RPRT-01/02 empty)"
    - "build_report_contract assembles a dict with keys assessment_id, version, initiative, dimension_scores, priority_list, radar_chart_svg, maturity_bands from the passed-in frozen dimension_scores (RPRT-04 contract shape)"
  prohibitions:
    - "MUST NOT interpolate end-user free-text (e.g. initiative.name) into the radar_chart_svg string; only server-controlled config category names and computed numeric scores may flow into the SVG that both surfaces later render unescaped. Breadcrumb: general XSS/injection is canon, covered by /gsd-secure-phase (Phase 18); not minted here."
  artifacts:
    - config/dssc-questionnaire.json (maturity_bands top-level key)
    - backend/app/services/report_generator.py (get_maturity_band, build_priority_list, generate_radar_svg, build_report_contract)
    - backend/app/schemas/report.py (MaturityBand, PriorityListItem, ReportContract)
    - backend/tests/services/test_report_generator.py (band/priority/radar unit cases)
  key_links:
    - "get_maturity_band is the sole band-lookup function, imported and called by both build_priority_list and generate_radar_svg (RPRT-03 single source of truth)"
---

<objective>
Build the pure, config-driven report-contract core that every downstream surface (in-app React report, WeasyPrint PDF, admin aggregate) consumes without recomputation: the single color-band lookup, the priority-list builder, the server-side radar-chart SVG generator, and the contract-assembly function — plus their Pydantic schema shapes and unit coverage.

Purpose: RPRT-03's literal deliverable is "color-band thresholds defined once and shared by both the chart and the list" — this plan is where that single source of truth (config maturity_bands + get_maturity_band) is created, so no later plan can duplicate band logic.
Output: config maturity_bands key; four new pure functions in report_generator.py; schemas/report.py models; unit tests for band boundaries, priority ordering, and radar structure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-CONTEXT.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-RESEARCH.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add maturity_bands config key + single get_maturity_band lookup + build_priority_list</name>
  <files>config/dssc-questionnaire.json, backend/app/services/report_generator.py</files>
  <read_first>
    - config/dssc-questionnaire.json (see current top-level keys: version, _schema_note, default_options, categories — add maturity_bands as a new sibling top-level key)
    - backend/app/services/report_generator.py (current file: generate_html_report, generate_report_data; you extend it in place, keeping existing functions until Task 2 of plan 16-02 retargets their callers)
    - backend/app/services/dimension_scoring.py (copy the config-comprehension idiom in _category_question_counts/_category_names and the round(sum/n, 2) precedent in compute_dimension_scores)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-RESEARCH.md (Pattern 1 get_maturity_band; Code Examples build_priority_list and maturity_bands JSON shape — hex values are authoritative)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md (Maturity Color Bands table — the three hex values #d64545/#e08e2b/#399e5a and labels are the approved source of truth, do not re-derive)
  </read_first>
  <behavior>
    - get_maturity_band(1.0) returns the red band; get_maturity_band(1.99) red; get_maturity_band(2.0) returns ORANGE (boundary belongs to higher band); get_maturity_band(3.49) orange; get_maturity_band(3.5) returns GREEN; get_maturity_band(5.0) returns green (top band inclusive of its max)
    - get_maturity_band raises ValueError for a score no band covers (defensive; should not occur for in-range 1.0-5.0 scores)
    - build_priority_list returns exactly 6 rows for a 6-dimension score list, sorted ascending by score; equal scores retain config category order (stable sort)
    - each priority-list row has category_id, name, score, band_id, band_label, band_color, with band fields sourced only from get_maturity_band
  </behavior>
  <action>
    Add a new top-level "maturity_bands" array to config/dssc-questionnaire.json as a sibling of "categories" (D-05, RPRT-03), with three ordered objects each having id, label, min, max, color: red {id red, label Needs attention, min 1.0, max 2.0, color #d64545}, orange {id orange, label Developing, min 2.0, max 3.5, color #e08e2b}, green {id green, label Mature, min 3.5, max 5.0, color #399e5a}. Use the exact hex values from 16-UI-SPEC.md — do not invent new colors. This key is served by the existing get_dssc_questionnaire_config() loader with no new loader (D-05).

    In report_generator.py add get_maturity_band(score: float, bands: list[dict]) -> dict following RESEARCH Pattern 1: iterate bands in order, return the band where min <= score < max, with the top (last) band also matching score == max, so a boundary score belongs to the higher band (2.0 to orange, 3.5 to green); raise ValueError naming the uncovered score otherwise. This is the ONLY band-classification function in the codebase (RPRT-03) — no inequality chain may be duplicated elsewhere, on any surface.

    Add build_priority_list(scores: list[dict], bands: list[dict]) -> list[dict] following RESEARCH Code Examples: return all 6 dimensions (D-06, never filtered to only red/orange), sorted ascending by score via sorted(..., key=lambda s: s["score"]) which is a stable sort so equal scores keep config order; each row carries category_id, name, score (already 2dp from the frozen snapshot), and band_id/band_label/band_color pulled from get_maturity_band(s["score"], bands). Both new functions read structure from config, never hardcode band lists or category counts.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import json,sys; d=json.load(open('../config/dssc-questionnaire.json')); b=d['maturity_bands']; assert [x['id'] for x in b]==['red','orange','green']; assert b[0]['color']=='#d64545' and b[1]['color']=='#e08e2b' and b[2]['color']=='#399e5a'; print('config OK')"</automated>
    <automated>cd backend && uv run python -c "from app.services.report_generator import get_maturity_band as g; import json; b=json.load(open('../config/dssc-questionnaire.json'))['maturity_bands']; assert g(2.0,b)['id']=='orange'; assert g(3.5,b)['id']=='green'; assert g(1.99,b)['id']=='red'; assert g(5.0,b)['id']=='green'; print('band boundaries OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `config/dssc-questionnaire.json` parses as JSON and has a top-level `maturity_bands` list of 3 objects with ids red/orange/green and the exact hex colors #d64545/#e08e2b/#399e5a
    - `get_maturity_band(2.0, bands)` returns the orange band and `get_maturity_band(3.5, bands)` returns the green band (boundary-to-higher-band rule)
    - `build_priority_list` on a 6-element score list returns 6 rows sorted ascending by score, each with band_id/band_label/band_color populated from get_maturity_band
    - grep confirms no numeric band threshold literal (2.0 / 3.5 as a color decision) appears in any file other than report_generator.py and the config: `grep -rn "3.5" backend/app/api backend/app/templates frontend/src | grep -iv test` returns no band-classification logic
  </acceptance_criteria>
  <done>maturity_bands config key added; get_maturity_band and build_priority_list implemented in report_generator.py; boundary and ordering behavior proven by the inline checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate_radar_svg + build_report_contract + schemas/report.py</name>
  <files>backend/app/services/report_generator.py, backend/app/schemas/report.py</files>
  <read_first>
    - backend/app/services/report_generator.py (after Task 1 — add generate_radar_svg, build_report_contract, and `import math`)
    - backend/app/schemas/assessment.py (the plain-BaseModel, hand-assembled convention to mirror exactly — no from_attributes/ORM config)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-RESEARCH.md (Pattern 2 generate_radar_svg with viewBox trigonometry; Pitfall 3 — SVG text needs explicit presentation attributes font-size/font-family/fill, not CSS shorthand, for WeasyPrint parity)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md (schemas/report.py section — exact field lists for MaturityBand, PriorityListItem, ReportContract)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md (typography/color tokens: navy #06004f label text, Rubik font — set these as explicit SVG presentation attributes per Pitfall 3)
  </read_first>
  <behavior>
    - generate_radar_svg(scores, bands) returns a string beginning with `<svg` and containing a `viewBox` attribute (not fixed pixel width/height), all 6 category names as <text> content, and one <polygon> element
    - each <text> element carries explicit font-size, font-family, and fill presentation attributes (WeasyPrint parity, Pitfall 3) — never relies on external CSS
    - generate_radar_svg with 6 equal scores produces a regular hexagon polygon (6 distinct points)
    - build_report_contract(dimension_scores, initiative, assessment, config) returns a dict whose keys are exactly assessment_id, version, initiative, dimension_scores, priority_list, radar_chart_svg, maturity_bands
    - the priority_list inside the contract equals build_priority_list(dimension_scores, config["maturity_bands"]) and radar_chart_svg equals generate_radar_svg(dimension_scores, config["maturity_bands"]) — one computation, reused
  </behavior>
  <action>
    Add `import math` and generate_radar_svg(scores: list[dict], bands: list[dict], *, size: int = 320, max_score: float = 5.0) -> str to report_generator.py per RESEARCH Pattern 2: place n=len(scores) axis points around a circle with angle = 2*pi*i/n - pi/2 so axis 0 points up; scale each score into a radius fraction min(score, max_score)/max_score; emit axis spoke <line> elements, a data <polygon> whose fill/stroke color comes from get_maturity_band(overall_average, bands), and one <text> label per axis using the config category name. Per Pitfall 3, set font on each <text> as explicit presentation attributes font-size="11" font-family="Rubik, sans-serif" fill="#06004f" — NOT a CSS class or shorthand — so the browser and WeasyPrint render identically. Root element must use viewBox (not fixed width/height px) so it scales in both the responsive card and the fixed-width PDF page (D-02 overflow). Only config category names and numeric scores flow into the SVG string — never initiative name or any end-user free-text (prohibition).

    Add build_report_contract(dimension_scores: list[dict], initiative, assessment, config: dict) -> dict that returns a single dict with keys assessment_id (assessment.id), version (assessment.version), initiative (a small dict of id + name via the existing ORM-or-dict idiom in generate_report_data), dimension_scores (the passed-in frozen list), priority_list (build_priority_list(dimension_scores, config["maturity_bands"])), radar_chart_svg (generate_radar_svg(dimension_scores, config["maturity_bands"])), and maturity_bands (config["maturity_bands"]). This one dict is what plan 16-02 returns as JSON AND passes into the Jinja2 template context — one payload, two renderings (RPRT-04).

    Create backend/app/schemas/report.py mirroring schemas/assessment.py's plain-BaseModel convention: MaturityBand {id: str, label: str, min: float, max: float, color: str}; PriorityListItem {category_id: str, name: str, score: float, band_id: str, band_label: str, band_color: str}; ReportContract {assessment_id: int, version: int, initiative: dict, dimension_scores: list[dict], priority_list: list[PriorityListItem], radar_chart_svg: str, maturity_bands: list[MaturityBand]}. These types document the contract for openapi and for the frontend fetch types in plan 16-04.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.report_generator import generate_radar_svg; import json; c=json.load(open('../config/dssc-questionnaire.json')); b=c['maturity_bands']; sc=[{'category_id':x['id'],'name':x['name'],'score':3.0} for x in c['categories']]; svg=generate_radar_svg(sc,b); assert svg.startswith('<svg') and 'viewBox' in svg and 'polygon' in svg; assert all(cat['name'] in svg for cat in c['categories']); assert 'font-family' in svg; print('radar OK')"</automated>
    <automated>cd backend && uv run python -c "import app.schemas.report as r; assert hasattr(r,'ReportContract') and hasattr(r,'PriorityListItem') and hasattr(r,'MaturityBand'); print('schemas OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `generate_radar_svg` output starts with `<svg`, contains `viewBox`, contains a `polygon`, and includes all 6 config category names as text
    - every `<text>` in the SVG carries explicit `font-family` and `font-size` presentation attributes (not a CSS class)
    - `build_report_contract` returns a dict with exactly the 7 keys assessment_id, version, initiative, dimension_scores, priority_list, radar_chart_svg, maturity_bands
    - `backend/app/schemas/report.py` imports cleanly and exposes MaturityBand, PriorityListItem, ReportContract
  </acceptance_criteria>
  <done>generate_radar_svg and build_report_contract implemented; schemas/report.py created; contract shape and SVG structure proven by inline checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for band boundaries, priority ordering, radar structure</name>
  <files>backend/tests/services/test_report_generator.py</files>
  <read_first>
    - backend/tests/services/test_report_generator.py (current tests — extend, do not delete existing passing cases; if the file only covers the old stub, replace those stub assertions)
    - backend/tests/services/test_dimension_scoring.py (the config-driven, no-hardcoded-question-id test idiom against config/dssc-questionnaire.json to mirror)
    - backend/app/services/report_generator.py (functions under test, after Tasks 1-2)
  </read_first>
  <behavior>
    - test_maturity_band: 1.0 and 1.99 -> red; 2.0 and 3.49 -> orange; 3.5 and 5.0 -> green (boundary-to-higher-band)
    - test_maturity_band_same_for_both_callers: the band returned for a given score is identical whether obtained directly or via a build_priority_list row (proves single source of truth, RPRT-03)
    - test_priority_list_six_rows_sorted: a 6-dimension input yields exactly 6 rows sorted ascending by score
    - test_priority_list_tie_stable: two dimensions with equal score retain config category order
    - test_radar_svg_structure: output has viewBox, one polygon, and all 6 category names
  </behavior>
  <action>
    Add pytest cases to test_report_generator.py driven from the real config/dssc-questionnaire.json (load it, derive categories and maturity_bands — never hardcode question ids or band numbers), mirroring test_dimension_scoring.py's config-comprehension style. Cover the boundary table (2.0 -> orange, 3.5 -> green), the RPRT-03 single-source assertion (band from get_maturity_band matches the band embedded in the corresponding build_priority_list row for the same score), the 6-row ascending sort with a stable tie-break, and the radar SVG structural assertions (viewBox present, polygon present, all 6 names present). These are the phase's Wave-0 smoke net for the pure functions; broader coverage is Phase 17 (TEST-01/02).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/services/test_report_generator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_report_generator.py` passes with cases named for maturity_band boundaries, priority_list ordering/tie, and radar structure
    - the maturity_band test asserts 2.0 -> orange and 3.5 -> green explicitly
    - tests derive categories/bands from config, containing no hardcoded question id or band threshold literal in assertions beyond the boundary values under test
  </acceptance_criteria>
  <done>test_report_generator.py covers band boundaries, priority ordering, single-source band equality, and radar structure; all green locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> SVG string | category names + scores are interpolated into a raw SVG string that downstream surfaces render unescaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Tampering/Elevation (XSS) | generate_radar_svg string | medium | mitigate | Only server-controlled config category names and computed numeric scores flow into the SVG string; no end-user free-text (initiative name etc.) is ever interpolated — enforced by the prohibition and by build_report_contract passing only config-derived names. General XSS/injection is canon, owned by /gsd-secure-phase (Phase 18). |
| T-16-SC | Tampering | package installs | low | accept | No new npm/pip/cargo package is introduced this phase (RESEARCH Package Legitimacy Audit: none) — nothing to verify. |
</threat_model>

<verification>
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports`
- `cd backend && uv run pytest tests/services/test_report_generator.py -q`
- config JSON parses and maturity_bands has the 3 approved bands with exact hex values
</verification>

<success_criteria>
- maturity_bands defined exactly once (config) and read via the existing loader
- get_maturity_band is the sole band-classification function, called by both build_priority_list and generate_radar_svg
- build_report_contract produces the single shared contract dict shape all surfaces will consume
- unit smoke net green
</success_criteria>

## Artifacts this phase produces (this plan)
- config key: `maturity_bands`
- functions: `get_maturity_band`, `build_priority_list`, `generate_radar_svg`, `build_report_contract` (backend/app/services/report_generator.py)
- Pydantic models: `MaturityBand`, `PriorityListItem`, `ReportContract` (backend/app/schemas/report.py)

<output>
Create `.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-01-SUMMARY.md` when done
</output>
