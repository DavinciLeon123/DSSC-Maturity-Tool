---
phase: 16-report-data-contract-dual-visualization-admin-aggregation
plan: 04
type: execute
wave: 3
depends_on: ["16-02", "16-03"]
files_modified:
  - frontend/src/lib/reports.ts
  - frontend/src/routes/_app/report.tsx
  - frontend/src/routes/_app/admin.heatmap.tsx
  - docs/api/openapi.json
autonomous: true
requirements: [RPRT-01, RPRT-02, RPRT-04, ADMN-01]
must_haves:
  truths:
    - "The report page renders the server-provided radar_chart_svg string verbatim (via dangerouslySetInnerHTML) — no client-side polygon math and no client-side band re-derivation (RPRT-01/D-02)"
    - "The priority list renders exactly 6 rows, each pairing the colored band dot with the band_label text and the score to 2 decimals — color is never the sole signal (RPRT-02, accessibility)"
    - "The report page reads optional initiative_id and assessment_id from search params to view a specific submitted version; absent initiative_id falls back to the current user's own initiative via /initiatives/me (D-04)"
    - "Loading uses a centered antd Spin; a fetch error shows an antd Alert plus a Retry button with the UI-SPEC copy (report load-failure and admin-load-failure states)"
    - "The admin page renders the org radar (org_radar_chart_svg) plus an antd Table of per-initiative rows with pagination pageSize 10 (ADMN-01/D-07)"
    - "The admin empty state 'No submitted assessments yet' shows when there is no submitted data org-wide (UI empty state)"
    - "Per-initiative rows show a 'No data yet' badge inline for initiatives with no submitted assessment (D-08)"
    - "The initiative/org name column uses antd ellipsis plus a native title tooltip so long names truncate with a tooltip rather than breaking the grid (UI long-text)"
    - "The admin summary caption pluralizes correctly: '1 submitted initiative' vs 'N submitted initiatives' (UI zero-one-many)"
    - "docs/api/openapi.json is regenerated so the docs-freshness CI gate (git diff --exit-code) passes against the new /report/data and /admin/heatmap response models"
    - statement: "priority-list dimension names wrap onto a second line at the Body/Label type sizes rather than truncating (longest current config name 'Control over Data & Trust'; config is user-editable per QSTN-03), enforced via CSS white-space normal, held-out visual check"
      verification: backstop
  prohibitions:
    - "MUST NOT re-derive maturity band color or label from a raw score anywhere in the frontend; the frontend only displays the band_color/band_label the backend attached to each priority_list row and to the SVG (RESEARCH anti-pattern, RPRT-03)."
  artifacts:
    - frontend/src/lib/reports.ts (fetchReportData, ReportContract interface)
    - frontend/src/routes/_app/report.tsx (radar SVG render + priority list, assessment_id search param)
    - frontend/src/routes/_app/admin.heatmap.tsx (org radar + per-initiative antd Table)
    - docs/api/openapi.json (regenerated)
  key_links:
    - "frontend fetchReportData consumes the backend ReportContract shape verbatim (RPRT-04)"
    - "admin View report links target /report?initiative_id=&assessment_id= resolving the per-initiative latest submitted report (D-07)"
---

<objective>
Build the two consuming surfaces of the report contract: the in-app report page (radar chart rendered from the server SVG string plus the 6-row priority list) and the admin aggregation page (org radar plus per-initiative table), extend the API client for the new JSON contract, and regenerate the OpenAPI schema for the docs-freshness gate.

Purpose: RPRT-01/02 are only observable once the frontend renders the radar + priority list; RPRT-04's "same data both surfaces" is completed when the React page renders the identical server SVG; ADMN-01's admin view is completed here.
Output: fetchReportData + ReportContract types; rebuilt report.tsx; rebuilt admin.heatmap.tsx; regenerated openapi.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-CONTEXT.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-PATTERNS.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-02-SUMMARY.md
@.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend reports.ts + rebuild report.tsx (radar SVG render + priority list + assessment_id)</name>
  <files>frontend/src/lib/reports.ts, frontend/src/routes/_app/report.tsx</files>
  <read_first>
    - frontend/src/lib/reports.ts (keep generateReport/getReportUrl untouched — still used by the blob-URL PDF flow; add the new typed fetch alongside)
    - frontend/src/lib/assessments.ts (fetchAssessmentHistory typed-fetch shape to mirror for fetchReportData)
    - frontend/src/routes/_app/report.tsx (current ReportPage fetch-on-mount via /initiatives/me then POST report/data; the loading Spin, error Alert, and card chrome L521-534 to keep; delete the HeatmapMatrix/StatusChip/MAMI_CODE_TO_REC_ID/RECOMMENDATIONS dead code)
    - frontend/src/routes/_app/assessments.tsx (search-param-free precedent; TanStack Route.useSearch is the mechanism for reading assessment_id/initiative_id)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md (Color/Typography/Copywriting: navy #06004f, Rubik, 16px card radius; Primary CTA "Download PDF Report"; report error copy; band dot + label paired never color-only)
    - backend/app/schemas/report.py (ReportContract field names to type exactly)
  </read_first>
  <action>
    In reports.ts add a ReportContract TypeScript interface matching the backend schema (dimension_scores: {category_id, name, score}[]; priority_list: {category_id, name, score, band_id, band_label, band_color}[]; radar_chart_svg: string; maturity_bands: {id, label, min, max, color}[]) and a fetchReportData(initiativeId, assessmentId?) that GETs /initiatives/{id}/report/data with an optional ?assessment_id= query, typed to ReportContract, mirroring fetchAssessmentHistory. Keep generateReport/getReportUrl.

    Rewrite report.tsx to fetch via fetchReportData. Read optional initiative_id and assessment_id from the route search params (TanStack validateSearch/useSearch); when initiative_id is absent, resolve the current user's own initiative via /initiatives/me (existing pattern) — this supports both the owner's own report and admin/history deep-links (D-04). Render the radar chart by injecting data.radar_chart_svg verbatim into a div via dangerouslySetInnerHTML (D-02 — server-controlled markup, safe by construction per RESEARCH; declare the radar as the primary visual anchor per the UI-checker note). Render the priority list as exactly 6 rows (data.priority_list is already sorted ascending and always 6 per D-06), each row showing the dimension name, score.toFixed(2), and a colored dot using band_color paired with band_label text — never re-derive color from the score (prohibition); ensure long names wrap (CSS white-space normal, backstop). Keep the centered Spin loading state and the Alert-plus-Retry error state using the UI-SPEC report error copy. Update the primary CTA button label to "Download PDF Report". Delete the retired HeatmapMatrix / MAMI recommendation code paths.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
    <automated>cd frontend && npx eslint src/routes/_app/report.tsx src/lib/reports.ts</automated>
    <automated>cd frontend && grep -q "dangerouslySetInnerHTML" src/routes/_app/report.tsx && test "$(grep -c 'HeatmapMatrix' src/routes/_app/report.tsx)" = "0" && echo "render OK / matrix removed"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -b --noEmit` and `eslint` pass for report.tsx and reports.ts
    - report.tsx renders `data.radar_chart_svg` via `dangerouslySetInnerHTML` and renders 6 priority rows with band_color dot + band_label text
    - report.tsx reads assessment_id (and optional initiative_id) from search params and falls back to /initiatives/me when initiative_id is absent
    - no maturity-band threshold/color is computed from a raw score in the frontend (grep finds no client-side 2.0/3.5 band logic)
    - retired HeatmapMatrix/MAMI recommendation code removed; CTA reads "Download PDF Report"
  </acceptance_criteria>
  <done>reports.ts exposes fetchReportData + ReportContract; report.tsx renders the server radar SVG + 6-row priority list with per-version support.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild admin.heatmap.tsx (org radar + per-initiative table)</name>
  <files>frontend/src/routes/_app/admin.heatmap.tsx</files>
  <read_first>
    - frontend/src/routes/_app/admin.heatmap.tsx (keep the beforeLoad ADMIN guard L10-23 verbatim and the page chrome L297-322; delete the DSI/SP dual-tab split and HeatmapGrid/CountPill)
    - frontend/src/routes/_app/assessments.tsx (antd Table + ColumnsType + pagination pattern L61-89, 213 to copy for the per-initiative table)
    - backend/app/api/v1/admin.py (AdminAggregateResponse / AdminInitiativeAggregateRow field names to type the fetch)
    - .planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-UI-SPEC.md (admin empty-state copy "No submitted assessments yet" / body; per-row "No data yet" badge; error copy "Couldn't load the aggregated maturity data."; long-name ellipsis + title tooltip; summary caption pluralization; Display/Heading type tokens)
  </read_first>
  <action>
    Rewrite admin.heatmap.tsx to fetch GET /admin/heatmap (single useEffect + api.get, drop the DSI/SP tab split — this aggregation is not type-scoped) typed to the AdminAggregateResponse shape. Render, in order: a page title "Aggregated Maturity Overview" (Display token); when org_radar_chart_svg is present, the org radar via dangerouslySetInnerHTML (declare it the primary visual anchor); a summary caption pluralized correctly ("1 submitted initiative" vs "N submitted initiatives"); and an antd Table of per-initiative rows (copy the assessments.tsx Table/pagination pattern, pageSize 10, showSizeChanger false). Columns: initiative name (ellipsis: true + title tooltip for long names), per-dimension scores / overall average, and a "View report" link to /report?initiative_id={id}&assessment_id={report_assessment_id} for rows with has_data; rows with has_data false show the "No data yet" badge inline and no report link (D-08). When the org has zero submitted assessments (org_radar_chart_svg null and no populated rows), render the "No submitted assessments yet" empty state (Copywriting Contract) instead of a degenerate chart. Keep the beforeLoad ADMIN guard verbatim; use the centered Spin loading state and the Alert-plus-Retry error state with the admin error copy. Never re-derive band color from a score client-side (prohibition) — use band fields the backend provides.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
    <automated>cd frontend && npx eslint src/routes/_app/admin.heatmap.tsx</automated>
    <automated>cd frontend && test "$(grep -c 'HeatmapGrid' src/routes/_app/admin.heatmap.tsx)" = "0" && echo "grid removed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -b --noEmit` and `eslint` pass for admin.heatmap.tsx
    - the page renders org_radar_chart_svg (when present) and an antd Table with pageSize 10 pagination
    - rows with has_data false show a "No data yet" badge and no report link; populated rows link to /report with initiative_id + assessment_id
    - the zero-submission empty state shows "No submitted assessments yet"; the name column uses ellipsis + title tooltip
    - the retired DSI/SP tab split and HeatmapGrid/CountPill code are removed
  </acceptance_criteria>
  <done>admin.heatmap.tsx renders the org radar + paginated per-initiative table with no-data badges, empty state, and per-version report links.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate docs/api/openapi.json for the docs-freshness gate</name>
  <files>docs/api/openapi.json</files>
  <read_first>
    - backend/scripts/export_openapi.py (the exact regeneration entrypoint the CI docs-freshness job runs: `uv run python scripts/export_openapi.py`)
    - .github/workflows/pr.yml (docs-freshness job L178-181: regenerate then git diff --exit-code -- docs/api/openapi.json)
    - CLAUDE.md (docs-freshness is a hard CI gate — regenerate after all response-model changes, once)
  </read_first>
  <action>
    After plans 16-02 and 16-03 have landed all backend response-model changes (the new /report/data ReportContract-shaped response and the /admin/heatmap AdminAggregateResponse), run the OpenAPI export exactly as CI does — `cd backend && uv run python scripts/export_openapi.py` — to regenerate docs/api/openapi.json, then confirm a second run produces no further diff. This is deliberately the phase's single regeneration point (not repeated per backend plan) to avoid conflicting writes to the same file across parallel Wave-2 plans.
  </action>
  <verify>
    <automated>cd backend && uv run python scripts/export_openapi.py && git diff --exit-code -- docs/api/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/export_openapi.py` followed by `git diff --exit-code -- docs/api/openapi.json` exits 0 (regeneration is stable / committed)
    - the regenerated schema reflects the new /report/data and /admin/heatmap response models
  </acceptance_criteria>
  <done>docs/api/openapi.json regenerated and diff-clean on a second run — docs-freshness gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server SVG string -> React DOM | radar_chart_svg injected via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02 | Tampering (XSS) | dangerouslySetInnerHTML for radar_chart_svg | medium | mitigate | The SVG is server-generated and carries only config category names + numeric scores (no end-user free-text), so verbatim injection is safe by construction (RESEARCH Security Domain). The frontend must never build the SVG from user input. |
| T-16-06 | Information Disclosure | frontend fetch of another user's report via crafted assessment_id | high | mitigate | Authorization is enforced server-side (plan 16-02 T-16-01, 404-on-mismatch); the frontend link shape does not itself grant access — the backend re-checks ownership/admin on every request. |
| T-16-SC | Tampering | package installs | low | accept | No new npm/pip package introduced this phase (existing antd only). |
</threat_model>

<verification>
- `cd frontend && npx tsc -b --noEmit && npx eslint src/routes/_app/report.tsx src/routes/_app/admin.heatmap.tsx src/lib/reports.ts`
- `cd backend && uv run python scripts/export_openapi.py && git diff --exit-code -- docs/api/openapi.json`
- Manual/CI: confirm the in-app radar chart and the mailed PDF chart render identically (Pitfall 3/4 backstop — via CI/Docker, not local)
</verification>

<success_criteria>
- In-app report shows the server-rendered radar + 6-row priority list; per-version viewing works
- Admin page shows the org radar + paginated per-initiative table with no-data handling
- OpenAPI schema regenerated; docs-freshness gate green
</success_criteria>

## Artifacts this phase produces (this plan)
- frontend function/type: `fetchReportData`, `ReportContract` (frontend/src/lib/reports.ts)
- rebuilt routes: `/report` (report.tsx), `/admin/heatmap` page (admin.heatmap.tsx)
- regenerated docs/api/openapi.json

<output>
Create `.planning/phases/16-report-data-contract-dual-visualization-admin-aggregation/16-04-SUMMARY.md` when done
</output>
