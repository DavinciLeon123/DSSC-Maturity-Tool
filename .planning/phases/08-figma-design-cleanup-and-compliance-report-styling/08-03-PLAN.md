---
phase: 08-figma-design-cleanup-and-compliance-report-styling
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_auth/login.tsx
  - frontend/src/routes/_auth/register.tsx
  - frontend/src/routes/_auth/forgot-password.tsx
  - frontend/src/routes/_auth/reset-password.tsx
autonomous: true
requirements:
  - UI-FIX-04
  - UI-FIX-05
  - UI-FIX-06
must_haves:
  truths:
    - "CoE DSC logo image appears above the brand label on the login page"
    - "CoE DSC logo image appears above the brand label on the register page"
    - "CoE DSC logo image appears above the brand label on the forgot-password page"
    - "CoE DSC logo image appears above the brand label on the reset-password page"
  artifacts:
    - path: "frontend/src/routes/_auth/login.tsx"
      provides: "Logo added above CoE-DSC / TNO text"
    - path: "frontend/src/routes/_auth/register.tsx"
      provides: "Logo added above brand label"
    - path: "frontend/src/routes/_auth/forgot-password.tsx"
      provides: "Logo added above brand label"
    - path: "frontend/src/routes/_auth/reset-password.tsx"
      provides: "Logo added above brand label"
  key_links:
    - from: "frontend/src/assets/logo-coe-dsc.svg"
      to: "all 4 auth screen header sections"
      via: "import logoSrc from '../../assets/logo-coe-dsc.svg' + <img src={logoSrc}>"
      pattern: "logo-coe-dsc"
---

<objective>
Add the CoE DSC SVG logo above the brand label on all 4 authentication screens (login, register, forgot-password, reset-password). The SVG file already exists at `frontend/src/assets/logo-coe-dsc.svg`.

Purpose: Match Figma design that shows the logo on auth screens (items 4–6 from phase scope).
Output: 4 updated auth route files with logo image rendered above the "CoE-DSC / TNO" label.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-CONTEXT.md
</context>

<interfaces>
<!-- Current brand header section in login.tsx (same pattern in all 4 files) -->
```tsx
<div style={{ marginBottom: "2rem", textAlign: "center" }}>
  <div style={{ fontSize: "0.75rem", color: "#399e5a", fontWeight: 600, letterSpacing: "0.1em", marginBottom: "0.5rem", fontFamily: "'Rubik', sans-serif" }}>CoE-DSC / TNO</div>
  <h1 style={{ fontSize: "1.5rem", fontWeight: 700, color: "#06004f", fontFamily: "'Rubik', sans-serif", margin: 0 }}>Sign In</h1>
</div>
```

<!-- Asset path -->
SVG logo: frontend/src/assets/logo-coe-dsc.svg (already on disk)
Import: `import logoSrc from "../../assets/logo-coe-dsc.svg";`
(adjust relative path per file location — all 4 files are in frontend/src/routes/_auth/ so path is correct)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add logo to all 4 auth screens</name>
  <files>
    frontend/src/routes/_auth/login.tsx,
    frontend/src/routes/_auth/register.tsx,
    frontend/src/routes/_auth/forgot-password.tsx,
    frontend/src/routes/_auth/reset-password.tsx
  </files>
  <action>
    For each of the 4 auth files:

    1. Add import at top of file: `import logoSrc from "../../assets/logo-coe-dsc.svg";`

    2. In the brand header `<div>` (the centered block with the "CoE-DSC / TNO" text), insert the logo image ABOVE the existing "CoE-DSC / TNO" green label:
    ```tsx
    <img
      src={logoSrc}
      alt="CoE DSC logo"
      style={{ width: "76px", height: "auto", marginBottom: "0.75rem", display: "block", margin: "0 auto 0.75rem" }}
    />
    ```

    The header section should end up:
    ```tsx
    <div style={{ marginBottom: "2rem", textAlign: "center" }}>
      <img src={logoSrc} alt="CoE DSC logo" style={{ width: "76px", height: "auto", display: "block", margin: "0 auto 0.75rem" }} />
      <div style={{ /* green label */ }}>CoE-DSC / TNO</div>
      <h1 ...>Sign In</h1>  {/* or Register / Reset password etc */}
    </div>
    ```

    Apply the same pattern to all 4 files. Do not change any other styles, logic, or form elements.

    Per user decision (Claude's discretion): size ~76×32px matching Figma header logo size. Use `width: "76px", height: "auto"` to preserve aspect ratio.
  </action>
  <verify>
    <automated>cd "C:/Users/djlia/Desktop/MaMi Checker/frontend" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compiles clean. `logo-coe-dsc.svg` is imported in all 4 auth files. `<img>` tag with logoSrc appears in each file's brand header section.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
grep: `grep -l "logo-coe-dsc" frontend/src/routes/_auth/*.tsx` returns all 4 files.
</verification>

<success_criteria>
- All 4 auth screens import and render the CoE DSC logo SVG
- Logo appears above the "CoE-DSC / TNO" green label, centered
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-03-SUMMARY.md`
</output>
