---
phase: 08-figma-design-cleanup-and-compliance-report-styling
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/questionnaire/StepPills.tsx
  - frontend/src/components/questionnaire/WizardPage.tsx
autonomous: true
requirements:
  - UI-FIX-02
must_haves:
  truths:
    - "Progress sidebar shows topic (sub-category) names under the active category, accordion style"
    - "Topics under non-active categories are hidden (collapsed)"
    - "Active topic is visually distinguished (bold + navy accent dot or left-border)"
    - "Topics are informational only — clicking a topic name does not navigate"
  artifacts:
    - path: "frontend/src/components/questionnaire/StepPills.tsx"
      provides: "Accordion-style progress sidebar with topic expansion"
    - path: "frontend/src/components/questionnaire/WizardPage.tsx"
      provides: "currentTopicIndex prop passed to StepPills"
  key_links:
    - from: "WizardPage.tsx"
      to: "StepPills"
      via: "currentTopicIndex prop added to StepPills call"
      pattern: "currentTopicIndex=\\{topicIndex\\}"
---

<objective>
Update the questionnaire progress sidebar (StepPills) to show topic (sub-category) names under the currently active category in accordion style. Non-active categories remain collapsed showing only the category name. The active topic is visually highlighted. Topics are read-only — no navigation on click.

Purpose: Align progress sidebar with Figma design showing topic-level granularity under active category.
Output: Updated StepPills.tsx (accordion expansion) and WizardPage.tsx (passes currentTopicIndex).
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-CONTEXT.md
</context>

<interfaces>
<!-- Current StepPills.tsx interface -->
```tsx
interface Props {
  categories: Category[];
  currentCategoryIndex: number;
  completedCategoryIds: Set<string>;
}
export function StepPills({ categories, currentCategoryIndex, completedCategoryIds }: Props)
```

<!-- Category type from lib/questionnaire.ts (typical shape) -->
```ts
interface Topic { id: string; label: string; questions: Question[] }
interface Category { id: string; label: string; topics: Topic[] }
```

<!-- WizardPage.tsx call site (line ~384) -->
```tsx
<StepPills
  categories={config.categories}
  currentCategoryIndex={categoryIndex}
  completedCategoryIds={completedCategoryIds}
/>
```

<!-- WizardPage.tsx topicIndex state (line ~90) -->
```tsx
const [topicIndex, setTopicIndex] = useState(0);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add currentTopicIndex prop to StepPills and update WizardPage call site</name>
  <files>frontend/src/components/questionnaire/StepPills.tsx, frontend/src/components/questionnaire/WizardPage.tsx</files>
  <action>
    **In StepPills.tsx:**

    1. Add `currentTopicIndex: number` to the Props interface.
    2. In the category map, when `isActive` is true, render an expanded topic list below the category row.
    3. Topic list rendering (for the active category only):
       - Wrap topics in a `<div>` with `marginLeft: "36px"` (aligns under the label, past the circle).
       - For each topic, render a `<div>` with:
         - `display: "flex"`, `alignItems: "center"`, `gap: "6px"`, `padding: "4px 0"`.
         - **Active topic** (index === currentTopicIndex): small navy dot (`width: 6, height: 6, borderRadius: "50%", background: "#06004f"`) + `<span>` with `fontWeight: 600`, `color: "#06004f"`, `fontSize: "0.8rem"`, Rubik font.
         - **Completed topic** (index < currentTopicIndex): small green dot (`background: "#399e5a"`) + `<span>` with `color: "#399e5a"`, `fontSize: "0.8rem"`.
         - **Future topic** (index > currentTopicIndex): no dot or transparent dot + `<span>` with `color: "rgba(6,0,79,0.45)"`, `fontSize: "0.8rem"`.
       - Topics are `<div>` elements (NOT `<button>` or `<a>`). No onClick. cursor: default.
    4. The connector line between category items should still render between categories (not between topics).

    **In WizardPage.tsx:**
    - Update the `<StepPills>` call to add `currentTopicIndex={topicIndex}`.
  </action>
  <verify>
    <automated>cd "C:/Users/djlia/Desktop/MaMi Checker/frontend" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compiles clean. StepPills accepts currentTopicIndex. Active category renders its topics as a sub-list. Non-active categories show only category name.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` in frontend directory — zero errors.
Visual check: open questionnaire, progress sidebar shows topics under active category only. Navigating to next topic updates the active topic highlight without navigating to a different page.
</verification>

<success_criteria>
- StepPills.tsx renders topic names under active category (accordion)
- Active topic is bold navy; completed topics are green; future topics are muted
- Topics are not clickable for navigation
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-02-SUMMARY.md`
</output>
