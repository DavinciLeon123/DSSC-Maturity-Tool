---
phase: 08-figma-design-cleanup-and-compliance-report-styling
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/index.tsx
autonomous: true
requirements:
  - UI-FIX-07
  - UI-FIX-08
must_haves:
  truths:
    - "Homepage hero headline reads 'MAMI - Minimal Agreements for Maximum Interoperability'"
    - "Homepage nav shows CoE DSC logo image (not text) and Log In / Register buttons"
    - 'Homepage "How it works" section shows 3 English step cards matching Figma spec'
    - "Homepage MAMI section has English body text about four domains"
    - "All Dutch text replaced with English equivalents"
  artifacts:
    - path: "frontend/src/routes/index.tsx"
      provides: "Fully English homepage matching Figma node 154-3458"
  key_links:
    - from: "Nav 'Log In' button"
      to: "/login route"
      via: "Link to='/login'"
      pattern: "Link.*login"
    - from: "Hero CTA 'Start the check'"
      to: "/login route"
      via: "Link to='/login'"
      pattern: "Start the check"
---

<objective>
Replace all Dutch homepage content with Figma-spec English text and add the CoE DSC logo to the nav. Apply Figma layout alignment per node 154-3458. The executor should use the Figma MCP tool (`get_design_context` + `get_screenshot`) to pull exact specs from the Figma file before implementing.

Purpose: Items 7+8 from phase scope — logo on homepage nav + full English restyle.
Output: Updated index.tsx with English content, CoE DSC logo in nav, and Figma-aligned layout.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-CONTEXT.md
</context>

<interfaces>
<!-- Figma reference -->
Figma URL: https://www.figma.com/design/V3v7Oq6DfXMpQ86llyVCQ0/MAMI-tool?node-id=154-3458
File key: V3v7Oq6DfXMpQ86llyVCQ0
Node ID: 154-3458

Use implement-design skill workflow:
1. get_design_context(fileKey="V3v7Oq6DfXMpQ86llyVCQ0", nodeId="154-3458")
2. get_screenshot(fileKey="V3v7Oq6DfXMpQ86llyVCQ0", nodeId="154-3458")
3. Translate to project conventions (antd + inline styles, Rubik font, DSC tokens)

<!-- Logo asset -->
import logoSrc from "../assets/logo-coe-dsc.svg";
(index.tsx is at frontend/src/routes/index.tsx — one level up from _auth)

<!-- Locked content decisions from CONTEXT.md -->
Nav: logo <img> (76px wide) + "Log In" (Link to /login) + "Register" (green Button, Link to /register)
Hero headline: "MAMI - Minimal Agreements for Maximum Interoperability"
Hero subtitle: "A practical self-assessment tool that helps you understand and improve the interoperability of your initiative"
Hero CTAs: "Start the check" (green, /login) + "Create an account" (outline, /register)
"How it works" heading: "How does it work?"
Step 01: "Register your initiative" — "Create an account and register your Data Sharing Initiative (DSI) or Service Provider (SP) initiative."
Step 02: "Complete the questionnaire" — "Work through the structured MAMI questionnaire with Yes / Not yet / Not applicable answers per topic."
Step 03: "Receive your report" — "Generate an instant interoperability heatmap with your current compliance level."
MAMI section heading: "The MAMI questionnaire gives you an immediate overview of your current level of interoperability."
MAMI body: "Across four key domains (Scheme, Participants, Data and Services), you will assess whether you already comply, plan to comply or it's not applicable. Your answers are visualised in a clear interoperability heatmap."
MAMI CTA: "Get started" (links to /login or /register)
Footer: already implemented in Phase 07-03 — preserve existing Footer component

<!-- Established patterns -->
- antd Button, Link components
- TanStack Router Link: import { Link } from "@tanstack/react-router"
- Design tokens: #06004f (navy), #399e5a (green), Rubik font, borderRadius 16px
- index.tsx is 313 lines — rewrite the full file keeping the same Route export structure
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fetch Figma specs and rewrite homepage with English content + logo</name>
  <files>frontend/src/routes/index.tsx</files>
  <action>
    **Step 1 — Fetch Figma design context:**
    Use the implement-design skill workflow (Figma MCP tools available):
    - `get_design_context(fileKey="V3v7Oq6DfXMpQ86llyVCQ0", nodeId="154-3458")` for layout/spacing/color specs
    - `get_screenshot(fileKey="V3v7Oq6DfXMpQ86llyVCQ0", nodeId="154-3458")` for visual reference
    If the response is too large, use `get_metadata` first to identify child section node IDs, then fetch each section individually.

    **Step 2 — Rewrite index.tsx:**
    Preserve the TanStack Router export pattern:
    ```tsx
    export const Route = createFileRoute("/")({ component: LandingPage });
    function LandingPage() { ... }
    ```

    Implement sections using exact content from CONTEXT.md decisions (listed in <interfaces> above). Layout from Figma screenshot. Use inline styles matching project patterns (no Tailwind).

    **Nav bar section:**
    - White or transparent bg, logo left, nav links right
    - `<img src={logoSrc} alt="CoE DSC" style={{ width: "76px", height: "auto" }} />`
    - "Log In" as text link → `/login`; "Register" as antd Button type="primary" → `/register`

    **Hero section:**
    - Dark navy background (`#06004f`) or gradient per Figma
    - Headline (Rubik Bold, large), subtitle, two CTA buttons

    **"How does it work?" section:**
    - 3 step cards (Step 01, 02, 03) with number label, title, description per content above
    - Card style: white bg, 16px radius, antd Card or inline div

    **MAMI section:**
    - Text content as specified
    - "Get started" button → `/login` or `/register`

    **Footer:** Import and render the existing Footer component (from Phase 07-03, already in codebase — find it at `frontend/src/components/Footer.tsx` or similar path, adjust import if needed).

    Use design tokens: #06004f navy, #399e5a green, Rubik font, borderRadius 16px.
    Do NOT add new npm packages. Use existing antd + TanStack Router.
  </action>
  <verify>
    <automated>cd "C:/Users/djlia/Desktop/MaMi Checker/frontend" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compiles clean. index.tsx contains no Dutch strings. Logo import from ../assets/logo-coe-dsc.svg present. All 4 sections (nav, hero, how-it-works, MAMI section) render with English content matching CONTEXT.md spec.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
`grep -i "vorige\|volgende\|meld\|aanmeld\|hoe\|werkt" frontend/src/routes/index.tsx` — no Dutch words.
`grep "logo-coe-dsc" frontend/src/routes/index.tsx` — import present.
</verification>

<success_criteria>
- Homepage is fully in English with content matching CONTEXT.md locked decisions
- CoE DSC logo renders in nav (not text)
- Layout matches Figma node 154-3458 as closely as possible
- TypeScript compiles without errors
- Footer component still renders
</success_criteria>

<output>
After completion, create `.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-04-SUMMARY.md`
</output>
