---
phase: 08-figma-design-cleanup-and-compliance-report-styling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/questionnaire/WizardPage.tsx
autonomous: true
requirements:
  - UI-FIX-01
  - UI-FIX-03
must_haves:
  truths:
    - "Question X of Y pill appears in the top-right corner of the question card header, not inline next to the topic label"
    - "Navigation buttons read 'Previous', 'Next', and 'Finish' (English, no Dutch)"
  artifacts:
    - path: "frontend/src/components/questionnaire/WizardPage.tsx"
      provides: "Updated WizardPage with English nav labels and repositioned question pill"
  key_links:
    - from: "card header row"
      to: "AutosaveBadge + question pill"
      via: "flex layout — pill top-right, badge below or beside"
      pattern: "Question.*of.*pill.*header"
---

<objective>
Fix two Figma alignment gaps in the questionnaire wizard: move the "Question X of Y" pill to the top-right of the question card header, and replace Dutch navigation labels ("Vorige"/"Volgende"/"Voltooien") with English equivalents ("Previous"/"Next"/"Finish").

Purpose: Align the wizard UI with the approved Figma design and English language requirement.
Output: Updated WizardPage.tsx with repositioned pill and English button labels.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts from WizardPage.tsx -->

Current "Question X of Y" pill location (lines ~427-463):
```tsx
{/* Topic label + "Question X of Y" chip */}
<div style={{ display: "flex", alignItems: "center", gap: "0.75rem", marginBottom: "0.75rem" }}>
  <h4 ...>{currentTopic.label}</h4>
  <span style={{ /* pill styles */ }}>Question X of Y</span>
</div>
```

Current card header (lines ~401-420) — has category title left + autosave badge right:
```tsx
<div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "1rem" }}>
  <h3>{currentCategory.label}</h3>
  <AutosaveBadge state={badgeState} />
</div>
```

Nav buttons (lines ~534, ~553):
```tsx
<button ...>← Vorige</button>
<button ...>{isSaving ? "Saving..." : isFinish ? "Voltooien →" : "Volgende →"}</button>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reposition "Question X of Y" pill to card header top-right</name>
  <files>frontend/src/components/questionnaire/WizardPage.tsx</files>
  <action>
    In WizardPage.tsx, move the "Question X of Y" / "Questions X–Y of Z" pill from the topic label row into the card header row.

    Current card header (find by `justifyContent: "space-between"` containing the category h3 and AutosaveBadge):
    - Change to a three-element flex row: category title (left, flex:1) | question pill (center or right) | autosave badge (right)
    - OR: keep two-row header — row 1: category title (left) + question pill (right); row 2: autosave badge (right-aligned, smaller)
    - Use user decision: top-right corner of card header, next to or replacing autosave badge position. Autosave badge can move below. Choose whichever layout keeps both visible without crowding.

    Remove the pill span from the topic label row (the `<div>` containing `<h4>` and the pill `<span>`). The h4 topic label stays but the pill moves up to the card header.

    Pill styling to keep (from context): `fontSize: "0.75rem"`, `background: "rgba(6,0,79,0.06)"`, `borderRadius: "100px"`, `padding: "2px 10px"`, `color: "rgba(6,0,79,0.5)"`, Rubik font.
  </action>
  <verify>
    <automated>cd "C:/Users/djlia/Desktop/MaMi Checker/frontend" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compiles clean. The pill span is no longer a sibling of the h4 topic label; it appears inside the card header flex row. The topic label h4 still renders below the header.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Dutch navigation labels with English</name>
  <files>frontend/src/components/questionnaire/WizardPage.tsx</files>
  <action>
    In WizardPage.tsx, replace all Dutch button labels with English equivalents:
    - "← Vorige" → "← Previous"
    - The ternary `isSaving ? "Saving..." : isFinish ? "Voltooien →" : "Volgende →"` → `isSaving ? "Saving..." : isFinish ? "Finish →" : "Next →"`

    Search for "Vorige", "Volgende", "Voltooien" and replace all occurrences. There should be exactly 3 replacement sites (lines ~534, ~553 per CONTEXT.md — verify by grep before editing).

    Do not change any other logic, only the string literals.
  </action>
  <verify>
    <automated>grep -n "Vorige\|Volgende\|Voltooien" "C:/Users/djlia/Desktop/MaMi Checker/frontend/src/components/questionnaire/WizardPage.tsx" && echo "FAIL: Dutch strings remain" || echo "PASS: No Dutch strings"</automated>
  </verify>
  <done>Zero occurrences of "Vorige", "Volgende", or "Voltooien" in WizardPage.tsx. "Previous", "Next", "Finish" are the visible button labels.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` in the frontend directory. Zero errors expected.
Visually: load the questionnaire wizard — "Question X of Y" pill appears in the card header area (top-right), navigation shows "Previous" / "Next" / "Finish".
</verification>

<success_criteria>
- No Dutch strings in WizardPage.tsx nav buttons
- Question pill is inside card header row, not next to the h4 topic label
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-figma-design-cleanup-and-compliance-report-styling/08-01-SUMMARY.md`
</output>
