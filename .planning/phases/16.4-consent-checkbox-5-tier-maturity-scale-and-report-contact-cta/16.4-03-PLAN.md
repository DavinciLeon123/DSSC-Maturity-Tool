---
phase: 16.4-consent-checkbox-5-tier-maturity-scale-and-report-contact-cta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_app/report.tsx
autonomous: true
requirements: [REQ-3]

must_haves:
  truths:
    - "A user who has completed the assessment and is viewing their report sees a thank-you message and a contact button below the existing report content"
    - "Clicking the contact button opens the user's mail client addressed to info@dssc.eu"
    - "The PDF report's existing 'Get expert help' box (report.html) is completely untouched — this CTA is in-app only, per D-04"
  artifacts:
    - path: "frontend/src/routes/_app/report.tsx"
      provides: "A new thank-you + mailto:info@dssc.eu Button block, styled consistently with the page's existing white-card/borderRadius-0/box-shadow idiom"
      contains: "mailto:info@dssc.eu"
  key_links:
    - from: "frontend/src/routes/_app/report.tsx new CTA block"
      to: "mailto:info@dssc.eu"
      via: "antd Button href prop, renders a real <a> tag"
      pattern: "href=\"mailto:info@dssc.eu\""
---

<objective>
Add a thank-you message + `mailto:info@dssc.eu` contact button to the in-app completed-assessment report page (REQ-3 of Phase 16.4).

Purpose: per 16.4-CONTEXT.md's **D-04** (locked, confirms research's recommendation) — this CTA is in-app only. The PDF's existing "Get expert help" box (`backend/app/templates/report.html:239-250`, shipped Phase 16/16.1/16.3, different wording, same `info@dssc.eu` mailto) stays completely untouched; it is a separate, complementary surface, not something this plan touches or duplicates content into.
Output: `frontend/src/routes/_app/report.tsx` gains a new CTA block, placed after the existing "Download PDF Report" button block, using the user's own wording (or a close variant, per "something like" latitude) and an antd `Button` with `href="mailto:info@dssc.eu"`.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.4-consent-checkbox-5-tier-maturity-scale-and-report-contact-cta/16.4-CONTEXT.md
@.planning/phases/16.4-consent-checkbox-5-tier-maturity-scale-and-report-contact-cta/16.4-RESEARCH.md

This phase (16.4) has NOT been through discuss-phase in the usual sense — 16.4-CONTEXT.md IS the locked-decisions document (D-01 through D-05). Treat every `D-0X` reference below as non-negotiable, already-resolved scope, not something to re-derive.

Locked decision this plan implements: **D-04** — in-app only, PDF untouched. Do NOT edit `backend/app/templates/report.html` in this plan (out of file scope entirely — its existing "Get expert help" box is a different, already-shipped feature that happens to share the same mailto address).

The user's copy ("something like" — some latitude explicitly granted): "Thank you for taking this assessment. If you have feedback or would like to have a conversation with an expert about your results, please click below to contact us." The mailto target `info@dssc.eu` and the core message (thanks + feedback/expert-conversation invitation) are locked; exact wording has latitude.
</context>

<interfaces>
<!-- Current exact state of the only file this plan touches. -->

From `frontend/src/routes/_app/report.tsx` — imports (line 1-5) already include everything needed, no new imports required:
```tsx
import { createFileRoute, useSearch } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { Button, Spin, Alert } from "antd";
import { api } from "../../lib/api";
import { fetchReportData, type ReportContract } from "../../lib/reports";
```

The tail end of the page (lines 292-320) — the two report cards close, then the download-button block, then the fragment/component close. This plan's new block goes AFTER this download-button `<div>`, still inside the same `{!loading && !error && data && (<>...</>)}` fragment:
```tsx
              <div>
                {data.priority_list.map((item) => (
                  <PriorityRow key={item.category_id} item={item} />
                ))}
              </div>
            </div>
          </div>

          <div style={{ marginTop: "32px" }}>
            <Button
              type="primary"
              size="large"
              loading={isDownloading}
              disabled={isDownloading}
              onClick={handleDownload}
              style={{
                borderRadius: "0",
                fontFamily: "'Jost', 'Helvetica Neue', Arial, sans-serif",
                fontWeight: 600,
              }}
            >
              Download PDF Report
            </Button>
            {downloadError && (
              <Alert type="error" message={downloadError} showIcon style={{ marginTop: "12px", maxWidth: "600px" }} />
            )}
          </div>
        </>
      )}
    </div>
  );
}
```

The page's established white-card styling idiom (matches the radar-chart and priority-list cards, lines ~243-251/272-280 — reuse this exact style, this page uses no `Card` component anywhere, all plain `<div>` + inline styles per the Phase 16.3 square-corner convention):
```tsx
style={{
  flex: "3 1 480px",
  background: "#ffffff",
  borderRadius: "0",
  boxShadow: "0 2px 12px rgba(0,142,207,0.08)",
  padding: "24px",
}}
```
The page's heading-color convention throughout is `#008ecf` with `fontFamily: "'Jost', 'Helvetica Neue', Arial, sans-serif"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add thank-you + mailto contact CTA block to the report page</name>
  <files>frontend/src/routes/_app/report.tsx</files>
  <action>
    Insert a new block immediately after the closing `</div>` of the `marginTop: "32px"` download-button block (i.e. the last element inside the `<>...</>` fragment, right before its closing `</>`  — see `<interfaces>` above for the exact surrounding code to locate this). No new imports are needed (`Button` is already imported).

    ```tsx
          <div
            style={{
              marginTop: "24px",
              background: "#ffffff",
              borderRadius: "0",
              boxShadow: "0 2px 12px rgba(0,142,207,0.08)",
              padding: "24px",
            }}
          >
            <p
              style={{
                fontFamily: "'Jost', 'Helvetica Neue', Arial, sans-serif",
                fontSize: "14px",
                color: "#008ecf",
                marginBottom: "16px",
              }}
            >
              Thank you for taking this assessment. If you have feedback or would like to
              have a conversation with an expert about your results, please click below to
              contact us.
            </p>
            <Button
              href="mailto:info@dssc.eu"
              size="large"
              style={{
                borderRadius: "0",
                fontFamily: "'Jost', 'Helvetica Neue', Arial, sans-serif",
                fontWeight: 600,
              }}
            >
              Contact us
            </Button>
          </div>
    ```

    Notes:
    - Use `type="default"` behavior (i.e. do NOT pass `type="primary"`) so this secondary CTA is visually distinct from the primary "Download PDF Report" button above it — matches this page's existing visual hierarchy (one primary action, no competing primary buttons).
    - antd `Button` with an `href` prop renders a real `<a href="mailto:...">` under the hood — no `target="_blank"` needed (unlike the PDF's `www.dssc.eu` external link, a `mailto:` link isn't a real cross-origin navigation).
    - Do not touch `backend/app/templates/report.html` in this task (D-04, out of scope for this plan entirely).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint && grep -n "mailto:info@dssc.eu" src/routes/_app/report.tsx</automated>
  </verify>
  <done>report.tsx renders a new white card below the Download PDF Report button, containing the thank-you copy and a "Contact us" Button whose href is mailto:info@dssc.eu; tsc/eslint both pass clean; report.html is untouched (confirm via git status/diff showing no changes to that file).</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck && npm run lint` — clean.
- `grep -n "mailto:info@dssc.eu" frontend/src/routes/_app/report.tsx` — one match.
- `git diff --stat -- backend/app/templates/report.html` — empty (confirms D-04's in-app-only scope was respected).
</verification>

<success_criteria>
- Loading a completed assessment's report page shows the new thank-you text and "Contact us" button below the existing radar/priority-list cards and the Download PDF Report button.
- Clicking "Contact us" opens the user's default mail client addressed to info@dssc.eu.
- No backend file, and no other frontend file, changed by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/16.4-consent-checkbox-5-tier-maturity-scale-and-report-contact-cta/16.4-03-SUMMARY.md`
</output>
