---
phase: 16.1-dssc-rebranding-pdf-content-registration-default
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/_app/admin.heatmap.tsx
  - frontend/src/routes/_app/admin.index.tsx
  - frontend/src/routes/_app/assessments.tsx
  - frontend/src/routes/_app/initiative.tsx
  - frontend/src/routes/_app/questionnaire.tsx
  - frontend/src/routes/_app/report.tsx
  - frontend/src/routes/_auth/login.tsx
  - frontend/src/routes/_auth/forgot-password.tsx
  - frontend/src/routes/_auth/register.tsx
  - backend/app/api/v1/reports.py
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "No file in frontend/src/routes contains the string 'Rubik' anywhere in font declarations — BRAND-01 per D-04"
    - "No file in frontend/src/routes contains old hex colors #06004f, #399e5a, or old rgba(6,0,79,...) variants — BRAND-01 per D-02"
    - "All font declarations in the 9 broken route files are 'Open Sans' (body text, per D-04), matching the patterns in dashboard.tsx and reset-password.tsx"
    - "All color values in the 9 broken route files use DSSC brand colors: #008ecf (Blue), #76b82a (Green), or opacity-adjusted rgba(0,142,207,...) / rgba(118,184,42,...) — BRAND-01 per D-02"
    - "The PDF download filename in reports.py line 242 is 'DSSC-Maturity-Report.pdf', matching line 127 — BRAND-02 per D-11"
  artifacts:
    - "frontend/src/routes/_app/admin.heatmap.tsx — zero Rubik, zero #399e5a, zero rgba(6,0,79,...)"
    - "frontend/src/routes/_app/admin.index.tsx — zero Rubik, zero #399e5a"
    - "frontend/src/routes/_app/assessments.tsx — zero Rubik, zero rgba(6,0,79,...)"
    - "frontend/src/routes/_app/initiative.tsx — zero Rubik, zero rgba(6,0,79,...), zero #399e5a"
    - "frontend/src/routes/_app/questionnaire.tsx — zero Rubik"
    - "frontend/src/routes/_app/report.tsx — zero Rubik, zero rgba(6,0,79,...)"
    - "frontend/src/routes/_auth/login.tsx — zero #399e5a (link colors)"
    - "frontend/src/routes/_auth/forgot-password.tsx — zero #399e5a (link colors)"
    - "frontend/src/routes/_auth/register.tsx — zero #399e5a (toggle colors, link colors)"
    - "backend/app/api/v1/reports.py line 242 — filename is 'DSSC-Maturity-Report.pdf'"
  key_links:
    - "Literal grep verification is mandatory: each task must prove zero remaining occurrences of old values via explicit shell commands, not subjective inspection"
    - "PATTERNS.md provides exact before/after patterns for all 10 files and is the source of truth for the migrations"
    - "Prior plan 16.1-01 claimed 'zero occurrences remain' but grep verification disproved this — acceptance criteria this round MUST be literal runnable commands"

---

<objective>
Fix residual CoE-DSC/MAMI branding (old colors, old fonts, old filename) across 9 broken frontend route files and 1 backend endpoint file, completing BRAND-01/BRAND-02/BRAND-03 requirements.

**Prior Failure Context:**  
Plan 16.1-01 claimed "zero occurrences of old hex values or Rubik" but independent grep verification discovered 84 instances (51 Rubik, 33 colors) still present. This gap-closure round uses literal runnable grep commands as acceptance criteria, not subjective claims.

Purpose: Close the gap between claimed and actual branding completion; ensure all font, color, and filename references align with DSSC identity (D-02 Blue #008ecf / Green #76b82a, D-04 Open Sans, D-11 DSSC filename).

Output: 10 corrected files with zero remaining old-branding references, verified by grep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-PATTERNS.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix frontend app-route files (6 files: admin.heatmap, admin.index, assessments, initiative, questionnaire, report)</name>
  <files>
    frontend/src/routes/_app/admin.heatmap.tsx
    frontend/src/routes/_app/admin.index.tsx
    frontend/src/routes/_app/assessments.tsx
    frontend/src/routes/_app/initiative.tsx
    frontend/src/routes/_app/questionnaire.tsx
    frontend/src/routes/_app/report.tsx
  </files>
  <read_first>
    - 16.1-PATTERNS.md (sections: "admin.heatmap.tsx", "admin.index.tsx", "assessments.tsx", "initiative.tsx", "questionnaire.tsx", "report.tsx" — these contain exact line-by-line before/after patterns extracted from the reference analog file dashboard.tsx)
    - frontend/src/routes/_app/dashboard.tsx (the correctly-rebranded reference file — extract exact patterns: font declarations using "'Open Sans', sans-serif", color values using rgba(0,142,207,...) and #76b82a)
    - Each broken file being edited (read each one to understand its current state before making targeted edits)
  </read_first>
  <action>
    For each of the 6 app-route files, apply two systematic passes:

    **Pass 1 — Font replacement (Rubik → Open Sans):**
    Replace all instances of `fontFamily: "'Rubik', sans-serif"` with `fontFamily: "'Open Sans', sans-serif"` throughout the file. Per PATTERNS.md and dashboard.tsx examples, all app-route components use Open Sans for body/section text (not Jost, which is PDF-only).

    **Pass 2 — Color replacement:**
    1. All instances of `color: "rgba(6,0,79,X)"` (old navy with opacity X) → `color: "rgba(0,142,207,X)"` (DSSC Blue, same opacity X)
    2. All instances of `boxShadow: "...rgba(6,0,79,X)..."` → `boxShadow: "...rgba(0,142,207,X)..."`
    3. All instances of `borderBottom: "...rgba(6,0,79,X)..."` → `borderBottom: "...rgba(0,142,207,X)..."`
    4. All instances of `color: "#399e5a"` (old green) → `color: "#76b82a"` (DSSC Green)
    5. All instances of `border: "...#399e5a..."` → `border: "...#76b82a..."`
    6. All instances of `background: "#399e5a"` → `background: "#76b82a"`

    **Concrete file-by-file specifics (from PATTERNS.md):**

    - **admin.heatmap.tsx**: Lines 147, 199, 209, 217, 235, 249, etc. — replace line 147's `fontFamily: "'Rubik', sans-serif"` with `"'Open Sans', sans-serif"`; replace all 5 color values (rgba and #399e5a) at the specified lines.
    - **admin.index.tsx**: Lines 306, 341, 411 have Rubik; line 341 has #399e5a color. Replace Rubik with Open Sans; replace color #399e5a with #76b82a.
    - **assessments.tsx**: Lines 137, 171, 181, 204, 231 have Rubik; lines 158, 164, 179, 194, 222 have `color: "rgba(6,0,79,0.6)"` etc. Replace all Rubik instances with Open Sans; replace all old navy rgba values with new DSSC Blue rgba (keeping opacity unchanged).
    - **initiative.tsx**: Largest file — 21 Rubik instances at lines 41, 167, 178, 214, 223, 235, 251, 252, 263, 272, 300, 311, 324, 331, 338, 347, 364, 372, 381, 409, 420, 433, 440, 447, 456 (exact line numbers may shift during execution — use search/replace within the file); 10 color instances with old navy rgba and #399e5a. Replace all with Open Sans and DSSC colors respectively.
    - **questionnaire.tsx**: 2 Rubik instances at lines 42 and 51 (or nearby — exact lines may shift). Replace both with Open Sans.
    - **report.tsx**: 8 Rubik instances at lines 59, 70, 81, 191, 225, 254, 283, 309; 6 color instances at lines 42, 73, 185, 227, 248, 277. Replace Rubik with Open Sans; replace old navy rgba with DSSC Blue rgba and old green (#399e5a) with #76b82a.

    **Execution note:** Use your editor's find-and-replace feature to apply these transformations systematically file-by-file. Verify each replacement by viewing the affected lines before moving to the next file. The PATTERNS.md file provides exact before/after code blocks for reference.

    Per CONTEXT.md D-02 and D-04, the DSSC brand tokens are:
    - Primary Blue: #008ecf
    - Secondary Green: #76b82a
    - Font: Open Sans (for components, not PDF)
  </action>
  <verify>
    <automated>test $(grep -c "'Rubik'" frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/initiative.tsx frontend/src/routes/_app/questionnaire.tsx frontend/src/routes/_app/report.tsx | grep -v ':0$' | wc -l) -eq 0 && test $(grep -c "rgba(6,0,79" frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/initiative.tsx frontend/src/routes/_app/questionnaire.tsx frontend/src/routes/_app/report.tsx | grep -v ':0$' | wc -l) -eq 0 && test $(grep -c "#399e5a" frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/initiative.tsx frontend/src/routes/_app/report.tsx | grep -v ':0$' | wc -l) -eq 0</automated>
  </verify>
  <done>
    - All 6 app-route files have zero remaining 'Rubik' references (verified by grep)
    - All 6 app-route files have zero remaining old navy rgba(6,0,79,...) references (verified by grep)
    - All files with old green #399e5a have zero remaining references (verified by grep)
    - All replacements use DSSC brand colors per D-02: Blue #008ecf / Green #76b82a
    - All font declarations use Open Sans per D-04
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend auth-route files (3 files: login, forgot-password, register) — old green color #399e5a → #76b82a</name>
  <files>
    frontend/src/routes/_auth/login.tsx
    frontend/src/routes/_auth/forgot-password.tsx
    frontend/src/routes/_auth/register.tsx
  </files>
  <read_first>
    - 16.1-PATTERNS.md (sections: "login.tsx", "forgot-password.tsx", "register.tsx" — these contain exact before/after patterns)
    - frontend/src/routes/_auth/reset-password.tsx (the correctly-rebranded reference file — no action needed, reference only; shows proper DSSC Green #76b82a for link colors and toggle styling)
    - Each broken auth file being edited
  </read_first>
  <action>
    For each of the 3 auth-route files, replace all instances of the old green color #399e5a with the DSSC Green #76b82a. These files are smaller and contain only color updates (no font changes), as none of them currently use Rubik font.

    **Concrete file-by-file specifics (from PATTERNS.md):**

    - **login.tsx (lines 101, 104):** Two `<Link>` elements styling inline with `color: "#399e5a"`. Replace both with `color: "#76b82a"`.
      Before: `<Link to="/register" style={{ color: "#399e5a", fontWeight: 600, textDecoration: "none" }}>`
      After: `<Link to="/register" style={{ color: "#76b82a", fontWeight: 600, textDecoration: "none" }}>`

    - **forgot-password.tsx (2 link instances):** Identical pattern to login.tsx. Find all `color: "#399e5a"` and replace with `color: "#76b82a"`.

    - **register.tsx (3 instances total — 2 toggle button styling, 1 link):**
      Lines 96-97 (toggle button styling):
        Before: `border: \`2px solid ${participantType === type ? "#399e5a" : "rgba(0,142,207,0.2)"}\`,`
                `background: participantType === type ? "#399e5a" : "white",`
        After: `border: \`2px solid ${participantType === type ? "#76b82a" : "rgba(0,142,207,0.2)"}\`,`
               `background: participantType === type ? "#76b82a" : "white",`
      Line 129 (link):
        Before: `<Link to="/login" style={{ color: "#399e5a", fontWeight: 600, textDecoration: "none" }}>`
        After: `<Link to="/login" style={{ color: "#76b82a", fontWeight: 600, textDecoration: "none" }}>`

    **Execution:** Use find-and-replace to swap `"#399e5a"` with `"#76b82a"` in each file. The grep verification below will confirm all replacements are complete.

    Per CONTEXT.md D-02, the DSSC Green (secondary color) is #76b82a.
  </action>
  <verify>
    <automated>test $(grep -c "#399e5a" frontend/src/routes/_auth/login.tsx frontend/src/routes/_auth/forgot-password.tsx frontend/src/routes/_auth/register.tsx | grep -v ':0$' | wc -l) -eq 0</automated>
  </verify>
  <done>
    - All 3 auth-route files have zero remaining #399e5a (old green) references (verified by grep)
    - All replaced values use DSSC Green #76b82a per D-02
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix backend PDF filename (reports.py line 242) — old 'MAMI-Interoperability-Report.pdf' → 'DSSC-Maturity-Report.pdf'</name>
  <files>
    backend/app/api/v1/reports.py
  </files>
  <read_first>
    - 16.1-PATTERNS.md (section: "backend/app/api/v1/reports.py" — shows exact before/after for line 242)
    - backend/app/api/v1/reports.py lines 120-250 (context around both line 127 and line 242; line 127 in _send_report_email is already correct, line 242 in download_report_pdf is not)
  </read_first>
  <action>
    Update line 242 in `backend/app/api/v1/reports.py`, in the `download_report_pdf()` function's `Content-Disposition` header, to use the new DSSC filename matching line 127 in `_send_report_email()`.

    **Exact change (from PATTERNS.md):**

    Before (line 242):
    ```python
    headers={"Content-Disposition": "attachment; filename=MAMI-Interoperability-Report.pdf"},
    ```

    After (line 242):
    ```python
    headers={"Content-Disposition": "attachment; filename=DSSC-Maturity-Report.pdf"},
    ```

    Verify that line 127 in `_send_report_email()` already contains the correct filename (it should):
    ```python
    "DSSC-Maturity-Report.pdf"
    ```

    Once line 242 is updated, both locations use the same filename per D-11, and the PDF download will save with the DSSC branding.
  </action>
  <verify>
    <automated>test $(grep -c "MAMI-Interoperability-Report" backend/app/api/v1/reports.py) -eq 0 && test $(grep -c "DSSC-Maturity-Report.pdf" backend/app/api/v1/reports.py) -ge 2</automated>
  </verify>
  <done>
    - The old filename "MAMI-Interoperability-Report.pdf" is completely removed from reports.py (verified by grep returning 0)
    - The new filename "DSSC-Maturity-Report.pdf" is present at both line 127 (_send_report_email) and line 242 (download_report_pdf), verified by grep returning >= 2
    - Requirement BRAND-02 is satisfied: both PDF filename occurrences now use DSSC branding
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase is pure cosmetic/string-level changes: font declarations, color hex values, and a filename string in a Content-Disposition header. No new input handling, no new auth paths, no new data processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.1-GC-01 | None | N/A | N/A | N/A | **No new attack surface introduced.** Phase 16.1 gap-closure only modifies CSS color values, font family declarations, and a static filename string. All input validation, authentication, and data processing remain unchanged. The changes are purely presentational and have zero impact on security posture. |

**Rationale for zero threats:** The STRIDE framework applies to code changes that introduce new input handling, trust boundaries, data flows, or authentication logic. This gap-closure round is pure string replacement (colors, fonts, filenames) — equivalent to updating marketing copy or logo assets. No new endpoints, no new parameters, no new validation logic.

</threat_model>

<probe_findings>
## Spec-less Probe Findings (N/A per protocol)

This phase has no SPEC.md, so the spec-less probe fallback ran against BRAND-01/BRAND-02/BRAND-03's requirement text. It returned 5 unresolved/unclassified items — 3 for BRAND-01 (adjacency, empty-input, ordering categories) and 1 each unclassified for BRAND-02 and BRAND-03. All 5 are explicitly marked N/A here rather than silently dropped, per the protocol's no-silent-drop-equality rule:

- BRAND-01 adjacency/empty/ordering: N/A — these are data-structure/algorithmic-boundary heuristics (e.g. "what happens on empty input", "are equal elements merged or separated"). This requirement is a pure CSS color/font-string replacement with no data structures, collections, or ordering involved.
- BRAND-02/BRAND-03 unclassified: N/A — same reason; these are wording/filename-string requirements with no algorithmic shape for the probe to classify.

No prohibitions were minted either: a Stage-1/Stage-2 prohibition-recall pass over BRAND-01/02/03 surfaced nothing beyond routine engineering hygiene (e.g. "don't introduce a new color that isn't in the design system") — already covered by must_haves.truths citing D-02/D-04 — so nothing canon-security/compliance-adjacent was found to mint or drop.
</probe_findings>

<verification>
## Phase Verification (Gap-Closure Specific)

**Mandatory literal grep verification after all tasks complete:**

```bash
# BRAND-01 verification: zero old fonts + zero old colors in frontend routes
echo "=== BRAND-01 Frontend Font/Color Verification ==="
echo "Rubik instances in app routes:"
grep -c "'Rubik'" frontend/src/routes/_app/*.tsx | grep -v ':0$' && echo "FAIL: Rubik still present" || echo "PASS: zero Rubik"

echo "Old navy rgba in app routes:"
grep -c "rgba(6,0,79" frontend/src/routes/_app/*.tsx | grep -v ':0$' && echo "FAIL: old navy rgba still present" || echo "PASS: zero old navy rgba"

echo "Old green #399e5a in all routes:"
grep -c "#399e5a" frontend/src/routes/_app/*.tsx frontend/src/routes/_auth/*.tsx | grep -v ':0$' && echo "FAIL: old green still present" || echo "PASS: zero old green"

# BRAND-02/BRAND-03 verification: PDF filename updated
echo "=== BRAND-02/BRAND-03 Backend Filename Verification ==="
echo "Old filename occurrences:"
grep -c "MAMI-Interoperability-Report" backend/app/api/v1/reports.py && echo "FAIL: old filename still present" || echo "PASS: zero old filename"

echo "New filename occurrences (must be >= 2, for line 127 and line 242):"
grep -c "DSSC-Maturity-Report.pdf" backend/app/api/v1/reports.py
```

**Expected output:** All grep counts must be 0 for old values; new filename count must be >= 2.

## Test Suite Green

Run the frontend test suite to ensure no regressions:
```bash
cd frontend && npm test
```

Run the backend test suite:
```bash
cd backend && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q
```

Both should pass with no new failures introduced by the cosmetic changes.

</verification>

<success_criteria>
- [ ] All 10 files (9 frontend routes + 1 backend endpoint) pass literal grep verification for zero old-brand references
- [ ] No 'Rubik' font references remain in any frontend file
- [ ] No old color hex values (#06004f, #399e5a, rgba(6,0,79,...)) remain in any file
- [ ] PDF download filename at line 242 of reports.py is "DSSC-Maturity-Report.pdf"
- [ ] All test suites (frontend and backend) pass with no new regressions
- [ ] BRAND-01 (visual identity), BRAND-02 (wording/filename), and BRAND-03 (PDF branding) are fully satisfied
</success_criteria>

<output>
Create `.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-04-SUMMARY.md` when done.

**No new files or symbols created by this plan** — this is a pure gap-closure / edit-and-fix round. All changes are replacements within existing files. The SUMMARY.md should document:
- Original gap: 84 instances of old branding (51 Rubik, 33 colors, 1 filename) across 10 files (claimed fixed by Plan 16.1-01 but actually not)
- Resolution: All 84 instances identified and corrected via targeted grep-verified replacements
- Verification method: Literal bash grep commands returning zero counts for old values
- Requirements satisfied: BRAND-01, BRAND-02, BRAND-03
</output>
