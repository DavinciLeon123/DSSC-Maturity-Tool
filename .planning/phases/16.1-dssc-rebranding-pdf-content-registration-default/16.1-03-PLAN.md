---
phase: 16.1-dssc-rebranding-pdf-content-registration-default
plan: 03
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  - frontend/src/routes/_auth/register.tsx
  - frontend/src/components/layout/TopNav.test.tsx
autonomous: true
requirements: [REG-01]

must_haves:
  truths:
    - "A new registration submits participant_type: 'DSI' unconditionally — the DSI/SP toggle UI is never rendered to the user"
    - "The SP option is restorable later by flipping a single module-level boolean constant back to true — no JSX rewrite, no backend/schema change required"
    - "The existing SHOW_PARTICIPANT_TYPE_TOGGLE-guarded useState default ('DSI') is unchanged, so hiding the toggle and defaulting to DSI happen in the same code path with no separate 'set the default' step — see flagged assumption below"
    - "TopNav.test.tsx's alt-text assertion matches TopNav.tsx's actual DSSC alt text (set by 16.1-01), so the existing frontend test suite is green"
  artifacts: []
  key_links:
    - "register.tsx's participantType useState default ('DSI', line 14, untouched by this plan) is the ONLY source of the submitted participant_type once the toggle is hidden — SHOW_PARTICIPANT_TYPE_TOGGLE gates rendering only, not submission logic (line 27's JSON.stringify body is untouched), so there is exactly one code path producing the submitted value, not two that could disagree"
  prohibitions:
    - "Do not delete the toggle JSX or the participantType state — wrap-in-a-conditional only, per D-14's explicit 'stays in the code but is feature-flagged off' requirement"
    - "Do not touch backend/app/schemas/auth.py's Literal[\"DSI\",\"SP\"] = \"DSI\" default or any other backend file — D-14 confirms DSI is already the schema default, no backend change needed"
    - "Do not touch any color/hex/font value in register.tsx in this plan — that is 16.1-01's completed scope; this plan only wraps existing JSX in a conditional and adds one constant"

  # REG-01 edge-probe coverage (spec-less fallback, no SPEC.md exists for
  # this phase):
  # - adjacency/ordering ("do hiding the toggle and defaulting to DSI happen
  #   in the same task, any ordering ambiguity"): flagged assumption, not a
  #   real gap — register.tsx's participantType useState already defaults to
  #   "DSI" today (line 14, pre-existing, unrelated to this plan). This plan
  #   only wraps the toggle's JSX in a conditional; it does not add or
  #   reorder a "defaulting" step, because none is needed — one existing
  #   default + one new render-gate, applied in the same task, in the same
  #   file, in one commit. No two-step sequencing exists to have an
  #   ordering question about.
  # - empty/null: N/A — SHOW_PARTICIPANT_TYPE_TOGGLE is a compile-time
  #   boolean literal, not a runtime/user-supplied value; there is no empty
  #   or null state for it to take.
---

<objective>
Hide the DSI/Service-Provider registration toggle behind a feature flag that defaults new accounts to DSI-only, without deleting the toggle code or touching the backend schema (D-14), and fix the one existing frontend test this phase's logo-alt-text rename (16.1-01) breaks.

Purpose: D-14 requires the SP option be restorable via a one-line constant flip later — wrapping the existing JSX in a conditional (rather than deleting it) is the only implementation that satisfies "no JSX rewrite... needed" when restoring. This plan is sequenced in Wave 2 (depends_on 16.1-01) purely because both plans edit register.tsx (16.1-01: color/logo; this plan: the toggle block) — no other reason.
Output: register.tsx submits DSI-only registrations with the toggle hidden but intact; TopNav.test.tsx's alt-text assertion matches the new DSSC alt text.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature-flag the DSI/SP toggle in register.tsx (D-14)</name>
  <files>frontend/src/routes/_auth/register.tsx</files>
  <read_first>
    frontend/src/routes/_auth/register.tsx (full file, confirmed 129 lines — line 14's `useState<"DSI" | "SP">("DSI")`, line 27's submit body, lines 78-109 the "I am a:" toggle block, structure unchanged in line-count by 16.1-01's color-only edits)
    .planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md (D-14)
    backend/app/schemas/auth.py line 22 (`participant_type: Literal["DSI", "SP"] = "DSI"` — confirms no backend change needed)
  </read_first>
  <action>
    Add a module-level constant near the top of frontend/src/routes/_auth/register.tsx, after the imports
    and before the `Route` export: `const SHOW_PARTICIPANT_TYPE_TOGGLE = false;` with a one-line comment
    noting it is the single flip-point to restore the Service Provider registration option (D-14) — no other
    code change needed to restore it, since the backend schema and the useState default already support "SP"
    unconditionally.

    Wrap the entire "I am a:" block — the `<label>I am a:</label>`, the button-pair `<div>` mapping over
    `(["DSI", "SP"] as const)`, and the trailing descriptive `<p>` showing "Data Space Initiative"/"Service
    Provider" — in `{SHOW_PARTICIPANT_TYPE_TOGGLE && ( ... )}`. Do not change the `useState<"DSI" |
    "SP">("DSI")` default (line 14) or the submit body's `participant_type: participantType` (line 27) — with
    the toggle hidden, `participantType` never changes from its "DSI" default, so every new registration
    submits `participant_type: "DSI"` with zero additional logic needed.
  </action>
  <verify>
    <automated>grep -q "const SHOW_PARTICIPANT_TYPE_TOGGLE = false;" frontend/src/routes/_auth/register.tsx && grep -q "{SHOW_PARTICIPANT_TYPE_TOGGLE &&" frontend/src/routes/_auth/register.tsx && grep -q 'useState<"DSI" | "SP">("DSI")' frontend/src/routes/_auth/register.tsx && cd frontend && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const SHOW_PARTICIPANT_TYPE_TOGGLE = false;" frontend/src/routes/_auth/register.tsx` is 1
    - `grep -c "{SHOW_PARTICIPANT_TYPE_TOGGLE &&" frontend/src/routes/_auth/register.tsx` is 1
    - `grep -c 'useState<"DSI" | "SP">("DSI")' frontend/src/routes/_auth/register.tsx` is 1 (unchanged)
    - `grep -c "participant_type: participantType" frontend/src/routes/_auth/register.tsx` is 1 (unchanged)
    - `cd frontend && npm run typecheck && npm run build` both pass
  </acceptance_criteria>
  <done>The "I am a:" toggle block renders nothing (SHOW_PARTICIPANT_TYPE_TOGGLE is false); every new registration submits participant_type: "DSI"; the toggle JSX and state remain in the file, restorable by flipping the one constant to true.</done>
</task>

<task type="auto">
  <name>Task 2: Fix TopNav.test.tsx's alt-text assertion (broken by 16.1-01's logo rename)</name>
  <files>frontend/src/components/layout/TopNav.test.tsx</files>
  <read_first>
    frontend/src/components/layout/TopNav.test.tsx (full file, confirmed 49 lines — line 45's `expect(screen.getByAltText('CoE DSC')).toBeInTheDocument();`)
    frontend/src/components/layout/TopNav.tsx (confirm the `<img alt="DSSC" .../>` value 16.1-01 actually set, since this test must match it exactly)
  </read_first>
  <action>
    Update the assertion at line 45 from `expect(screen.getByAltText('CoE DSC')).toBeInTheDocument();` to
    `expect(screen.getByAltText('DSSC')).toBeInTheDocument();` — matching exactly the `alt` value 16.1-01 set
    on TopNav.tsx's logo `<img>` tag (read TopNav.tsx first to confirm the exact string before editing the
    test, in case 16.1-01's executor chose different wording than this plan assumes).
  </action>
  <verify>
    <automated>grep -q "getByAltText('DSSC')" frontend/src/components/layout/TopNav.test.tsx && ! grep -q "getByAltText('CoE DSC')" frontend/src/components/layout/TopNav.test.tsx && cd frontend && npm run test -- --run TopNav.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getByAltText('DSSC')" frontend/src/components/layout/TopNav.test.tsx` is 1
    - `grep -c "getByAltText('CoE DSC')" frontend/src/components/layout/TopNav.test.tsx` is 0
    - `cd frontend && npm run test -- --run TopNav.test.tsx` (or the project's equivalent Vitest invocation) passes
  </acceptance_criteria>
  <done>TopNav.test.tsx's alt-text assertion matches TopNav.tsx's actual post-rebrand alt attribute; the test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registration form -> POST /auth/register | Pre-existing boundary, unchanged by this plan. The frontend already sends whatever `participantType` currently is; the backend's `Literal["DSI", "SP"] = "DSI"` schema still accepts either value unconditionally — this plan only changes what the UI lets a human choose, not what the API accepts. |

## STRIDE Threat Register

Per this phase's actual narrow scope (ASVS L1, do not manufacture threats beyond what's real):

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.1-05 | Elevation of Privilege (checked, not present) | register.tsx's SHOW_PARTICIPANT_TYPE_TOGGLE | low | accept | Confirmed this is a frontend-only rendering gate — `backend/app/schemas/auth.py`'s `Literal["DSI","SP"]` still accepts `"SP"` unconditionally via direct API calls (curl/Postman), unchanged by this plan. This is explicitly accepted, not a gap to fix: D-14 only requires hiding the UI choice, not enforcing DSI-only at the API layer (no requirement asks for that, and REG-01's own text frames this as a UI-visibility change, not a data-integrity control). No backend authorization logic is touched by this plan, confirmed via read_first of schemas/auth.py. |
| T-16.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages. |
</threat_model>

<assumption_delta_decision>
An advisory pluralization-detector false-fired on the word "alongside" in the ROADMAP goal text for this
phase ("...adds a new per-question submitted-answers section to the PDF only... alongside the scores...").
Decision: no-change. "Submitted answers" and "dimension scores" are two sibling fields returned within one
report-rendering payload (RPRT-05's answers section rendered next to the existing priority-list/dimension
scores in the same PDF, 16.1-02's Task 2), not a pluralized identity or an enumerable list-of-things needing
a promote/add-alongside-question treatment. No SPEC.md question applies here.
</assumption_delta_decision>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass.
- `cd frontend && npm run test` (full Vitest suite) passes, including the fixed TopNav.test.tsx.
- Manual: load `/register` in a browser and confirm no "I am a:" toggle renders; submit a registration and confirm (via network tab or backend log) `participant_type: "DSI"` is sent.
</verification>

<success_criteria>
- The registration screen no longer offers a Service Provider option; every new account registers as DSI.
- The toggle's JSX/state remain in the codebase, restorable via a single constant flip, with no backend/schema change.
- The full frontend test suite (including TopNav.test.tsx) is green.
</success_criteria>

<output>
Create `.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-03-SUMMARY.md` when done
</output>
