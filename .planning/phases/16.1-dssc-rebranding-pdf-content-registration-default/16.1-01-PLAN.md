---
phase: 16.1-dssc-rebranding-pdf-content-registration-default
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/theme.ts
  - frontend/src/styles/globals.css
  - frontend/index.html
  - frontend/src/assets/logo-dssc-color.png
  - frontend/src/assets/logo-dssc-white.png
  - frontend/src/components/layout/TopNav.tsx
  - frontend/src/components/layout/Footer.tsx
  - frontend/src/components/questionnaire/QuestionCard.tsx
  - frontend/src/components/questionnaire/StepPills.tsx
  - frontend/src/components/questionnaire/WizardPage.tsx
  - frontend/src/components/questionnaire/AnswerButtonGroup.tsx
  - frontend/src/routes/_app.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/_auth/register.tsx
  - frontend/src/routes/_auth/login.tsx
  - frontend/src/routes/_auth/forgot-password.tsx
  - frontend/src/routes/_auth/reset-password.tsx
  - frontend/src/routes/_app/assessments.tsx
  - frontend/src/routes/_app/admin.heatmap.tsx
  - frontend/src/routes/_app/admin.index.tsx
  - frontend/src/routes/_app/report.tsx
  - frontend/src/routes/_app/initiative.tsx
  - frontend/src/routes/_app/about.tsx
  - frontend/src/routes/_app/dashboard.tsx
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "No file in frontend/src or frontend/index.html contains the old hex values #06004f, #399e5a, #00006b, or the old globals.css tokens #020059/#41A765/#3D52D5, or the string 'Rubik' anywhere"
    - "theme.ts's antd ThemeConfig and globals.css's :root custom properties resolve to DSSC Blue #008ecf / Green #76b82a / Jost+Open Sans"
    - "The two real DSSC logo PNGs exist at frontend/src/assets/, byte-identical to the source design assets, and are imported at every site that previously imported logo-coe-dsc.svg (TopNav.tsx, index.tsx, register.tsx, login.tsx, forgot-password.tsx, reset-password.tsx) plus two new white-logo placements (Footer.tsx, index.tsx hero section) per D-01"
    - "The antd Layout background (colorBgLayout/Layout.bodyBg) is plain white, not the old green-tinted rgba, per D-05"
    - "frontend/index.html loads Jost + Open Sans from Google Fonts and no longer references Rubik"
  artifacts:
    - frontend/src/assets/logo-dssc-color.png
    - frontend/src/assets/logo-dssc-white.png
  key_links:
    - "theme.ts (antd ThemeConfig tokens) and globals.css (:root CSS custom properties consumed by Sidebar.tsx/questionnaire.tsx via var(--color-navy) etc.) are the two centralized brand-token sources; every per-file inline-style swap in this plan must resolve to the SAME two new hex values (#008ecf/#76b82a) these central files declare, or the app will show visually inconsistent blues/greens across pages"
  prohibitions:
    - "Do not add a new npm font package (no @fontsource/* install) — Jost/Open Sans load via the existing Google Fonts <link>/@import URL mechanism only, preserving zero-dependency font-loading"
    - "Do not delete frontend/src/assets/logo-coe-dsc.svg — leave it in place even though nothing imports it after this plan lands (a stray unused asset is harmless; a broken import from a missed reference is not)"
    - "Do not touch any backend file (report.html, report_generator.py, reports.py, auth.py, main.py) — that recolor + wording work belongs to 16.1-02"
    - "Do not touch register.tsx's participantType state/toggle JSX (lines 14, 78-109) beyond straight hex/logo substitution — the SHOW_PARTICIPANT_TYPE_TOGGLE feature flag is 16.1-03's job, sequenced after this plan specifically because both plans touch register.tsx"

  # Edge-probe coverage (BRAND-01, spec-less fallback — no SPEC.md exists for
  # this phase; per the phase's edge-probe coverage report, adjacency/empty/
  # ordering probes are recorded as flagged assumptions rather than silently
  # dropped, since none of them meaningfully apply to a static color/font/
  # logo-asset swap):
  # - adjacency ("do two things merge/collide/separate when equal"): N/A — a
  #   hex token or font-family string has exactly one value at a time; there
  #   is no set of comparable/adjacent elements that could collide.
  # - empty ("empty/single/null input"): N/A — every file this plan touches
  #   is a fixed, non-empty existing file edited in place; there is no
  #   variable-length input collection whose empty case matters here.
  # - ordering ("stable order when equal"): N/A — CSS custom properties and
  #   antd theme tokens are key-value pairs, not an ordered list; no sort
  #   stability question applies.
  #
  # D-03 discretion note: the secondary palette (Amber #fbbc43, Purple
  # #50568e, Grey #627179) is explicitly "available at planner/executor
  # discretion... if a clear use arises" per CONTEXT.md — no clear UI role
  # for it surfaced during this plan's task breakdown (every touched file
  # only ever used the old primary navy/green pair, never a tertiary
  # accent), so it is deliberately left unused this phase rather than
  # inventing an artificial application.
---

<!-- planner-discipline-allow: Rubik -->
<!-- "Rubik" is the literal font-family string being REMOVED by every task's
     grep-count-must-be-zero acceptance gate below. It legitimately appears
     throughout this plan's <action> prose to name the old value being
     replaced (e.g. "'Rubik' -> 'Open Sans'") — never as a new value written
     into any file, so it cannot echo into the executor's own output. -->

<objective>
Replace every trace of the old CoE-DSC navy/green/Rubik visual identity — across theme.ts, globals.css, index.html, and all ~19 other frontend files carrying hardcoded hex/Rubik occurrences — with DSSC's real Blue #008ecf / Green #76b82a / Jost+Open Sans identity (D-02/D-04/D-05), and copy the two real DSSC logo PNGs into the codebase, swapping every import site (D-01).

Purpose: `theme.ts` and `globals.css` centralize brand tokens, but per 16.1-RESEARCH.md §2 that centralization does NOT cascade to the ~173 other hardcoded-hex occurrences scattered across individual components/routes as inline `style={{ color: "#06004f" }}` — every one of those files needs its own literal value substitution, or the app will show a visually inconsistent mix of old and new brand colors.
Output: Recolored theme.ts/globals.css/index.html, every listed component/route file with zero old hex/Rubik occurrences, and the two real DSSC logo PNGs live at frontend/src/assets/ under their final import-ready names, wired into all 6 existing import sites plus 2 new white-logo placements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Central tokens — theme.ts, globals.css, index.html fonts, logo asset copy</name>
  <files>frontend/src/lib/theme.ts, frontend/src/styles/globals.css, frontend/index.html, frontend/src/assets/logo-dssc-color.png, frontend/src/assets/logo-dssc-white.png</files>
  <read_first>
    frontend/src/lib/theme.ts (full file, 38 lines)
    frontend/src/styles/globals.css (full file, 19 lines)
    frontend/index.html (full file, 17 lines)
    .planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md (D-01, D-02, D-04, D-05)
  </read_first>
  <action>
    Copy the two real logo files verbatim (binary copy, do not re-encode/re-export) from
    ".planning/milestones/Design assets/DSSC_logo_4c.png" to "frontend/src/assets/logo-dssc-color.png",
    and from ".planning/milestones/Design assets/DSSC_logo_white.png" to "frontend/src/assets/logo-dssc-white.png".
    Leave frontend/src/assets/logo-coe-dsc.svg in place, untouched.

    In frontend/index.html: replace the Google Fonts stylesheet link currently reading
    `href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600&display=swap"`
    with `href="https://fonts.googleapis.com/css2?family=Jost:wght@500;600;700&family=Open+Sans:wght@300;400;600;700&display=swap"`
    (one multi-family Google Fonts URL: Jost weights 500/600/700 for headline/highlight per D-04, Open Sans
    weights 300/400/600/700 for body per D-04). Keep the two `<link rel="preconnect">` tags unchanged.
    Do NOT touch the `<title>` tag in this task — the MAMI->DSSC title rename is wording scope, handled by 16.1-02.

    In frontend/src/lib/theme.ts, apply this direct value substitution to every token (preserve every key
    name, only change color/font-family string values):
    - `colorPrimary: '#06004f'` -> `'#008ecf'`
    - `colorSuccess: '#399e5a'` -> `'#76b82a'`
    - `colorLink: '#399e5a'` -> `'#76b82a'`
    - `fontFamily: "'Rubik', -apple-system, BlinkMacSystemFont, sans-serif"` -> `"'Open Sans', -apple-system, BlinkMacSystemFont, sans-serif"`
    - `colorText: '#06004f'` -> `'#008ecf'`
    - `Button.colorPrimary: '#00006b'` -> `'#008ecf'` (collapses the old darker-navy button variant into the
      single new primary blue — D-02 names "buttons" as one of Blue's roles, no separate darker shade specified)
    - `Form.labelColor: '#06004f'` -> `'#008ecf'`
    Per D-05 (drop the colored-tint layout background entirely — the ONE exception to direct value-swap,
    not a hex substitution):
    - `colorBgLayout: 'rgba(57,158,90,0.1)'` -> `'#ffffff'`
    - `Layout.bodyBg: 'rgba(57,158,90,0.1)'` -> `'#ffffff'`
    Update the file's header comment (currently describing "dark blue #06004f, green #399e5a, button blue
    #00006b, Rubik font") to describe the new DSSC tokens instead. Do NOT rename the exported `mamiTheme`
    constant — it is an internal identifier, not user-facing, and renaming it would touch main.tsx for no
    branding benefit (out of this plan's scope, consistent with D-12's precedent that internal names are
    excluded from the rebrand).

    In frontend/src/styles/globals.css, replace line 1's
    `@import url('https://fonts.googleapis.com/css2?family=Rubik:wght@300;400;500;600;700&display=swap');`
    with `@import url('https://fonts.googleapis.com/css2?family=Jost:wght@500;600;700&family=Open+Sans:wght@300;400;600;700&display=swap');`
    (this file has its own separate `@import`, a pre-existing duplicate font-loading mechanism alongside
    index.html's `<link>` — keep both in sync rather than removing either). Update the `:root` custom
    properties (this file's own legacy token set — different hex values from theme.ts's, not caught by
    16.1-RESEARCH.md's grep pattern since that pattern only searched for #06004f/#399e5a/#00006b variants,
    but load-bearing since Sidebar.tsx and questionnaire.tsx consume these vars):
    - `--color-navy: #020059;` -> `--color-navy: #008ecf;`
    - `--color-green: #41A765;` -> `--color-green: #76b82a;`
    - `--color-blue: #3D52D5;` -> `--color-blue: #008ecf;` (zero current consumers per grep — updated for
      consistency in case a future file starts using it)
    - `--font-family: 'Rubik', sans-serif;` -> `--font-family: 'Open Sans', sans-serif;` (this is the global
      body-text default, consumed by the bare `body { font-family: var(--font-family); }` rule)
    Leave `--color-text-gray`, `--color-bg-light`, `--border-radius-sm`, `--border-radius-lg` unchanged.
  </action>
  <verify>
    <automated>cmp -s "frontend/src/assets/logo-dssc-color.png" ".planning/milestones/Design assets/DSSC_logo_4c.png" && cmp -s "frontend/src/assets/logo-dssc-white.png" ".planning/milestones/Design assets/DSSC_logo_white.png" && test $(grep -clE '#06004f|#399e5a|#00006b|Rubik' frontend/src/lib/theme.ts) -eq 0 && test $(grep -clE '#020059|#41A765|#3D52D5|Rubik' frontend/src/styles/globals.css) -eq 0 && test $(grep -c Rubik frontend/index.html) -eq 0 && cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cmp -s "frontend/src/assets/logo-dssc-color.png" ".planning/milestones/Design assets/DSSC_logo_4c.png"` exits 0
    - `cmp -s "frontend/src/assets/logo-dssc-white.png" ".planning/milestones/Design assets/DSSC_logo_white.png"` exits 0
    - `test -f frontend/src/assets/logo-coe-dsc.svg` exits 0 (not deleted)
    - `grep -clE '#06004f|#399e5a|#00006b|Rubik' frontend/src/lib/theme.ts` is 0
    - `grep -c "colorPrimary: '#008ecf'" frontend/src/lib/theme.ts` is 1 and `grep -c "colorBgLayout: '#ffffff'" frontend/src/lib/theme.ts` is 1
    - `grep -clE '#020059|#41A765|#3D52D5|Rubik' frontend/src/styles/globals.css` is 0
    - `grep -c -- "--color-navy: *#008ecf;" frontend/src/styles/globals.css` is 1
    - `grep -c "Rubik" frontend/index.html` is 0 and `grep -c "family=Jost" frontend/index.html` is 1
    - `cd frontend && npm run typecheck` passes
  </acceptance_criteria>
  <done>Both PNG logo files exist byte-identical to source under final names; theme.ts/globals.css/index.html carry only DSSC Blue/Green/Jost/Open Sans tokens with zero old-brand occurrences; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Recolor + logo-swap layout and questionnaire-wizard components</name>
  <files>frontend/src/components/layout/TopNav.tsx, frontend/src/components/layout/Footer.tsx, frontend/src/components/questionnaire/QuestionCard.tsx, frontend/src/components/questionnaire/StepPills.tsx, frontend/src/components/questionnaire/WizardPage.tsx, frontend/src/components/questionnaire/AnswerButtonGroup.tsx, frontend/src/routes/_app.tsx</files>
  <read_first>
    frontend/src/components/layout/TopNav.tsx (full file, confirmed 149 lines — 14 hex occurrences, logo import line 7 + img line 52, 5 inline Rubik font-family occurrences)
    frontend/src/components/layout/Footer.tsx (full file, confirmed 92 lines — background #06004f line 7, Rubik lines 10/69, no existing logo img)
    .planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-RESEARCH.md §2, §3
  </read_first>
  <action>
    In every file listed above, replace every occurrence of the following (case-sensitive, literal
    substring replacement, preserving each occurrence's original alpha/opacity value in the rgba forms):
    - `#06004f` -> `#008ecf`
    - `#399e5a` -> `#76b82a`
    - `#00006b` -> `#008ecf`
    - `rgba(6,0,79,` -> `rgba(0,142,207,` (RGB decomposition of #008ecf: 0,142,207)
    - `rgba(57,158,90,` -> `rgba(118,184,42,` (RGB decomposition of #76b82a: 118,184,42)
    - `'Rubik'` (inline `fontFamily` values) -> `'Jost'` when the styled element is a heading-level or
      display-emphasis element (e.g. a Drawer title, a large label meant to stand out); -> `'Open Sans'`
      for everything else (body text, nav links, buttons, form labels) — per D-04's headline/body split.
      In this file set, every current Rubik occurrence styles nav links, drawer titles, buttons, or labels
      at body/label scale (14-16px), not a display headline, so `'Open Sans'` is correct for all of them
      here unless a specific element is visually a large display heading.

    TopNav.tsx: change the logo import from `import logoSrc from '../../assets/logo-coe-dsc.svg'` to
    `import logoSrc from '../../assets/logo-dssc-color.png'` (nav bar is a light/white background — color
    logo per D-01's corrected placement, RESEARCH.md §3). Change `alt="CoE DSC"` to `alt="DSSC"` on the
    `<img>` tag. Do not touch nav label text ("Questionnaire" etc.) — that is 16.1-02's wording scope.

    Footer.tsx: this component sits on a dark navy/now-blue background with NO existing `<img>` logo element
    (RESEARCH.md §3's corrected finding — D-01 assumed one existed to swap; it doesn't). Add a new white-logo
    `<img>` to the "Brand column" div, immediately above the "CoE DSC" text line: import
    `import logoWhite from '../../assets/logo-dssc-white.png'` at the top of the file, then render
    `<img src={logoWhite} alt="DSSC" style={{ height: '32px', marginBottom: '0.75rem', display: 'block' }} />`
    before the existing brand-name div. Do not touch the "CoE DSC" text, mailto link, or copyright text in
    this task — those are wording, 16.1-02's scope.

    QuestionCard.tsx, StepPills.tsx, WizardPage.tsx, AnswerButtonGroup.tsx, _app.tsx: straight hex/Rubik
    substitution only, per the rules above — no logo import in any of these 5 files.
  </action>
  <verify>
    <automated>test -z "$(grep -rlE '#06004f|#399e5a|#00006b|rgba\(6,0,79|rgba\(57,158,90|Rubik' frontend/src/components/layout/TopNav.tsx frontend/src/components/layout/Footer.tsx frontend/src/components/questionnaire/QuestionCard.tsx frontend/src/components/questionnaire/StepPills.tsx frontend/src/components/questionnaire/WizardPage.tsx frontend/src/components/questionnaire/AnswerButtonGroup.tsx frontend/src/routes/_app.tsx)" && grep -q "logo-dssc-color" frontend/src/components/layout/TopNav.tsx && grep -q "logo-dssc-white" frontend/src/components/layout/Footer.tsx && cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE '#06004f|#399e5a|#00006b|rgba\(6,0,79|rgba\(57,158,90|Rubik' frontend/src/components/layout/TopNav.tsx frontend/src/components/layout/Footer.tsx frontend/src/components/questionnaire/QuestionCard.tsx frontend/src/components/questionnaire/StepPills.tsx frontend/src/components/questionnaire/WizardPage.tsx frontend/src/components/questionnaire/AnswerButtonGroup.tsx frontend/src/routes/_app.tsx` produces empty output (no file listed)
    - `grep -c "logo-dssc-color" frontend/src/components/layout/TopNav.tsx` is 1
    - `grep -c 'alt="DSSC"' frontend/src/components/layout/TopNav.tsx` is 1
    - `grep -c "logo-dssc-white" frontend/src/components/layout/Footer.tsx` is 1
    - `cd frontend && npm run typecheck` passes
  </acceptance_criteria>
  <done>All 7 listed files carry zero old-brand hex/Rubik occurrences; TopNav shows the color logo with "DSSC" alt text; Footer shows a new white DSSC logo image above its brand text; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Recolor + logo-swap routes — homepage, auth pages, app pages</name>
  <files>frontend/src/routes/index.tsx, frontend/src/routes/_auth/register.tsx, frontend/src/routes/_auth/login.tsx, frontend/src/routes/_auth/forgot-password.tsx, frontend/src/routes/_auth/reset-password.tsx, frontend/src/routes/_app/assessments.tsx, frontend/src/routes/_app/admin.heatmap.tsx, frontend/src/routes/_app/admin.index.tsx, frontend/src/routes/_app/report.tsx, frontend/src/routes/_app/initiative.tsx, frontend/src/routes/_app/about.tsx, frontend/src/routes/_app/dashboard.tsx</files>
  <read_first>
    frontend/src/routes/index.tsx (full file, confirmed 293 lines — logo import line 4/img line 33-37, hero section background line 71, 16 hex occurrences)
    frontend/src/routes/_auth/register.tsx (full file, confirmed 129 lines — logo import line 4/img line 48, page background line 45, 13 hex occurrences)
    .planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-RESEARCH.md §2, §3
  </read_first>
  <action>
    Apply the same substitution rules as Task 2 (`#06004f`->`#008ecf`, `#399e5a`->`#76b82a`,
    `#00006b`->`#008ecf`, `rgba(6,0,79,`->`rgba(0,142,207,`, `rgba(57,158,90,`->`rgba(118,184,42,`,
    `'Rubik'`->`'Open Sans'` for body/label/button text, `'Rubik'`->`'Jost'` for h1/h2/h3 display headings)
    to every file listed above.

    Logo import swaps (4 auth pages + homepage — 5 of these 12 files import the logo; the remaining 7
    app-route files below have no logo import, color-only):
    - index.tsx: change `import logoSrc from '../assets/logo-coe-dsc.svg'` to
      `import logoSrc from '../assets/logo-dssc-color.png'` (header `<img>` sits on the white nav bar —
      color logo, per RESEARCH.md §3's corrected placement). Change `alt="CoE DSC"` to `alt="DSSC"`.
      In the hero section (the `linear-gradient(135deg, #008ecf 0%, #008ecf 100%)` block after this task's
      color substitution — collapse the two-stop navy gradient into a single flat DSSC Blue, since D-02
      names one primary blue, not a two-tone navy gradient), add a new white-logo `<img>` immediately
      above the `<h1>`: import `logoWhite from '../assets/logo-dssc-white.png'` and render
      `<img src={logoWhite} alt="DSSC" style={{ height: '48px', marginBottom: '1.5rem' }} />` inside the
      `<div style={{ maxWidth: '800px', ... }}>` wrapper, before the `<h1>` (RESEARCH.md §3: no existing
      dark-bg logo slot here, this is a new addition per D-01's intent).
    - register.tsx, login.tsx, forgot-password.tsx, reset-password.tsx: change each file's
      `import logoSrc from "../../assets/logo-coe-dsc.svg"` to
      `import logoSrc from "../../assets/logo-dssc-color.png"` (logo sits on the white card, not the navy
      page backdrop — color logo per RESEARCH.md §3's corrected reading of D-01). Change each file's
      `alt="CoE DSC logo"` to `alt="DSSC logo"`.

    Do not touch any user-facing text (MAMI/Questionnaire/CoE-DSC wording) in any of these 12 files —
    that is 16.1-02's scope, sequenced after this plan specifically because of this file overlap.
  </action>
  <verify>
    <automated>test -z "$(grep -rlE '#06004f|#399e5a|#00006b|rgba\(6,0,79|rgba\(57,158,90|Rubik' frontend/src/routes/index.tsx frontend/src/routes/_auth/register.tsx frontend/src/routes/_auth/login.tsx frontend/src/routes/_auth/forgot-password.tsx frontend/src/routes/_auth/reset-password.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/report.tsx frontend/src/routes/_app/initiative.tsx frontend/src/routes/_app/about.tsx frontend/src/routes/_app/dashboard.tsx)" && grep -q "logo-dssc-white" frontend/src/routes/index.tsx && cd frontend && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE '#06004f|#399e5a|#00006b|rgba\(6,0,79|rgba\(57,158,90|Rubik' frontend/src/routes/index.tsx frontend/src/routes/_auth/register.tsx frontend/src/routes/_auth/login.tsx frontend/src/routes/_auth/forgot-password.tsx frontend/src/routes/_auth/reset-password.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/report.tsx frontend/src/routes/_app/initiative.tsx frontend/src/routes/_app/about.tsx frontend/src/routes/_app/dashboard.tsx` produces empty output
    - `grep -c "logo-dssc-color" frontend/src/routes/index.tsx frontend/src/routes/_auth/register.tsx frontend/src/routes/_auth/login.tsx frontend/src/routes/_auth/forgot-password.tsx frontend/src/routes/_auth/reset-password.tsx | grep -vc ':1$'` is 0 (every one of the 5 files has exactly 1 match)
    - `grep -c "logo-dssc-white" frontend/src/routes/index.tsx` is 1
    - `cd frontend && npm run typecheck && npm run build` both pass
  </acceptance_criteria>
  <done>All 12 files carry zero old-brand hex/Rubik occurrences; the 5 logo-import sites (homepage + 4 auth pages) use the new color logo with "DSSC"/"DSSC logo" alt text; the homepage hero section shows a new white logo and a flat single-tone Blue background; typecheck and build both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | This plan edits static config/token/style files and copies two local binary asset files already present in the repo checkout (`.planning/milestones/Design assets/`). No network request, no user input, no database, no auth path is touched. |

## STRIDE Threat Register

Per this phase's actual scope (ASVS L1, narrow surface — do not manufacture threats beyond what's real):

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.1-01 | Tampering | Binary asset copy (`cp` of the two logo PNGs) | low | accept | Source files are already inside this repo's own checkout, provided directly by the project owner this session — not a third-party download, no supply-chain surface. Acceptance criteria byte-compare (`cmp`) the copy against the source to catch accidental corruption/truncation. |
| T-16.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages (Google Fonts `<link>`/`@import` URL swap only, per CONTEXT.md's discretion note). No package-legitimacy gate applies. |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass after all 3 tasks land.
- `grep -rlE '#06004f|#399e5a|#00006b|#020059|#41A765|#3D52D5|Rubik' frontend/src frontend/index.html` produces empty output across the whole frontend tree.
- Manual: open both new PNGs and visually confirm they render the real DSSC logo (fast human glance during review, not machine-verifiable).
</verification>

<success_criteria>
- Zero occurrences of any old CoE-DSC hex value or "Rubik" remain anywhere under frontend/src or frontend/index.html.
- The two DSSC logo PNGs exist, byte-identical to the provided design assets, and are wired into all 6 pre-existing import sites plus 2 new white-logo placements (Footer, homepage hero).
- theme.ts/globals.css resolve to DSSC Blue #008ecf / Green #76b82a / Jost+Open Sans; the layout background is plain white.
- `npm run typecheck`/`npm run lint`/`npm run build` all pass.
</success_criteria>

<output>
Create `.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-01-SUMMARY.md` when done
</output>
