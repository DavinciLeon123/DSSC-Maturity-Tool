---
phase: 16.1-dssc-rebranding-pdf-content-registration-default
plan: 02
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  - backend/app/templates/report.html
  - backend/app/services/report_generator.py
  - backend/app/api/v1/reports.py
  - backend/app/api/v1/auth.py
  - backend/app/api/v1/admin.py
  - backend/app/main.py
  - backend/tests/api/test_reports.py
  - backend/tests/services/test_report_generator.py
  - docs/api/openapi.json
  - frontend/index.html
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/TopNav.tsx
  - frontend/src/components/layout/Footer.tsx
  - frontend/src/components/questionnaire/WizardPage.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/_auth/reset-password.tsx
  - frontend/src/routes/_app/questionnaire.tsx
  - frontend/src/routes/_app/dashboard.tsx
  - frontend/src/routes/_app/admin.index.tsx
  - frontend/src/routes/_app/admin.heatmap.tsx
  - frontend/src/routes/_app/assessments.tsx
  - frontend/src/routes/_app/about.tsx
autonomous: true
requirements: [BRAND-02, BRAND-03, RPRT-05]

must_haves:
  truths:
    - "The mailed/downloaded PDF report's header, card headers, expert-help box, and radar-chart axis labels render in DSSC Blue #008ecf (not old navy), with the white DSSC logo on the header band, per D-06"
    - "No file touched by this plan contains the strings 'MAMI', 'MaMi', 'CoE-DSC', 'CoE DSC', or 'coe-dsc.nl'"
    - "'Questionnaire' as user-facing text (nav labels, headings, buttons, loading/empty/error copy, admin UI copy) reads 'Dataspace Maturity Assessment' (or its natural lowercase/plural inflection) everywhere it appears outside of internal route/file/component/API/cache-key names, per D-12"
    - "GET/POST /initiatives/{id}/report (HTML) and GET /initiatives/{id}/report/pdf render a new 'Submitted answers' section, grouped by the 6 config categories in config order, showing each answered question's text + selected answer label + score — sourced from the real QuestionnaireAnswer rows for that assessment, never from a hardcoded stub"
    - "A category with zero answered questions renders a visible empty-state message in that section, not a crash or a silently-omitted category heading"
    - "A QuestionnaireAnswer row whose question_id does not match any question in the current config is silently excluded from the rendered answers section (never raises, never renders a blank/garbled row)"
    - "Each rendered answer row's score is color-coded via get_maturity_band(score, config['maturity_bands'])['color'] — the same sole band-classification function every other caller in report_generator.py uses (RPRT-03 invariant) — per D-08's discretion, exercised as literal maturity_bands reuse for per-answer-row coloring"
    - "The shared /initiatives/{id}/report/data JSON contract (ReportContract) is byte-for-byte unchanged by this plan — the new answers data flows only through the PDF/HTML rendering path (_render_html_for), never through build_report_contract, per D-10"
    - "docs/api/openapi.json is regenerated and committed so the docs-freshness CI gate (git diff --exit-code) stays green after main.py's title/description/contact change"
  artifacts:
    - backend/app/services/report_generator.py (new build_answers_by_category function)
    - backend/tests/services/test_report_generator.py (new tests for build_answers_by_category, including the empty-category and stale-question_id edge cases)
    - backend/tests/api/test_reports.py (new integration tests asserting the answers section renders; updated filename assertion)
  key_links:
    - "_render_html_for() (reports.py) is the sole call site wiring build_answers_by_category's output into generate_html_report — build_report_contract() must NOT be touched, or /report/data's JSON contract silently changes shape, breaking D-10 and the in-app React report page that consumes it"
    - "build_answers_by_category iterates config['categories'][*]['questions'] (not raw QuestionnaireAnswer rows) to determine what to render — this ordering choice is also what makes the stale-question_id edge case a no-op (an orphaned answer row is simply never visited) rather than a lookup KeyError"
  prohibitions:
    - "Do not modify build_report_contract()'s return shape or ReportContract's schema — the answers-by-category data is PDF/HTML-only per D-10, never added to the shared JSON contract"
    - "Do not attempt to self-host Jost/Open Sans .woff/.ttf files for WeasyPrint in this plan — per 16.1-RESEARCH.md §1, Rubik already silently fell back to a system sans-serif in the PDF (no @font-face ever existed), so the PDF font-family declaration is a text-only string change, not a real webfont-rendering fix; that is out of scope and not required by any locked decision"
    - "Do not touch backend/app/services/mami_config.py, the questionnaire.py route module, or any React Query cache key/type name (e.g. 'questionnaire-config', QuestionnaireConfig) — internal names are excluded from the rebrand per D-12"
    - "Do not touch frontend/src/lib/auth.ts's 'mami_access_token' localStorage key or frontend/src/routes/_app/report.tsx's matching localStorage.getItem call — this is an internal storage key never shown to any user; renaming it would only add churn/session-invalidation risk with zero branding benefit"
    - "Do not touch backend/app/core/config.py's DATABASE_URL default connection string ('postgresql://mami:mami@...') — infrastructure-only default, never rendered to any user"

  # Edge-probe coverage note (RPRT-05's real edge cases ARE covered above as
  # explicit truths — zero-answer category and stale question_id both have
  # concrete non-crash acceptance criteria, not flagged assumptions).
  # BRAND-02/03 edge-probe coverage (spec-less fallback — no SPEC.md exists
  # for this phase):
  # - adjacency ("do two things merge/collide/separate when equal"): N/A for
  #   the pure wording/color renames — literal string substitution across
  #   fixed files has no adjacent/comparable-elements collision case.
  # - ordering: N/A for wording renames. DOES apply to the new answers
  #   section (D-07) — explicitly covered: category order = config order,
  #   question order within category = config's questions array order (not
  #   answered_at/insertion order) — see Task 2.
  # - empty/null: DOES apply to the new answers section (D-07/RPRT-05) —
  #   explicitly covered above as a required truth, not flagged away.
---

<objective>
Recolor the PDF report template + radar-SVG font to DSSC's real identity (BRAND-03), add a new per-question submitted-answers section to the PDF/HTML report grouped by category (RPRT-05), and remove every remaining "MAMI"/"CoE-DSC"/"Questionnaire" occurrence across both the backend (email/PDF-filename/OpenAPI metadata) and the frontend's remaining user-facing text (BRAND-02).

Purpose: `report.html`/`report_generator.py` are the PDF's sole rendering path (RESEARCH.md §5/§8); the new answers section must thread through `_render_html_for()` only, preserving the shared `/report/data` JSON contract (D-10) untouched. The wording pass is bundled here — sequenced after 16.1-01 (wave 2) — specifically because it edits the SAME frontend files 16.1-01 already recolored (TopNav.tsx, index.tsx, dashboard.tsx, admin.index.tsx, WizardPage.tsx, about.tsx, reset-password.tsx, Footer.tsx), so both plans can't safely run in the same wave.
Output: Recolored report.html/report_generator.py; a new build_answers_by_category() function wired into the PDF/HTML rendering path with real test coverage including both named edge cases; zero remaining MAMI/CoE-DSC/coe-dsc.nl occurrences and "Questionnaire" renamed to "Dataspace Maturity Assessment" everywhere user-facing, across both backend and frontend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-CONTEXT.md
@.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recolor report.html + report_generator.py's radar SVG font (BRAND-03)</name>
  <files>backend/app/templates/report.html, backend/app/services/report_generator.py</files>
  <read_first>
    backend/app/templates/report.html (full file, 228 lines — confirmed `:root { --primary: #06004f; }` line 8, direct hex at lines 44/95/103/110/141, header/expert-help use var(--primary))
    backend/app/services/report_generator.py lines 176-197 (generate_radar_svg's label rendering — `font-family="Rubik, sans-serif" fill="#06004f"` at the `labels.append(...)` call)
  </read_first>
  <action>
    In backend/app/templates/report.html:
    - `--primary: #06004f;` -> `--primary: #008ecf;` (the header band, card headers, and expert-help box all
      reference `var(--primary)` — this one edit recolors all of them to DSSC Blue per D-06, keeping the
      existing header-band structure rather than restyling to white/transparent).
    - Direct hex occurrences (not routed through the var): `box-shadow: 0 2px 12px rgba(6,0,79,0.08);` ->
      `rgba(0,142,207,0.08)`; `.priority-name { color: #06004f; }` -> `#008ecf`; `.priority-score { color:
      #06004f; }` -> `#008ecf`; `.priority-band-label { color: rgba(6,0,79,0.65); }` -> `rgba(0,142,207,0.65)`;
      `.legend-item { color: rgba(6,0,79,0.6); }` -> `rgba(0,142,207,0.6)`.
    - `body { font-family: "Rubik", "Segoe UI", Arial, sans-serif; }` -> `font-family: "Jost", "Open Sans",
      "Segoe UI", Arial, sans-serif;` — a text-only declaration change (per RESEARCH.md §1, Rubik never
      actually rendered in the PDF since no `@font-face`/font file was ever wired up for WeasyPrint; this
      phase does not attempt to fix that pre-existing condition, only updates the declared family name for
      branding-intent consistency).
    - Add a white DSSC logo `<img>` to the `.report-header` block, before the existing `<h1>`: reference the
      logo the same way the frontend does (an inline `<img>` tag) — since Jinja2 templates render standalone
      HTML strings (not bundled by Vite), embed it as a base64 data URI OR a `file://`-relative path WeasyPrint
      can resolve from `TEMPLATES_DIR`. Simplest reliable approach: copy
      "frontend/src/assets/logo-dssc-white.png" to "backend/app/templates/assets/logo-dssc-white.png" (new
      directory, byte-identical copy) and reference it in the template as
      `<img src="assets/logo-dssc-white.png" alt="DSSC" style="height: 40px; margin-bottom: 12px;" />`
      (WeasyPrint resolves relative `src` paths against the template's own directory when no explicit
      `base_url` override exists elsewhere in this codebase — confirmed no `base_url` param is passed to
      `WeasyHTML(string=html_content)` anywhere in reports.py, so relative-to-CWD would break; use the
      byte-identical copy under `backend/app/templates/assets/` specifically so the relative path resolves
      regardless of process CWD, matching Jinja2's own `FileSystemLoader(str(TEMPLATES_DIR))` base).

    In backend/app/services/report_generator.py's `generate_radar_svg`, change the label's presentation
    attributes: `font-family="Rubik, sans-serif" fill="#06004f"` -> `font-family="Jost, sans-serif"
    fill="#008ecf"` (this is a separate hardcoded occurrence from report.html's CSS, per RESEARCH.md §8 —
    every `<text>` element carries explicit presentation attributes rather than relying on external CSS,
    for WeasyPrint `<text>` parity; do not remove those explicit attributes, only change their values).
  </action>
  <verify>
    <automated>test $(grep -clE '#06004f|rgba\(6,0,79|Rubik' backend/app/templates/report.html) -eq 0 && grep -q -- "--primary: *#008ecf;" backend/app/templates/report.html && cmp -s "backend/app/templates/assets/logo-dssc-white.png" "frontend/src/assets/logo-dssc-white.png" && grep -q 'font-family="Jost, sans-serif" fill="#008ecf"' backend/app/services/report_generator.py && cd backend && uv run pytest tests/services/test_report_generator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -clE '#06004f|rgba\(6,0,79|Rubik' backend/app/templates/report.html` is 0
    - `grep -c -- "--primary: *#008ecf;" backend/app/templates/report.html` is 1
    - `grep -c 'logo-dssc-white' backend/app/templates/report.html` is 1
    - `cmp -s "backend/app/templates/assets/logo-dssc-white.png" "frontend/src/assets/logo-dssc-white.png"` exits 0
    - `grep -c 'font-family="Jost, sans-serif" fill="#008ecf"' backend/app/services/report_generator.py` is 1
    - `cd backend && uv run pytest tests/services/test_report_generator.py -q` passes (existing CSS/structure regression tests still find their expected rules — none of them assert on the old hex, so they remain green)
  </acceptance_criteria>
  <done>report.html and the radar SVG both render in DSSC Blue #008ecf with the white logo on the header band; all existing report_generator test-suite assertions still pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New submitted-answers-by-category PDF section (RPRT-05)</name>
  <files>backend/app/services/report_generator.py, backend/app/api/v1/reports.py, backend/app/templates/report.html, backend/tests/services/test_report_generator.py, backend/tests/api/test_reports.py</files>
  <read_first>
    backend/app/services/report_generator.py (full file — build_report_contract at line 223, generate_html_report at line 32, get_maturity_band at line 67)
    backend/app/api/v1/reports.py lines 88-103 (_render_html_for) and lines 180-213 (the two /report/data endpoints — confirm these are NOT touched)
    backend/app/models/questionnaire.py (QuestionnaireAnswer: id, assessment_id, question_id, category_id, score, answered_at)
    backend/tests/api/test_reports.py lines 1-95 (_answer_all_questions helper with its `skip=` param, _fully_answered_submitted_initiative, _submit — reuse these fixtures)
    backend/tests/factories.py (make_answer, make_assessment, make_initiative, make_user signatures)
  </read_first>
  <behavior>
    - build_answers_by_category(session, assessment_id, config) returns a list with one dict per
      config["categories"] entry, in config order, each shaped
      {"category_id": str, "name": str, "answers": [{"question_id", "text", "answer_label", "score",
      "band_color"}, ...]} — "answers" ordered by that category's config["questions"] array order (never
      answered_at/DB-insertion order), per D-07.
    - A category with zero matching QuestionnaireAnswer rows still appears in the returned list, with
      "answers": [] (not omitted) — RPRT-05's empty-state edge case.
    - A QuestionnaireAnswer row whose question_id is not found in ANY config category's questions is never
      visited (the function iterates config's questions, looking up answers by question_id — never the
      reverse) — RPRT-05's "question_id doesn't match current config" edge case resolves to silent
      exclusion, not a KeyError.
    - Each answer's "answer_label" is resolved by matching the stored integer score against
      question["options"] where option["score"] == score (D-08's "question text + selected answer label +
      score" row shape); "band_color" is get_maturity_band(score, config["maturity_bands"])["color"] — D-08's
      discretion exercised as literal maturity_bands reuse for row-coloring, via the same single
      band-classification function every other caller
      in this module uses (RPRT-03's "sole band-classification function" invariant), applied per-answer
      rather than per-dimension-aggregate.
  </behavior>
  <action>
    RED: in backend/tests/services/test_report_generator.py, add (using the real
    `load_dssc_questionnaire_config()` fixture already used throughout this file, plus a DB `session`
    fixture and `tests.factories.make_user/make_initiative/make_assessment/make_answer`):
    - test_build_answers_by_category_groups_in_config_order — fully answer all 52 questions across all 6
      categories (loop `config["categories"]` -> `question["id"]`, calling make_answer per question, mirroring
      test_reports.py's `_answer_all_questions` pattern); assert the returned list has exactly 6 entries in
      the same category_id order as config["categories"], and the total answer count across all entries is 52.
    - test_build_answers_by_category_answer_row_shape_and_label_lookup — answer one specific question with a
      known score (e.g. score=4); assert that question's row has `answer_label` equal to the config option
      whose `score == 4` for that exact question, and carries `question_id`/`text`/`score`/`band_color` keys.
    - test_build_answers_by_category_empty_category_present_with_empty_list — answer every question in every
      category EXCEPT skip an entire category's questions (e.g. loop but `continue` whenever
      `question["category_id"] == "cat-3"`); assert the function does not raise, the "cat-3" entry is still
      present in the returned list, and its "answers" list is exactly `[]`.
    - test_build_answers_by_category_ignores_stale_question_id — after normally answering all questions,
      insert one additional QuestionnaireAnswer row directly via make_answer with `question_id="q-stale-999"`
      (not present in any config category); assert the function does not raise, and no returned category's
      "answers" list contains an entry with `question_id == "q-stale-999"`.
    Run these 4 tests and confirm they fail (build_answers_by_category does not exist yet).

    GREEN: implement in backend/app/services/report_generator.py. Add imports `from sqlmodel import Session,
    select` and `from app.models.questionnaire import QuestionnaireAnswer` at the top of the file. Add:
    a function `build_answers_by_category(session: Session, assessment_id: int, config: dict) -> list[dict]`
    that queries `select(QuestionnaireAnswer).where(QuestionnaireAnswer.assessment_id == assessment_id)`,
    builds a `{question_id: row}` lookup dict from the results, then iterates `config["categories"]` in
    order; for each category, iterates its `questions` in order, looks up each question's id in the answer
    lookup dict, and — only when a matching answer row exists — appends a dict with question_id/text (from
    the config question)/answer_label (looked up from `question["options"]` by matching `score`, falling
    back to `f"Score {row.score}"` if no option matches, a schema-drift safety net, not the primary path)/
    score (from the row)/band_color (via `get_maturity_band(row.score, config["maturity_bands"])["color"]`).
    Appends one dict per category to the result list regardless of whether that category's answers list
    ended up empty. Then update `generate_html_report`'s signature to accept a new required keyword-only
    param `answers_by_category: list[dict]` and add it to the Jinja2 `context` dict passed to
    `template.render(**context)`.

    Wire it into backend/app/api/v1/reports.py's `_render_html_for`: after computing `scores` and `contract`,
    add `answers_by_category = build_answers_by_category(session, assessment.id, config)` and pass
    `answers_by_category=answers_by_category` as an additional keyword arg to the existing
    `generate_html_report(...)` call. Do NOT modify `build_report_contract()` or either `/report/data`
    endpoint — this keeps the shared JSON contract exactly as-is, per D-10.

    Add the new section to backend/app/templates/report.html, placed after the existing "PRIORITY AREAS"
    `.card` block and before the "GET EXPERT HELP" `.expert-help` block (D-09): a new `.card` with
    `.card-header` text "Submitted answers", containing — per category — a category-name subheading
    followed by either a flat single-level-flex list of answer rows (one `.answers-row` per question,
    following this file's existing single-level-flex precedent from `.priority-row`/`.legend`, RESEARCH.md
    §8's WeasyPrint-bug-avoidance pattern — do not nest flex containers) each showing the question text, the
    answer label, and the score (colored via the row's `band_color`, mirroring `.band-dot`'s existing
    pattern), OR — when that category's `answers` list is empty — a plain empty-state paragraph reading "No
    answers recorded for this category."
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/services/test_report_generator.py tests/api/test_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run pytest tests/services/test_report_generator.py -k build_answers_by_category -v` — all 4 new tests pass
    - `grep -c "def build_answers_by_category" backend/app/services/report_generator.py` is 1
    - `grep -c "answers_by_category" backend/app/api/v1/reports.py` is at least 2 (the assignment + the generate_html_report kwarg)
    - `grep -c "build_report_contract\|build_priority_list\|generate_radar_svg" backend/app/api/v1/reports.py` output is unchanged from before this task (build_report_contract call sites in the two /report/data endpoints are untouched — confirm via `git diff backend/app/api/v1/reports.py` shows no changes inside `generate_report_data_endpoint`/`get_report_data_endpoint`)
    - `grep -c "No answers recorded for this category" backend/app/templates/report.html` is 1
    - New test `test_get_report_html_includes_submitted_answers_section` in backend/tests/api/test_reports.py (using `_fully_answered_submitted_initiative`) asserts the GET /report HTML response contains at least one answered question's text and its answer_label
    - New test `test_get_report_html_answers_section_empty_category_shows_placeholder` in backend/tests/api/test_reports.py builds a submitted assessment with one whole category left unanswered and asserts the response is 200 (not 500) and contains "No answers recorded for this category."
    - `cd backend && uv run pytest tests/api/test_reports.py tests/services/test_report_generator.py -q` passes in full
  </acceptance_criteria>
  <done>build_answers_by_category exists, is unit-tested for the grouping/shape/empty-category/stale-question_id behaviors, is wired only into the PDF/HTML rendering path (never build_report_contract), and the rendered report.html shows a new "Submitted answers" section with a real non-crashing empty-state for an unanswered category.</done>
</task>

<task type="auto">
  <name>Task 3: Remove MAMI/CoE-DSC wording + rename "Questionnaire" everywhere (backend + frontend) (BRAND-02)</name>
  <files>backend/app/api/v1/reports.py, backend/app/api/v1/auth.py, backend/app/api/v1/admin.py, backend/app/main.py, backend/tests/api/test_reports.py, docs/api/openapi.json, frontend/index.html, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/TopNav.tsx, frontend/src/components/layout/Footer.tsx, frontend/src/components/questionnaire/WizardPage.tsx, frontend/src/routes/index.tsx, frontend/src/routes/_auth/reset-password.tsx, frontend/src/routes/_app/questionnaire.tsx, frontend/src/routes/_app/dashboard.tsx, frontend/src/routes/_app/admin.index.tsx, frontend/src/routes/_app/admin.heatmap.tsx, frontend/src/routes/_app/assessments.tsx, frontend/src/routes/_app/about.tsx</files>
  <read_first>
    .planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-RESEARCH.md §4 (full wording inventory table with file:line detail)
  </read_first>
  <action>
    Canonical product name for this phase (Claude's discretion per D-11): "DSSC Maturity Scan" — used
    consistently everywhere a standalone product name is needed (browser title, sidebar brand, email
    sender/team sign-off, OpenAPI title). Canonical assessment-feature name (D-12, locked): "Dataspace
    Maturity Assessment" (Title Case when it replaces a heading/nav-label/button; "dataspace maturity
    assessment" lowercase when replacing a mid-sentence lowercase "questionnaire"). Canonical contact
    domain (D-13, locked): dssc.eu / info@dssc.eu.

    Backend — apply these exact replacements:
    - backend/app/api/v1/reports.py: filename string `"MAMI-Interoperability-Report.pdf"` -> `"DSSC-Maturity-Report.pdf"` (both occurrences — inside `_send_report_email` and inside `download_report_pdf`'s Content-Disposition header). `"from": "MaMi Checker <onboarding@resend.dev>"` -> `"from": "DSSC Maturity Scan <onboarding@resend.dev>"` (keep the resend.dev sandbox domain — a transactional-sending domain unrelated to branding, changing it needs real DNS verification, out of this phase's scope). `"subject": "Your MAMI Interoperability Heatmap"` -> `"subject": "Your DSSC Maturity Report"`. Email body text: replace "MAMI Interoperability Assessment" -> "DSSC Dataspace Maturity Assessment", "personalised Interoperability Heatmap report" -> "personalised DSSC Maturity Report", "Centre of Excellence for Data Sharing and Cloud (CoE-DSC)" -> "Data Spaces Support Centre (DSSC)" (both mentions), "CoE-DSC website" -> "DSSC website", "The MAMI Checker team" -> "The DSSC Maturity Scan team".
    - backend/app/api/v1/auth.py: `"from": "MaMi Checker <onboarding@resend.dev>"` -> `"from": "DSSC Maturity Scan <onboarding@resend.dev>"`; `"subject": "Reset your MaMi Checker password"` -> `"subject": "Reset your DSSC Maturity Scan password"`; "your MaMi Checker account" -> "your DSSC Maturity Scan account"; "The MaMi Checker team" -> "The DSSC Maturity Scan team".
    - backend/app/api/v1/admin.py: `filename=mami-dataset.csv` -> `filename=dssc-dataset.csv` (Content-Disposition header — this occurrence was missed by 16.1-RESEARCH.md's case-sensitive grep for "MAMI"/"MaMi", since it's lowercase "mami"; confirmed no existing test asserts on this filename string).
    - backend/app/main.py: `title="MAMI Checker API"` -> `title="DSSC Maturity Scan API"`; `description="API for the MAMI Framework DSI Assessment Tool — CoE-DSC / TNO"` -> `description="API for the DSSC Maturity Scan — Data Spaces Support Centre (DSSC)"`; `contact={"name": "CoE-DSC", "url": "https://coe-dsc.nl"}` -> `contact={"name": "DSSC", "url": "https://dssc.eu"}`.
    - backend/tests/api/test_reports.py: update the filename assertion at (around) line 123 —
      `assert sent_params["attachments"][0]["filename"] == "MAMI-Interoperability-Report.pdf"` ->
      `assert sent_params["attachments"][0]["filename"] == "DSSC-Maturity-Report.pdf"`.
    - Regenerate the OpenAPI schema so the docs-freshness CI gate stays green: run
      `cd backend && uv run python scripts/export_openapi.py` and commit the resulting
      `docs/api/openapi.json` diff (title/description/contact fields only should change).

    Frontend — apply these exact replacements (file:old-text -> new-text):
    - frontend/index.html: `<title>MAMI Compliance Questionnaire</title>` -> `<title>DSSC Maturity Scan</title>`.
    - frontend/src/components/layout/Sidebar.tsx: nav item `{ label: "Questionnaire", to: "/questionnaire" }` -> `{ label: "Dataspace Maturity Assessment", to: "/questionnaire" }` (route path string unchanged, D-12 excludes internal route names); "CoE-DSC" caption text -> "DSSC"; "MAMI Checker" app-name text -> "DSSC Maturity Scan".
    - frontend/src/components/layout/TopNav.tsx: nav item `{ label: 'Questionnaire', to: '/questionnaire' }` -> `{ label: 'Dataspace Maturity Assessment', to: '/questionnaire' }`.
    - frontend/src/components/layout/Footer.tsx: "CoE DSC" brand text -> "DSSC"; "Centre of Excellence for Data Sharing &amp; Cloud" -> "Data Spaces Support Centre"; `mailto:info@coe-dsc.nl` -> `mailto:info@dssc.eu`; "CoE DSC. All rights reserved." -> "DSSC. All rights reserved.".
    - frontend/src/components/questionnaire/WizardPage.tsx: "Thank you for completing the MAMI Questionnaire. You can now view your MAMI Interoperability heatmap." -> "Thank you for completing the Dataspace Maturity Assessment. You can now view your DSSC Maturity Report.".
    - frontend/src/routes/index.tsx: `alt="CoE DSC"` was already changed to `alt="DSSC"` by 16.1-01 — do not touch the `<img>` tag again. "MAMI - Minimal Agreements for Maximum Interoperability" (hero h1) -> "DSSC Maturity Scan for Dataspaces"; "Work through the structured MAMI questionnaire with Yes / Not yet / Not applicable answers per topic." -> "Work through the structured Dataspace Maturity Assessment with a 1-5 maturity score per question."  (the old Yes/Not yet/Not applicable 3-way answer model was replaced by the current 1-5 scoring scale in Phase 13/14 — this sentence is corrected alongside the rename it shares, not new scope); "The MAMI questionnaire gives you an immediate overview of your current level of interoperability." -> "The Dataspace Maturity Assessment gives you an immediate overview of your current level of dataspace maturity."; "Across four key domains (Scheme, Participants, Data and Services), you will assess whether you already comply, plan to comply or it's not applicable. Your answers are visualised in a clear interoperability heatmap." -> "Across six categories (Governance, Business, Legal, Interoperability, Control over Data & Trust, and Value Creation), you will rate your current maturity level. Your answers are visualised in a clear maturity radar." (corrects a pre-existing stale description of the removed 4-domain/3-way-answer model in the same paragraph being renamed — the real current structure per config/dssc-questionnaire.json).
    - frontend/src/routes/_auth/reset-password.tsx: "CoE-DSC / TNO" caption -> "DSSC" (the `alt="CoE DSC logo"` on this same file was already changed to `alt="DSSC logo"` by 16.1-01 — do not touch that attribute again).
    - frontend/src/routes/_app/questionnaire.tsx: heading "MAMI Questionnaire" -> "Dataspace Maturity Assessment"; "Loading questionnaire..." -> "Loading assessment..."; "Please create your initiative first before filling in the questionnaire." -> "Please create your initiative first before filling in the assessment."; "Failed to load questionnaire. Please try again." -> "Failed to load assessment. Please try again.".
    - frontend/src/routes/_app/dashboard.tsx: "Failed to generate report. Make sure you have answered the questionnaire." -> "...answered the dataspace maturity assessment."; button text `{initiative.status === "submitted" ? "Retake Questionnaire" : "Start Questionnaire"}` -> `{initiative.status === "submitted" ? "Retake Dataspace Maturity Assessment" : "Start Dataspace Maturity Assessment"}`.
    - frontend/src/routes/_app/admin.index.tsx: `a.download = "mami-dataset.csv";` -> `a.download = "dssc-dataset.csv";` (found beyond 16.1-RESEARCH.md's inventory — same lowercase-miss as admin.py above); "their questionnaire data" -> "their dataspace maturity assessment data"; Popconfirm `title="Delete questionnaire?"` -> `title="Delete Dataspace Maturity Assessment?"`; tab `label: \`Questionnaires (${initiatives.length})\`` -> `label: \`Dataspace Maturity Assessments (${initiatives.length})\``; "questionnaire answers) as a CSV file." -> "dataspace maturity assessment answers) as a CSV file.".
    - frontend/src/routes/_app/admin.heatmap.tsx: "Once an initiative fully completes and submits the questionnaire, its scores will appear here." -> "...submits the dataspace maturity assessment, its scores will appear here.".
    - frontend/src/routes/_app/assessments.tsx: "Finish your first questionnaire to see it appear here as your first assessment version." -> "Finish your first dataspace maturity assessment to see it appear here as your first version.".
    - frontend/src/routes/_app/about.tsx: "About MAMI" -> "About DSSC"; "MAMI (Minimal Agreements for Maximal Interoperability)" -> "DSSC (Data Spaces Support Centre)"; `href="https://coe-dsc.nl"` -> `href="https://dssc.eu"`; "CoE-DSC" link text -> "DSSC"; "The framework organizes requirements across a 4×3 matrix covering governance, technical, legal, and operational dimensions" -> "The framework organizes requirements across 6 categories — Governance, Business, Legal, Interoperability, Control over Data & Trust, and Value Creation" (corrects the same stale 4×3-matrix description in the paragraph whose framework name is being renamed); "structured questionnaire" -> "structured Dataspace Maturity Assessment".

    Explicitly excluded from this task (internal-only per D-12, confirmed via case-insensitive grep during
    planning — leave untouched): frontend/src/lib/auth.ts's `"mami_access_token"` localStorage key and
    frontend/src/routes/_app/report.tsx's matching `localStorage.getItem("mami_access_token")` read;
    frontend/src/lib/theme.ts's `mamiTheme` export and its main.tsx import site; backend/app/core/config.py's
    `DATABASE_URL` default; any `mami_code` field name in FindingsPanel.tsx/scoring.ts (dead code from the
    removed ZEN engine, Phase 14 — unrelated to this rebrand); the `{/* MAMI section */}` JSX comment in
    index.tsx (code comment only, not rendered).
  </action>
  <verify>
    <automated>test -z "$(grep -rlE 'MAMI|MaMi|CoE-DSC|CoE DSC|coe-dsc\.nl' backend/app/api/v1/reports.py backend/app/api/v1/auth.py backend/app/main.py frontend/index.html frontend/src/components/layout/Sidebar.tsx frontend/src/components/layout/TopNav.tsx frontend/src/components/layout/Footer.tsx frontend/src/components/questionnaire/WizardPage.tsx frontend/src/routes/index.tsx frontend/src/routes/_auth/reset-password.tsx frontend/src/routes/_app/questionnaire.tsx frontend/src/routes/_app/dashboard.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/about.tsx)" && cd backend && uv run pytest tests/api/test_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE "MAMI|MaMi|CoE-DSC|CoE DSC|coe-dsc\.nl" backend/app/api/v1/reports.py backend/app/api/v1/auth.py backend/app/main.py frontend/index.html frontend/src/components/layout/Sidebar.tsx frontend/src/components/layout/TopNav.tsx frontend/src/components/layout/Footer.tsx frontend/src/components/questionnaire/WizardPage.tsx frontend/src/routes/index.tsx frontend/src/routes/_auth/reset-password.tsx frontend/src/routes/_app/questionnaire.tsx frontend/src/routes/_app/dashboard.tsx frontend/src/routes/_app/admin.index.tsx frontend/src/routes/_app/admin.heatmap.tsx frontend/src/routes/_app/assessments.tsx frontend/src/routes/_app/about.tsx` produces empty output
    - `grep -ci "mami-dataset" backend/app/api/v1/admin.py frontend/src/routes/_app/admin.index.tsx` both 0
    - `grep -c "dssc-dataset.csv" backend/app/api/v1/admin.py frontend/src/routes/_app/admin.index.tsx` both 1
    - `grep -c "DSSC-Maturity-Report.pdf" backend/app/api/v1/reports.py backend/tests/api/test_reports.py` both at least 1
    - `grep -c "Dataspace Maturity Assessment" frontend/src/components/layout/Sidebar.tsx frontend/src/components/layout/TopNav.tsx frontend/src/routes/_app/questionnaire.tsx frontend/src/routes/_app/dashboard.tsx frontend/src/routes/_app/admin.index.tsx` each at least 1
    - After running `cd backend && uv run python scripts/export_openapi.py`, the regenerated `docs/api/openapi.json` must be saved/staged as part of this task's changes — a subsequent CI run's `git diff --exit-code -- docs/api/openapi.json` (docs-freshness gate) must exit 0 against what this task commits
    - `cd backend && uv run pytest tests/api/test_reports.py -q` passes
    - `cd frontend && npm run typecheck && npm run build` both pass
  </acceptance_criteria>
  <done>Zero MAMI/CoE-DSC/coe-dsc.nl occurrences remain in any file this task touches; "Questionnaire" reads "Dataspace Maturity Assessment" (or its natural inflection) everywhere user-facing across backend emails/OpenAPI metadata and the frontend; the regenerated OpenAPI schema is committed so CI's docs-freshness gate stays green; all backend/frontend checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Requesting user -> new answers-by-category query | The new `build_answers_by_category(session, assessment_id, config)` reads `QuestionnaireAnswer` rows scoped by `assessment_id`. `assessment_id` itself is only ever resolved via `resolve_report_assessment(session, initiative_id, assessment_id)` after `_get_authorized_initiative` has already 404'd any initiative the current user doesn't own (or isn't ADMIN for) — this plan adds no new route, no new query param accepted from the client beyond what `_render_html_for`'s existing callers already validate, so the new function inherits the existing ownership check rather than needing its own. |
| Config-driven question text -> Jinja2 template | `question["text"]`/`option["label"]` come from `config/dssc-questionnaire.json`, a server-controlled file, never end-user input — same trust level as `generate_radar_svg`'s category names (RESEARCH.md §5/§8), rendered through Jinja2's `select_autoescape(["html"])` (auto-escaping is already on for this template, per `_get_jinja_env()`), so no new unescaped-HTML surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16.1-02 | Information Disclosure | `build_answers_by_category(session, assessment_id, config)` | high | mitigate | Confirmed: this function is called only from `_render_html_for`, which every route handler calls only AFTER `_get_authorized_initiative` has resolved the initiative for the current user (owner or ADMIN) and `resolve_report_assessment` has scoped the assessment to that same initiative — an attacker cannot reach this function with an `assessment_id` belonging to another user's initiative without first passing the existing ownership check. No new route/query-param surface is added. Verified by the Task 2 acceptance criteria's existing-test-suite-passes gate (ownership tests already exist for every /report endpoint in test_reports.py and are unmodified by this plan). |
| T-16.1-03 | Tampering | Jinja2 template interpolation of config-sourced question/answer text into report.html | low | accept | `select_autoescape(["html"])` is already enabled for this template (pre-existing, `_get_jinja_env()`); question text and answer labels come from the server-controlled `config/dssc-questionnaire.json`, never end-user free text — same trust level already accepted for `dimension_scores`/`priority_list` rendering in this same template. |
| T-16.1-04 | Tampering | Local asset copy of `logo-dssc-white.png` into `backend/app/templates/assets/` | low | accept | Byte-identical copy of a file already introduced and verified in 16.1-01 (itself sourced from the repo owner's own checkout) — no new external download. |
| T-16.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero new packages. |
</threat_model>

<verification>
- `cd backend && uv run ruff check . && uv run ruff format --check . && uv run mypy app --ignore-missing-imports && uv run pytest tests/ -n auto -m "not perf and not benchmark" -q` passes in full (project's standard local quality gate, per CLAUDE.md).
- `cd frontend && npm run typecheck && npm run lint && npm run build` all pass.
- Manual: render `GET /api/v1/initiatives/{id}/report/pdf` for a fully-answered submitted initiative and visually confirm DSSC Blue branding + the new "Submitted answers" section appears after the priority list (fast human glance, not machine-verifiable — matches this phase's existing WeasyPrint-visual-confirmation precedent from 16-05).
</verification>

<success_criteria>
- report.html and the radar SVG render in DSSC Blue #008ecf with the white logo on the header band.
- A new "Submitted answers" section renders per-question answers grouped by category, in config order, with a working empty-state for a fully-unanswered category and silent (non-crashing) exclusion of any stale question_id — covered by new automated tests.
- The shared `/report/data` JSON contract is byte-for-byte unchanged.
- Zero MAMI/CoE-DSC/coe-dsc.nl occurrences remain anywhere this plan touches; "Questionnaire" reads "Dataspace Maturity Assessment" everywhere user-facing.
- `docs/api/openapi.json` is regenerated and committed; docs-freshness CI gate stays green.
</success_criteria>

<output>
Create `.planning/phases/16.1-dssc-rebranding-pdf-content-registration-default/16.1-02-SUMMARY.md` when done
</output>
