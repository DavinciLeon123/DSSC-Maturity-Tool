---
phase: 09-ux-improvements-initiative-registration-on-dashboard-responsive-design-and-expanded-report-heatmap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/initiative.py
  - backend/app/schemas/initiative.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Creating an initiative with only name and sector (omitting contact_name, contact_email, organization) returns 201 with no validation error"
    - "Existing initiatives with all fields populated continue to work without errors"
  artifacts:
    - path: "backend/app/models/initiative.py"
      provides: "Initiative SQLModel with contact_name, contact_email, organization as Optional[str] = None"
      contains: "Optional[str] = None"
    - path: "backend/app/schemas/initiative.py"
      provides: "InitiativeCreate schema with contact_name, contact_email, organization as Optional"
      contains: "Optional"
  key_links:
    - from: "backend/app/schemas/initiative.py"
      to: "backend/app/models/initiative.py"
      via: "InitiativeCreate used by POST /initiatives endpoint to construct Initiative model"
      pattern: "InitiativeCreate"
---

<objective>
Loosen the InitiativeCreate backend schema so that contact_name, contact_email, and organization are no longer required fields. This unblocks the Dashboard inline registration form (Plan 09-02) which only shows name and sector.

Purpose: The dashboard inline form only collects name + sector. Without this backend change, any POST /initiatives call omitting the three formerly-required fields will fail validation with a 422 error.
Output: Updated Initiative model and InitiativeCreate schema where contact_name, contact_email, and organization are Optional[str] = None (Optional[EmailStr] = None for contact_email). No migration needed — DB columns are already nullable in PostgreSQL (SQLModel Optional columns map to nullable).
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/models/initiative.py
@backend/app/schemas/initiative.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make contact_name, contact_email, organization optional in model and schema</name>
  <files>backend/app/models/initiative.py, backend/app/schemas/initiative.py</files>
  <action>
    In `backend/app/models/initiative.py`:
    - Change the `contact_name` field from `str` to `Optional[str] = None`
    - Change the `contact_email` field from `str` to `Optional[str] = None`
    - Change the `organization` field from `str` to `Optional[str] = None`
    - Ensure `from typing import Optional` is present at top of file
    - Do NOT change `name` or `sector` — those remain required (non-optional)
    - Do NOT add an Alembic migration — PostgreSQL columns are already nullable (SQLModel maps Optional[str] to nullable columns in the existing schema). Verify by checking the existing column definitions; if they were previously `str` without Optional, the DB already has the columns but as VARCHAR NOT NULL. Check the actual DB situation: if migration IS needed, create one in `backend/alembic/versions/` following the existing pattern — but only if columns are currently NOT NULL in DB.

    In `backend/app/schemas/initiative.py`:
    - In the `InitiativeCreate` class, change:
      - `contact_name: str` → `contact_name: Optional[str] = None`
      - `contact_email: EmailStr` → `contact_email: Optional[EmailStr] = None`
      - `organization: str` → `organization: Optional[str] = None`
    - Ensure `Optional` is imported from `typing` (or use `str | None` syntax if already using Python 3.10+ union style)
    - Do NOT change `name` or `sector` — they remain required
    - Do NOT remove the SECTOR_OPTIONS validator — sector still validates against allowed values

    PITFALL to avoid: Make BOTH the Pydantic schema AND the SQLModel model optional. If only the schema is changed, the model will raise a NOT NULL DB constraint violation when SQLModel tries to insert None values.
  </action>
  <verify>
    <automated>cd "/c/Users/djlia/Desktop/MaMi Checker" && docker compose exec api python -c "
from app.schemas.initiative import InitiativeCreate
from pydantic import ValidationError
# Must succeed with only name + sector
try:
    obj = InitiativeCreate(name='Test Initiative', sector='Government')
    assert obj.contact_name is None
    assert obj.contact_email is None
    assert obj.organization is None
    print('PASS: Optional fields accepted as None')
except ValidationError as e:
    print('FAIL:', e)
"
    </automated>
  </verify>
  <done>
    POST /initiatives with body {"name": "Test", "sector": "Government"} (omitting contact_name, contact_email, organization) returns 201. POST with all fields populated still returns 201. Pydantic validation does not raise on None values for those three fields.
  </done>
</task>

</tasks>

<verification>
Run the verify command above inside the running Docker container. Additionally, test via the API directly:
```
curl -X POST http://localhost:8000/api/v1/initiatives \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Dashboard Init", "sector": "Government"}'
```
Expected: 201 with initiative object where contact_name, contact_email, organization are null.
</verification>

<success_criteria>
Initiative can be created with only name + sector. The three formerly-required fields (contact_name, contact_email, organization) accept None without raising validation or DB errors.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ux-improvements-initiative-registration-on-dashboard-responsive-design-and-expanded-report-heatmap/09-01-SUMMARY.md`
</output>
