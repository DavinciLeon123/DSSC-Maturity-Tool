---
phase: 09-ux-improvements-initiative-registration-on-dashboard-responsive-design-and-expanded-report-heatmap
plan: "06"
type: execute
wave: 3
depends_on:
  - "09-02"
  - "09-03"
  - "09-05"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 6 UX improvements from Phase 9 are visually confirmed working"
    - "No regressions in existing functionality (login, questionnaire save/resume, report generation)"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint to confirm all 6 UX improvements from Phase 9 are working correctly in the browser. This plan has no automated tasks — it verifies the complete Phase 9 output.

Purpose: Visual and functional confirmation that all plans (09-01 through 09-05) delivered their intended user-facing changes before the phase is marked complete.
Output: Phase 9 marked complete or issues documented for gap closure.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 6 Phase 9 UX improvements:
    1. (09-01 + 09-02) Inline initiative registration on Dashboard — only Name + Sector required
    2. (09-02) "Role: USER/ADMIN" text removed from Dashboard
    3. (09-02) Dashboard shows initiative details + "Start Questionnaire" / "Retake Questionnaire" CTA
    4. (09-03) Post-questionnaire completion screen: updated text + "Generate heatmap" button → navigates to /report
    5. (09-04) Expanded heatmap: 4 category group headers + 9 topic rows with status chips
    6. (09-05) Mobile responsive: questionnaire wizard + /report page work at 375px viewport
  </what-built>
  <how-to-verify>
    Start the dev environment: `docker compose up` (backend) + `npm run dev` (frontend).

    **Test 1 — Inline initiative registration (items 1, 2, 3):**
    a. Register a fresh test user account (or clear an existing user's initiative via the admin panel).
    b. Log in and navigate to /dashboard.
    c. Confirm: "Role: USER" text is NOT visible anywhere on the page.
    d. Confirm: An inline registration form appears on the Dashboard with "Initiative Name" and "Sector" fields.
    e. Fill in only Name + Sector (leave all other fields empty) and submit.
    f. Confirm: Initiative is created successfully (no 422 error).
    g. Confirm: Dashboard now shows initiative details (name, sector) and a "Start Questionnaire" button.

    **Test 2 — Retake Questionnaire CTA:**
    a. Complete and submit the questionnaire for the test user.
    b. Navigate to /dashboard.
    c. Confirm: Button label is "Retake Questionnaire" (not "Start Questionnaire").

    **Test 3 — Post-questionnaire completion screen (item 4):**
    a. Start a new questionnaire session (or retake).
    b. Navigate through and submit the questionnaire.
    c. On the completion screen, confirm:
       - Body text reads exactly: "Thank you for completing the MAMI Questionnaire. You can now view your MAMI Interoperability heatmap."
       - Button is labeled "Generate heatmap"
    d. Click "Generate heatmap".
    e. Confirm: Browser navigates to /report (not /dashboard).

    **Test 4 — Expanded heatmap (item 5):**
    a. Navigate to /report.
    b. Count heatmap rows: should see 4 category headers + 9 topic rows = 13 rows total.
    c. Confirm category headers (Scheme, Participants, Data, Services) have navy background and no chips.
    d. Confirm topic rows show topic labels (e.g. "Scheme publication & updates", "On(off)-boarding") with 3 chips each.

    **Test 5 — Mobile responsive (item 6):**
    a. Open DevTools → set viewport to 375px width.
    b. Navigate to /questionnaire:
       - StepPills panel should NOT be visible.
       - A compact progress text should appear (e.g. "Scheme · Topic 1 of 3").
       - Question card should use full viewport width with no horizontal scroll.
    c. Navigate to /report:
       - Heatmap should display without horizontal overflow/scrollbar.
       - All topic rows and chips should be readable.
    d. Set viewport back to 1024px — confirm both pages show standard desktop layouts.

    **Regression check:**
    a. Navigate to /questionnaire normally — confirm answers save and wizard navigation works.
    b. Navigate to /admin (if admin account) — confirm admin panel is unaffected.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 items pass, or describe specific issues found for gap closure</resume-signal>
</task>

</tasks>

<verification>
Human verification as described above.
</verification>

<success_criteria>
All 6 UX improvements from Phase 9 confirmed working in the browser. No regressions in questionnaire, dashboard, or report functionality.
</success_criteria>

<output>
After verification passes, create `.planning/phases/09-ux-improvements-initiative-registration-on-dashboard-responsive-design-and-expanded-report-heatmap/09-06-SUMMARY.md`
</output>
