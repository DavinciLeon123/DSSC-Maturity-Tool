---
wave: 4
depends_on:
  - 07-01
  - 07-02
files_modified:
  - frontend/src/routes/_app/dashboard.tsx
  - frontend/src/routes/_app/initiative.tsx
  - frontend/src/routes/_app/about.tsx
  - frontend/src/routes/_app/admin.tsx
autonomous: false
requirements:
  - FRNT-DASH-01
  - FRNT-INIT-01
  - FRNT-ADMIN-01
---

# Plan 07-06: Dashboard, Initiative, and Admin Screens

## Goal

Apply the Phase 7 design tokens to the remaining authenticated screens: dashboard, initiative, about, and admin. These screens do not have explicit Figma designs (admin) or are secondary screens (about) — apply design tokens (Rubik, navy `#06004f`, green `#399e5a`, 16px card radius, white cards on green-tinted background) consistently. Preserve all existing API calls, state management, and business logic.

## Context

- All four screens currently use raw HTML with inline style objects and CSS variables.
- The green-tinted background is already applied at the `_app.tsx` shell level (Plan 07-02) — inner screens should NOT re-set the background.
- Figma does not show explicit designs for these screens (per CONTEXT.md "Claude's Discretion" clause for admin/dashboard).
- Use antd `Card`, `Button`, `Table`, `Tabs`, `Modal`, `Alert`, `Tag` components to replace raw HTML where appropriate.
- Do NOT change any API calls, useState hooks, or business logic.

## Tasks

<task id="07-06-T1" type="checkpoint:human-action">

### Read all screen files before editing

Read the following files to understand current state and structure:
- `frontend/src/routes/_app/dashboard.tsx`
- `frontend/src/routes/_app/initiative.tsx`
- `frontend/src/routes/_app/about.tsx`
- `frontend/src/routes/_app/admin.tsx`

Note for each:
- All `useState` variables (must be preserved)
- All API calls via `api.get()` / `api.post()` / `api.delete()` (must be preserved)
- All `useEffect` hooks (must be preserved)
- Any conditional rendering logic (must be preserved)

No MCP call needed for this plan — Figma does not show these screens.

</task>

<task id="07-06-T2" type="implementation">

### Restyle `frontend/src/routes/_app/dashboard.tsx`

Preserve all existing logic. Apply token-consistent visual updates:

1. Replace the outer `<div>` page wrapper with a `<div>` using `maxWidth: '900px'`, `margin: '0 auto'` for centered content.
2. Replace raw heading `<h1>` with styled heading (Rubik, 1.75rem, `#06004f`, fontWeight 700).
3. Replace the white info card (`background: white`, `borderRadius: ...`, `boxShadow: ...`) with antd `Card` component:
   ```typescript
   import { Card, Button, Alert } from 'antd';

   <Card style={{ borderRadius: '16px', boxShadow: '0 2px 12px rgba(6,0,79,0.06)' }}>
     {/* existing card content */}
   </Card>
   ```
4. Replace the "Generate Compliance Report" raw button with antd `Button`:
   ```typescript
   <Button
     type="primary"
     onClick={handleGenerateReport}
     loading={reportLoading}
     size="large"
     style={{ borderRadius: '8px', fontFamily: "'Rubik', sans-serif", fontWeight: 600 }}
   >
     Generate Compliance Report
   </Button>
   ```
5. Replace error divs with antd `Alert type="error"`.
6. Update the "no initiative yet" panel to use the same antd `Card` with a helpful CTA style.
7. Update inline text "sidebar" reference to "menu" (since sidebar is now a drawer): `"Use the menu to navigate to My Initiative"`.

**Preserve:** `user`, `initiative`, `reportLoading`, `reportError`, `error` state; `handleGenerateReport` function; all API calls.

</task>

<task id="07-06-T3" type="implementation">

### Restyle `frontend/src/routes/_app/initiative.tsx`

Read the file first. This screen handles initiative registration (form) and may also show an existing initiative.

Apply consistent visual updates:
1. Page heading: Rubik, `#06004f`, 1.75rem, fontWeight 700.
2. Form card: antd `Card` with `borderRadius: '16px'`.
3. Form inputs: replace raw `<input>` and `<select>` with antd `Input` and `Select`.
4. Submit button: antd `Button type="primary"` with `loading={loading}`.
5. Error/success messages: antd `Alert`.
6. If there is an "existing initiative" display panel, wrap it in antd `Card`.

**Preserve:** All form state, submit handler, API calls, participant_type selection, redirect after creation.

</task>

<task id="07-06-T4" type="implementation">

### Restyle `frontend/src/routes/_app/about.tsx`

Read the file first. This is likely a static content page.

Apply consistent visual updates:
1. Page heading: Rubik, `#06004f`, 1.75rem, fontWeight 700.
2. Content sections: wrap in antd `Card` with `borderRadius: '16px'`.
3. Any links: use green `#399e5a` color.
4. Text body: `color: rgba(6,0,79,0.75)`, `lineHeight: 1.8`, `fontFamily: "'Rubik', sans-serif"`.

This file may be minimal — apply tokens and ensure it matches the overall visual language.

</task>

<task id="07-06-T5" type="implementation">

### Restyle `frontend/src/routes/_app/admin.tsx`

Read the file first. The admin panel has 3 tabs (Users, Questionnaires, Actions) with expandable rows, delete buttons, CSV download, and Reset Demo modal.

Apply design tokens while preserving ALL admin functionality:

1. Page heading: Rubik, `#06004f`, 1.75rem, fontWeight 700.
2. antd `Tabs` already likely used — verify and keep. Adjust tab style if needed (active tab in green or navy).
3. antd `Table` for users/questionnaires lists — keep existing columns. Apply consistent styling:
   ```typescript
   style={{ borderRadius: '8px', overflow: 'hidden' }}
   ```
4. Delete buttons: antd `Button danger size="small"`.
5. CSV download button: antd `Button type="primary"`.
6. Reset Demo button: antd `Button danger` (red, to signal destructive action).
7. Confirmation modal: antd `Modal` — already likely used; verify styling matches tokens (title in `#06004f`).
8. Error/success alerts: antd `Alert`.
9. Admin panel page card: antd `Card` with `borderRadius: '16px'`.

**Preserve:** All tab logic, table data fetching, delete handlers (with cascade), CSV download handler, reset-demo handler, confirmation state.

**Critical note:** The admin API uses raw SQL to bypass enum deserialization — do not change any API calls or data transformation logic.

</task>

<task id="07-06-T6" type="checkpoint:human-verify">

### Verify dashboard, initiative, and admin screens

**User action required — check the following before marking this plan complete:**

**Dashboard (`/dashboard`):**
1. Confirm the dashboard card uses white background with 16px border radius.
2. Confirm the "Generate Compliance Report" button has primary styling (navy/blue fill).
3. Click the button — confirm it still generates and opens the report in a new tab.
4. Confirm no visual regression from previous plan's top navbar.

**Initiative (`/initiative`):**
5. Navigate to `/initiative`. If you already have an initiative, confirm it shows the existing initiative details.
6. If testing fresh: register a new initiative — confirm form submits correctly and participant_type is selectable.
7. Confirm the form card styling (white, 16px radius) matches the overall design.

**About (`/about`):**
8. Navigate to `/about` — confirm the page renders with consistent font and card styling.

**Admin (`/admin`, admin role required):**
9. Log in as admin and navigate to `/admin`.
10. Confirm all 3 tabs are visible and clickable (Users, Questionnaires, Actions).
11. Confirm the Users tab table renders with expandable rows.
12. Confirm a delete button (if shown) triggers the confirmation flow (does not delete immediately).
13. Confirm the CSV download button works (downloads a file).
14. Confirm the Reset Demo button shows a confirmation modal before executing.

**Overall:**
15. Navigate through all screens — confirm the green-tinted background is consistent on all authenticated pages.
16. Confirm no console errors on any screen.

**Pass criteria:** All screens styled consistently with design tokens, all business logic intact and functional, admin panel remains fully operational.

</task>

## must_haves

- All 4 screens use white antd `Card` components with `borderRadius: '16px'` for content panels
- All heading text uses Rubik font, color `#06004f`, fontWeight 700
- All submit/action buttons use antd `Button type="primary"` with `loading` prop
- All error messages use antd `Alert type="error"`
- Dashboard "Generate Compliance Report" still works end-to-end
- Admin panel all 3 tabs still functional (Users, Questionnaires, Actions)
- Admin delete operations still work with cascade logic
- CSV export and Reset Demo still work
- "sidebar" text references updated to "menu" where present in UI copy
- No Tailwind classes used

## Verification Criteria

- Each of the 4 screen files imports at least `Card` from `antd`
- `dashboard.tsx` imports `Button` from antd and uses it for the report button
- `admin.tsx` preserves all tab/table/modal logic
- `initiative.tsx` preserves `participant_type` selection logic
- No TypeScript errors (`tsc --noEmit` passes)
- All API calls (`api.get`, `api.post`, `api.delete`) unchanged in all 4 files
