---
phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf
plan: 06
subsystem: ui
tags: [antd, theme, react, css, borderRadius, dssc-rebrand]

# Dependency graph
requires:
  - phase: 16.3-01,16.3-02,16.3-03,16.3-05
    provides: retake-save fix, questionnaire text color, View Report assessment_id fix, admin completed-assessment-count fields — all landed in dashboard.tsx/assessments.tsx/admin.index.tsx/QuestionCard.tsx before this plan edited the same files' unrelated borderRadius lines
provides:
  - "Square (0px) corners on every antd Button/Card component app-wide via theme.ts component-level overrides"
  - "Square corners on every native <button>/CTA-styled <Link>/card-like <div> across 17 files' inline style overrides (53 individual edits)"
  - "Decorative circles, pills, Input/Select/TextArea fields, antd Table wrappers, and antd Alert banners left explicitly unchanged"
affects: [16.3-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Component-scoped antd theme overrides (components.Button.borderRadius / components.Card.borderRadiusLG) instead of root token.borderRadius/borderRadiusLG — avoids squaring Modal/Alert/Tag/Select/Popconfirm/Drawer, which have no component-level override and would otherwise inherit the root token"

key-files:
  created: []
  modified:
    - frontend/src/lib/theme.ts
    - frontend/src/routes/_app/dashboard.tsx
    - frontend/src/routes/_app/assessments.tsx
    - frontend/src/routes/_app/admin.index.tsx
    - frontend/src/routes/_app/questionnaire.tsx
    - frontend/src/components/questionnaire/QuestionCard.tsx
    - frontend/src/routes/index.tsx
    - frontend/src/components/questionnaire/WizardPage.tsx
    - frontend/src/components/questionnaire/StepPills.tsx
    - frontend/src/routes/_app/admin.heatmap.tsx
    - frontend/src/routes/_app/about.tsx
    - frontend/src/routes/_auth/reset-password.tsx
    - frontend/src/routes/_app/initiative.tsx
    - frontend/src/components/questionnaire/WelcomeScreen.tsx
    - frontend/src/routes/_app/report.tsx
    - frontend/src/routes/_auth/login.tsx
    - frontend/src/routes/_auth/register.tsx
    - frontend/src/routes/_auth/forgot-password.tsx

key-decisions:
  - "Changed only components.Button.borderRadius and components.Card.borderRadiusLG in theme.ts, not the root token.borderRadius/borderRadiusLG — root tokens are the default inherited by Modal/Alert/Tag/Select/Popconfirm/Drawer, none of which have their own override in this file; squaring the root would silently square those too, violating D-01's explicit alert-banner exclusion"
  - "Edited by unique surrounding-context match rather than by the plan's literal line numbers, since Wave 1's edits (16.3-01/02/03/05) shifted line numbers in the 5 shared files without changing the borderRadius lines' content — verified every edit's content matched the plan's description exactly before applying"

patterns-established:
  - "borderRadius inline overrides squared to \"0\"/'0' matching each file's pre-existing quote-style convention (double-quote files use \"0\", single-quote files use '0')"

requirements-completed: [BUG-06]

# Metrics
duration: ~20min
completed: 2026-08-05
---

# Phase 16.3 Plan 06: App-wide square button/card corners (BUG-06) Summary

**Squared every antd Button/Card corner via 2 theme.ts component overrides plus 53 individual inline-style edits across 17 files, matching dssc.eu's `.btn{border-radius:0}`/`.card{border-radius:0}` brand style, while leaving decorative circles/pills/Inputs/Tables/Alerts untouched.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-05T05:10:31Z (session resumed from Wave 1 completion)
- **Completed:** 2026-08-05T07:19:37+02:00
- **Tasks:** 3 completed
- **Files modified:** 18 (theme.ts + 17 component/route files)

## Accomplishments
- `theme.ts`'s `components.Button.borderRadius` (8→0) and `components.Card.borderRadiusLG` (16→0) squared, with root `token.borderRadius`/`token.borderRadiusLG`/`components.Input.borderRadius` deliberately left at 8/16/8 to avoid squaring Modal/Alert/Tag/Select/Popconfirm/Drawer/Input
- 16 inline `borderRadius` overrides squared across the 5 files Wave 1 already touched (`dashboard.tsx`, `assessments.tsx`, `admin.index.tsx`, `questionnaire.tsx`, `QuestionCard.tsx`), verified against Wave 1's post-edit content rather than the plan's stale line numbers
- 37 inline `borderRadius` overrides squared across the remaining 12 independent files (`index.tsx`, `WizardPage.tsx`, `StepPills.tsx`, `admin.heatmap.tsx`, `about.tsx`, `reset-password.tsx`, `initiative.tsx`, `WelcomeScreen.tsx`, `report.tsx`, `login.tsx`, `register.tsx`, `forgot-password.tsx`), including native `<button>`s (WizardPage's Previous/Next/Retry, WelcomeScreen's Begin, register.tsx's DSI/SP toggle) that are invisible to antd theming and required direct edits
- Repo-wide `grep -rn "borderRadius" src` sweep after all edits confirms every changed line reads `0`/`"0"`/`'0'` and every explicitly out-of-scope line (decorative circles `50%`, the `100px` "Question X of Y" pill, `Input`/`Select`/`TextArea`, antd `Table` wrapper styles, antd `Alert` styles, error/warning banners, TopNav's hamburger-icon bars, index.tsx's small icon-accent badge, StepPills's step-state circles) is unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: theme.ts — square antd Button and Card corners only** - `645b643` (fix)
2. **Task 2: Square inline overrides in the files this phase's earlier plans already touched** - `1b521e3` (fix)
3. **Task 3: Square inline overrides in the remaining independent files** - `7816ef4` (fix)

_No TDD tasks in this plan — all three were direct `type="auto"` inline-style edits._

## Files Created/Modified
- `frontend/src/lib/theme.ts` - `components.Button.borderRadius`/`components.Card.borderRadiusLG` set to 0
- `frontend/src/routes/_app/dashboard.tsx` - 6 Card/Button `borderRadius` overrides squared
- `frontend/src/routes/_app/assessments.tsx` - 4 Card `borderRadius` overrides squared (Table wrappers untouched)
- `frontend/src/routes/_app/admin.index.tsx` - expanded-row panel + 3 Button/Card overrides squared (Table/Alert untouched)
- `frontend/src/routes/_app/questionnaire.tsx` - Retry button `borderRadius` squared (warning/error banners untouched)
- `frontend/src/components/questionnaire/QuestionCard.tsx` - question-card wrapper `borderRadius` squared
- `frontend/src/routes/index.tsx` - 5 CTA-Link/step-card overrides squared (small icon-accent badge left untouched)
- `frontend/src/components/questionnaire/WizardPage.tsx` - 5 native-button/question-card overrides squared (error banners/pill/intro box untouched)
- `frontend/src/components/questionnaire/StepPills.tsx` - outer step-nav panel squared (step-state circles untouched)
- `frontend/src/routes/_app/admin.heatmap.tsx` - 3 Card overrides squared (Table wrapper untouched)
- `frontend/src/routes/_app/about.tsx` - 1 Card override squared
- `frontend/src/routes/_auth/reset-password.tsx` - 2 card-wrapper divs + submit Button squared
- `frontend/src/routes/_app/initiative.tsx` - 7 Card/Button overrides squared (Input/TextArea untouched)
- `frontend/src/components/questionnaire/WelcomeScreen.tsx` - card wrapper + Begin button squared
- `frontend/src/routes/_app/report.tsx` - 2 card-style divs + Download PDF Button squared (band-color dot untouched)
- `frontend/src/routes/_auth/login.tsx` - card wrapper + submit Button squared
- `frontend/src/routes/_auth/register.tsx` - card wrapper + DSI/SP toggle button + submit Button squared
- `frontend/src/routes/_auth/forgot-password.tsx` - card wrapper + submit Button squared

## Decisions Made
- Left `token.borderRadius`/`token.borderRadiusLG` (root) and `components.Input.borderRadius` unchanged in `theme.ts` — the root tokens are the default fallback for Modal/Alert/Tag/Select/Popconfirm/Drawer, none of which have a component-level override in this file, so changing the root would have silently squared all of those, directly violating D-01/CONTEXT.md's explicit "not alert banners... out of scope" carve-out
- Applied every edit by matching unique surrounding code context (element type, sibling props, following text) rather than the plan's literal line numbers — Wave 1's plans (16.3-01/02/03/05) shifted line numbers in the 5 shared files (dashboard.tsx, assessments.tsx, admin.index.tsx, questionnaire.tsx, QuestionCard.tsx) without touching the `borderRadius` lines themselves; verified each target line's content matched the plan's description exactly before editing

## Deviations from Plan

None — plan executed exactly as written. All 53 inline-style edits plus the 2 theme.ts component overrides match the plan's per-line table precisely (content-verified, since Wave 1 shifted some absolute line numbers without changing the target lines). No architectural changes, no scope expansion or contraction.

## Issues Encountered
None — `tsc -b --noEmit`, `eslint .`, and `npm run build` all passed cleanly on the first attempt for every task. The only unrelated finding was `frontend/src/routeTree.gen.ts`'s pre-existing line-ending-only diff (recurring build/typecheck environment noise documented in prior sessions since Phase 16.2-01) and, mid-Task-3, unrelated concurrent changes to `.planning/ROADMAP.md` and a new `16.3-04-SUMMARY.md` from the still-in-flight sibling plan 16.3-04 (report.html PDF template, awaiting its own human-verify checkpoint) — both left untouched/unstaged per the scope-boundary rule, consistent with this plan's own `<important_note>` that 16.3-04 is unrelated and should not be waited on.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- BUG-06 fully satisfied: every antd Button/Card and every native-button/CTA-Link/card-like-div across the app now renders with square (0px) corners, matching dssc.eu's live `.btn{border-radius:0}`/`.card{border-radius:0}` styling, while decorative circles/pills/Inputs/Tables/Alerts remain untouched
- Manual visual confirmation (homepage, dashboard, wizard, a report page, Admin Panel) against a running dev server or the Railway Integration deployment is still recommended per the plan's own `<verification>` block, but is not a blocking checkpoint in this plan (no `checkpoint:human-verify` task was defined) — code-level verification (repo-wide grep sweep + tsc/eslint/build) is complete and passing
- Phase 16.3 (7-bug fix batch) now has 6/6 named-bug plans complete (16.3-01 through 16.3-06 all landed); 16.3-04 (PDF "Centre of Excellence" wording + score-dot clipping + "For the initiative:" label, a backend `report.html` template plan) remains paused at its own `checkpoint:human-verify` gate awaiting the orchestrator/user, unrelated to this plan's file set
- Ready for the orchestrator to check remaining phase-completion status once 16.3-04's checkpoint is resolved

---
*Phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf*
*Completed: 2026-08-05*

## Self-Check: PASSED

- FOUND: frontend/src/lib/theme.ts
- FOUND: .planning/phases/16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf/16.3-06-SUMMARY.md
- FOUND: 645b643 (Task 1 commit)
- FOUND: 1b521e3 (Task 2 commit)
- FOUND: 7816ef4 (Task 3 commit)
