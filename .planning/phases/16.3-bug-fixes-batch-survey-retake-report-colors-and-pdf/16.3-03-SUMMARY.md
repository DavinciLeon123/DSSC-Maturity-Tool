---
phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf
plan: 03
subsystem: ui
tags: [react, tanstack-router, assessments, report]

# Dependency graph
requires:
  - phase: 16
    provides: "resolve_report_assessment (backend/app/services/dimension_scoring.py) already accepts and resolves an explicit ?assessment_id=, proven by test_report_data_specific_assessment_id_returns_that_version"
  - phase: 16
    provides: "The admin.heatmap.tsx View-report Link pattern (search={{ assessment_id: ... }}) already shipped and proven"
provides:
  - "assessments.tsx's per-row 'View report' link now carries that row's own assessment_id as a search param instead of an unparameterized /report link"
affects: [16.3-06]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Per-row Link search param derived from the table record (record.id), matching the existing admin.heatmap.tsx precedent"]

key-files:
  created: []
  modified: [frontend/src/routes/_app/assessments.tsx]

key-decisions:
  - "None beyond the plan's own instructions - implementation matched the plan's <interfaces> code block verbatim"

patterns-established: []

requirements-completed: [BUG-03]

# Metrics
duration: <5min (verification + commit only; the code edit was already present in the working tree from a prior crashed attempt)
completed: 2026-08-05
---

# Phase 16.3 Plan 03: View Report assessment_id fix Summary

**assessments.tsx's "Report" column now passes `search={{ assessment_id: record.id }}` on every history row's Link, so each row opens that specific submitted version's report instead of always the newest one.**

## Performance

- **Duration:** <5 min (this session — the code change itself was already present in the working tree from a prior attempt that crashed mid-execution before committing; this session verified it matched the plan exactly and committed it)
- **Started:** 2026-08-05T05:03:00Z (approx, this session)
- **Completed:** 2026-08-05T05:08:00Z (approx, this session)
- **Tasks:** 1/1
- **Files modified:** 1

## Accomplishments
- Fixed Bug #3: clicking "View report" on any row in the Assessment History table now shows/downloads that row's own version, not always the newest submitted assessment
- Removed a stale, factually-outdated Phase-15-scope comment that claimed per-version reports were still Phase 16's unfinished job (Phase 16 already shipped that capability)

## Task Commits

Each task was committed atomically:

1. **Task 1: Pass assessment_id through the Report column link** - `b6324a5` (fix)

**Plan metadata:** (this commit, docs: complete plan)

_Note: this single task's change was already staged in the working tree from a prior crashed execution attempt (unrelated API error, not a content problem). This session read the plan, confirmed the working-tree diff matched the plan's `<interfaces>` code block and file list exactly, ran the plan's full verification suite (`tsc -b --noEmit`, `eslint`, and the literal `grep` check from the plan's `<verify>` block), then committed._

## Files Created/Modified
- `frontend/src/routes/_app/assessments.tsx` - "Report" column's `render` now takes `(_, record: AssessmentSummary)` and renders `<Link to="/report" search={{ assessment_id: record.id }}>View report</Link>` per row, replacing the prior parameterless `<Link to="/report">` shared by every row; deleted the stale Phase-15-scope comment above it

## Decisions Made
None beyond the plan's own instructions — implementation matched the plan's `<interfaces>` code block verbatim, including the explicit instruction not to add `initiative_id` (this page is already scoped to the current user's own initiative) and not to touch the Version/Submitted/Overall-average columns or the comparison table below.

## Deviations from Plan

### Auto-fixed Issues

None — the code change itself required no fixes; it matched the plan exactly on first inspection.

### Process deviation (not a code deviation)

**1. Commit landed via `git commit --only` after a concurrent sibling-plan agent's git-index race swept this plan's staged change into their own commit**
- **Found during:** Task 1, at commit time
- **Issue:** This working tree is shared by multiple concurrently-running plan agents (16.3-01 through 16.3-06, Wave 1 plans execute in parallel per `ROADMAP.md`). After `git add frontend/src/routes/_app/assessments.tsx`, a concurrent 16.3-02 agent's own `git add`/`git commit` ran in the same window and its commit (`b6324a5`, "fix(16.3-02): change questionnaire text color from DSSC blue to near-black") absorbed this plan's already-staged `assessments.tsx` change alongside 16.3-02's own `AnswerButtonGroup.tsx`/`QuestionCard.tsx` files. A direct `git commit --only -m ... -- frontend/src/routes/_app/assessments.tsx` attempt immediately after found nothing left to commit (already swept into `b6324a5`).
- **Fix:** No fix needed — verified via `git show b6324a5 -- frontend/src/routes/_app/assessments.tsx` that the diff inside that commit is byte-for-byte identical to this plan's intended change (the exact `search={{ assessment_id: record.id }}` render function and stale-comment removal, nothing else touched, nothing corrupted or lost). Per this repo's own established precedent for this exact race (see STATE.md's Phase 16.2-01/16.2-04/16.2-05 entries — "did not rewrite a concurrently-running agent's commit"), left the change attributed to `b6324a5` rather than attempting a `git commit --amend` or revert/recommit that could disturb another agent's in-flight work.
- **Files modified:** frontend/src/routes/_app/assessments.tsx (content unaffected — verified correct)
- **Verification:** `git show b6324a5 -- frontend/src/routes/_app/assessments.tsx` diff matches this plan's `<interfaces>` code block exactly; `git log --oneline --all | grep b6324a5` confirms the commit exists on this branch.
- **Committed in:** `b6324a5` (not a dedicated 16.3-03 commit — see above)

---

**Total deviations:** 0 code deviations, 1 process deviation (git-index race, documented and verified, no content impact)
**Impact on plan:** None on functionality or correctness — the shipped diff is exactly what the plan specified. Only the commit's authorship/grouping differs from a clean 1:1 task-to-commit mapping.

## Issues Encountered
None beyond the git-index race documented above (same recurring pattern as prior phases in this repo when multiple plan agents share one working tree — see STATE.md's Phase 16.2 sessions for the established handling precedent).

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- BUG-03 fully resolved: every Assessment History row's "View report" link is now correctly parameterized by that row's own `assessment_id`, matching the already-proven `admin.heatmap.tsx` pattern and the already-tested backend `resolve_report_assessment` behavior.
- No backend changes were needed or made — this was purely a missing frontend search param at one call site.
- Ready for Wave 2 (16.3-06, square-corners styling), which depends on this plan's file (`assessments.tsx`) already being in its final Wave-1 state.

---
*Phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf*
*Completed: 2026-08-05*

## Self-Check: PASSED

- FOUND: frontend/src/routes/_app/assessments.tsx
- FOUND: .planning/phases/16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf/16.3-03-SUMMARY.md
- FOUND: b6324a5 (git log --oneline --all)
