---
phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/questionnaire/QuestionCard.tsx
  - frontend/src/components/questionnaire/AnswerButtonGroup.tsx
autonomous: true
requirements: [BUG-02]

must_haves:
  truths:
    - "Question text in the assessment wizard renders in near-black (--dssc-ink), not DSSC blue"
    - "Answer-option labels under each radio circle render in near-black, not DSSC blue"
    - "The selected-answer radio circle's blue border/fill accent is unchanged (this is an interactive-state color, not body text, and is explicitly out of scope)"
  artifacts:
    - path: "frontend/src/components/questionnaire/QuestionCard.tsx"
      provides: "Question text color changed from #008ecf to #1c2025"
      contains: "color: \"#1c2025\""
    - path: "frontend/src/components/questionnaire/AnswerButtonGroup.tsx"
      provides: "Answer-option label color changed from #008ecf to #1c2025 (radio circle accent colors untouched)"
      contains: "color: \"#1c2025\""
  key_links: []
---

<objective>
Fix Bug #2 (questionnaire text is blue, should be black): `QuestionCard.tsx` and `AnswerButtonGroup.tsx` both hardcode `color: "#008ecf"` (DSSC brand blue) on the question text and answer-option labels, overriding the app-wide near-black body-text color (`#1c2025`, `--dssc-ink`) that Phase 16.2-01 already established via `theme.ts`'s `colorText`. These two components predate that rebrand sweep and were never updated.

Purpose: match the rest of the app's post-16.2 text-color convention. Scope is deliberately narrow — per `16.3-RESEARCH.md`, `#008ecf` appears as inline text color pervasively elsewhere in the codebase (dashboard, assessments, report, admin), but the bug report is explicit and narrow ("the questions and the answers in the questionnaire itself"). Do not perform a repo-wide blue-to-black sweep — that is scope creep beyond this bug.
Output: two one-line color changes.
</objective>

<execution_context>
@C:/Users/djlia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/djlia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf/16.3-RESEARCH.md
@frontend/src/components/questionnaire/QuestionCard.tsx
@frontend/src/components/questionnaire/AnswerButtonGroup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change question and answer-label text color to near-black</name>
  <files>frontend/src/components/questionnaire/QuestionCard.tsx, frontend/src/components/questionnaire/AnswerButtonGroup.tsx</files>
  <action>
In `frontend/src/components/questionnaire/QuestionCard.tsx`, line 35 — the question `<p>` element's inline style — change:
```tsx
color: "#008ecf",
```
to:
```tsx
color: "#1c2025",
```
(This is the `<p style={{ fontWeight: 400, fontSize: "1rem", lineHeight: 1.5, color: "#008ecf", ... }}>{question.text}</p>` block. Do not touch the outer card `<div>`'s `borderRadius`/`boxShadow`/`background` — those are out of scope for this bug, handled separately by Bug #6's plan.)

In `frontend/src/components/questionnaire/AnswerButtonGroup.tsx`, line 92 — the answer-option label `<span>`'s inline style — change:
```tsx
color: "#008ecf",
```
to:
```tsx
color: "#1c2025",
```
(This is the `<span style={{ fontSize: "0.8125rem", lineHeight: 1.4, textAlign: "center", color: "#008ecf", fontWeight: isSelected ? 600 : 400, ... }}>{opt.label}</span>` block, inside the `.map((opt) => ...)` render.)

Do NOT change lines 66-67 in `AnswerButtonGroup.tsx` (`border: \`2px solid ${isSelected ? "#008ecf" : "rgba(0,142,207,0.25)"}\`` and `background: isSelected ? "#008ecf" : "white"`) — this is the radio circle's selected-state accent color, an interactive-state indicator, not body text, and explicitly out of scope per the bug report's wording.
  </action>
  <verify>
    <automated>cd frontend && grep -n 'color: "#008ecf"' src/components/questionnaire/QuestionCard.tsx src/components/questionnaire/AnswerButtonGroup.tsx; test $? -eq 1 && echo "PASS: no remaining #008ecf text color in either file"</automated>
  </verify>
  <done>Both files' question/answer-label text renders #1c2025; the radio-circle selected-state blue (border/fill) is unchanged; grep for `color: "#008ecf"` in these two files returns zero matches.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc -b --noEmit && npx eslint src/components/questionnaire/QuestionCard.tsx src/components/questionnaire/AnswerButtonGroup.tsx
```
Manual: open the wizard on any dimension page — question text and the 5 answer-option labels underneath each radio circle should read near-black; the selected radio circle itself should still show its blue border/fill.
</verification>

<success_criteria>
- Question text and answer-option labels in the wizard render `#1c2025` (near-black), matching the rest of the post-16.2 app.
- The selected-state radio circle accent color is unchanged.
- `npm run typecheck` and `npm run lint` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf/16.3-02-SUMMARY.md`
</output>
