---
phase: 16.3-bug-fixes-batch-survey-retake-report-colors-and-pdf
verified: 2026-08-05T05:29:02Z
status: passed
score: 21/21 must-haves verified (20 at code/test level, 1 confirmed by human visual sign-off post-deploy)
resolution: "The 2 pre-declared human-verification backstops below (Bug #5a PDF dot-clip render, Bug #6 square-corners visual pass) were both confirmed by the user against the Railway Integration deployment after PR #11 merged to staging (2026-08-05) — user's verbatim response: \"Everything works, thank you!\" See 16.3-04-SUMMARY.md's Post-Merge Confirmation section."
human_verification:
  - test: "PDF status-dot clipping fix (Bug #5a) — render a real report PDF (WeasyPrint, e.g. via Railway Integration deployment) for a submitted assessment"
    expected: "Every submitted-answer row's colored status dot (green/orange/red) renders as a full, complete circle next to its score — not a clipped sliver or partial arc. Also confirm no regression elsewhere (radar chart labels unclipped, priority list scores flush-right, legend on one line) and the other two report.html text fixes (\"For the initiative:\" label, \"Data Spaces Support Centre\" wording) render as expected."
    why_human: "WeasyPrint's native libraries are unavailable in this local sandbox (confirmed this session: `OSError: cannot load library 'libgobject-2.0-0'`), a standing project limitation carried over from Phase 16-05. The template edit itself was code-reviewed and matches the already-proven `.priority-row`/`.legend` WeasyPrint-safe pattern (flattened single-level flex, `flex-shrink: 0`, fixed-width score column) byte-for-byte against the plan, but only a real render can confirm the visual clipping is actually gone. The user explicitly deferred this check to a post-deploy Railway pass per 16.3-04-SUMMARY.md's conditional/provisional checkpoint approval — this is by design, not a gap."
    resolution: "RESOLVED 2026-08-05 — confirmed on a real WeasyPrint render via the Railway Integration deployment (PR #11 merged to staging). Status dot renders as a full unclipped circle; no other visual regression reported."
  - test: "General visual pass on Bug #6 (square corners) across homepage, dashboard, wizard, a report page, and Admin Panel"
    expected: "Every button and card-style panel shows square (0px) corners, matching dssc.eu, while status dots/radio circles/pills/Inputs/Tables/Alerts remain visually unchanged (still rounded where appropriate)."
    why_human: "Code-level verification (theme.ts overrides + a repo-wide grep sweep of all ~90 borderRadius occurrences) is complete and every changed/unchanged line matches the plan's classification exactly, but no rendered-browser screenshot pass was performed in this environment — recommended as a quick manual sanity check, not a blocking gap (no checkpoint task required this in the plan)."
    resolution: "RESOLVED 2026-08-05 — confirmed on the Railway Integration deployment alongside the Bug #5a check."
---

# Phase 16.3: Bug fixes batch: survey retake, report colors, and PDF Verification Report

**Phase Goal:** Fix 7 user-reported bugs: (1) retaking the questionnaire fails to save because the retake isn't registered as a new assessment, (2) questionnaire question/answer text renders blue instead of black, (3) "View Report" always shows/downloads the newest survey instead of the one the user selected, (4) PDF report is missing a "For the initiative:" label before the DSI name, (5a) PDF report's score status dot (green/orange/red) is visually clipped, (5b) PDF "Get expert help" box says "Centre of Excellence" instead of "Data Spaces Support Centre", (6) buttons AND cards app-wide have rounded corners but DSSC's brand style (dssc.eu) uses square corners, (7) Admin Panel's "Answers per user" should show completed-assessment count instead of raw answer count on both the Dataspace Maturity Assessments tab and the Users tab, plus an indicator for an in-progress next assessment.

**Verified:** 2026-08-05T05:29:02Z
**Status:** passed (originally `human_needed`; both human-verification backstops confirmed 2026-08-05 post-deploy — see `resolution` in frontmatter)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | TopNav drawer's "Dataspace Maturity Assessment" item routes through the same confirm-then-retake gate as Dashboard's CTA | VERIFIED | `frontend/src/lib/retake.ts` exports `useStartOrRetakeAssessment`; `TopNav.tsx` calls it and wires a per-item `onClick` (`preventDefault` + `void startOrRetake()`) on the nav item instead of a bare `<Link>` |
| 2 | A TopNav-initiated retake calls `POST /initiatives/{id}/retake` and lands on an editable wizard whose saves succeed (200, not 403) | VERIFIED | Same hook drives both entry points; backend `POST /initiatives/{id}/retake` contract proven by `tests/api/test_retake_flow.py` (2/2 passing this session) |
| 3 | A user who lands on `/questionnaire` with a still-`submitted` initiative is redirected to the dashboard | VERIFIED | `questionnaire.tsx` has a `useEffect` that calls `navigate({ to: "/dashboard" })` when `initiative?.status === "submitted"` |
| 4 | Question text in the wizard renders near-black (`#1c2025`), not DSSC blue | VERIFIED | `QuestionCard.tsx:35` reads `color: "#1c2025"`; no `#008ecf` text-color occurrence remains in the file |
| 5 | Answer-option labels render near-black, not DSSC blue | VERIFIED | `AnswerButtonGroup.tsx:92` reads `color: "#1c2025"` |
| 6 | Selected-answer radio circle's blue accent is unchanged (interactive state, out of scope) | VERIFIED | `AnswerButtonGroup.tsx:66-67` still reads `border: ...#008ecf...` / `background: isSelected ? "#008ecf" : "white"` |
| 7 | Clicking "View report" on a specific Assessment History row shows that row's own scores/graph, not always the newest | VERIFIED | `assessments.tsx`'s Report column renders `<Link to="/report" search={{ assessment_id: record.id }}>`; backend `resolve_report_assessment` proven by `test_report_data_specific_assessment_id_returns_that_version` (passing) |
| 8 | Downloading the PDF from that report view downloads the same version being viewed | VERIFIED | `report.tsx`'s `handleDownload` forwards `assessment_id` from the URL search params (`useReportSearchParams`) into the download request URL, unchanged and consistent with the clicked row's id |
| 9 | Clicking "View report" on the newest row still works (no regression) | VERIFIED | Same code path, `assessment_id` omission/defaulting handled server-side; no special-casing removed |
| 10 | PDF header reads "For the initiative: `<name>`", not a bare name | VERIFIED | `backend/app/templates/report.html:171` — `For the initiative: <strong>{{ initiative.name }}</strong>...` |
| 11 | Each submitted-answer row's score status dot renders as a full, unclipped circle | VERIFIED (code-level only) | `report.html:222-228` — the nested-flex wrapper was removed; dot and score are now direct children of `.answers-row` with explicit `flex-shrink: 0`/`min-width`, matching the proven `.priority-row` pattern. **Not yet visually confirmed against a real WeasyPrint render** — see Human Verification below (by design, per user's conditional checkpoint approval) |
| 12 | "Get expert help" box says "The Data Spaces Support Centre", not "The Centre of Excellence" | VERIFIED | `report.html:243` — "The Data Spaces Support Centre is here to help you..."; zero occurrences of the old wording remain |
| 13 | No other visual regression in the PDF (radar/priority-list/legend) vs. Phase 16-05 baseline | VERIFIED (code-level) | `.priority-row`/`.legend`/`.radar-wrap` blocks untouched by this phase's diff (`git show f478f4c` limited to header/answers-row/expert-help blocks) |
| 14 | Admin Panel's Assessments tab shows completed (submitted) assessment count, not raw answer count | VERIFIED | `admin.py`'s `list_initiatives()` computes `COUNT(DISTINCT a.id) FILTER (WHERE a.status = 'submitted')`; `admin.index.tsx` renders `"Completed Assessments"` column from `completed_assessment_count` |
| 15 | Admin Panel's Users tab expandable row shows the same completed-assessment-count semantics | VERIFIED | `admin.py`'s `list_users()` uses an equivalent `FILTER`/`EXISTS` sub-query; `admin.index.tsx`'s expanded-row panel renders `"Completed assessments: {record.completed_assessment_count}"` |
| 16 | Both tabs show an "In progress" indicator when a draft assessment is underway | VERIFIED | Both endpoints return `has_draft_in_progress` (correlated `EXISTS`); both tabs render an antd `<Tag color="processing">In progress</Tag>` conditionally |
| 17 | An initiative with 2 submitted + 1 simultaneous draft assessment shows `completed_assessment_count=2`, `has_draft_in_progress=true` (no Cartesian-product miscount) | VERIFIED | Both rewritten tests in `test_admin.py` assert exactly this scenario; 17/17 tests in `test_admin.py` pass |
| 18 | Every antd Button/Card renders with square (0px) corners | VERIFIED | `theme.ts`: `components.Button.borderRadius: 0`, `components.Card.borderRadiusLG: 0` (root token/Input left untouched, deliberately, to avoid squaring Modal/Alert/Tag/Select/Popconfirm/Drawer) |
| 19 | Every native `<button>`/CTA-styled `<Link>` renders with square corners | VERIFIED | Repo-wide `grep -rn "borderRadius" src` sweep confirms all ~53 identified inline overrides across 17 files now read `0`/`"0"`/`'0'` |
| 20 | Card-like plain-`<div>` containers render with square corners | VERIFIED | Same sweep — all card-style wrapper `<div>`s (login/register/reset-password/forgot-password card wrappers, question-card wrapper, StepPills panel, report.tsx radar/priority cards, etc.) read `0` |
| 21 | Decorative circles, pills, Input/Select/TextArea, Table wrappers, and Alert banners remain unchanged | VERIFIED | Sweep confirms `50%` circles (radio buttons, band dots, step circles), `100px` pill, `Input`/`TextArea` `8px`, `Table` wrapper `8px`, `Alert` `8px`, and error/warning banners were all left untouched exactly as scoped |

**Score:** 20/21 truths verified at the appropriate level; 1 (#11) verified at code-level only, correctly deferred to human visual confirmation per the phase's own explicit, user-approved plan.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `frontend/src/lib/retake.ts` | `useStartOrRetakeAssessment()` hook | VERIFIED | Exists, exports the hook, matches plan's `<action>` block verbatim |
| `frontend/src/components/layout/TopNav.tsx` | Nav item routes through shared hook | VERIFIED | Imports and calls `useStartOrRetakeAssessment`, custom `onClick` on the assessment nav item |
| `frontend/src/routes/_app/dashboard.tsx` | `handleStartOrRetake` delegates to shared hook | VERIFIED | `handleStartOrRetake` now just calls `void startOrRetake()` |
| `frontend/src/routes/_app/questionnaire.tsx` | Defense-in-depth redirect | VERIFIED | `useEffect` redirect present, `status` added to fetched shape |
| `frontend/src/components/questionnaire/QuestionCard.tsx` | Question text `#1c2025` | VERIFIED | Line 35 |
| `frontend/src/components/questionnaire/AnswerButtonGroup.tsx` | Answer-label text `#1c2025` | VERIFIED | Line 92; radio-circle accent untouched |
| `frontend/src/routes/_app/assessments.tsx` | Report column passes row's `assessment_id` | VERIFIED | `search={{ assessment_id: record.id }}` present |
| `backend/app/templates/report.html` | 3 content/layout fixes | VERIFIED (code) | "For the initiative:" label, flattened answers-row, corrected expert-help wording all present; visual confirmation pending (see Human Verification) |
| `backend/app/api/v1/admin.py` | `completed_assessment_count`/`has_draft_in_progress` on both endpoints | VERIFIED | Both `list_initiatives()`/`list_users()` rewritten; `answer_count` fully removed |
| `backend/tests/api/test_admin.py` | Tests rewritten for new semantics | VERIFIED | Both tests renamed/rewritten with submitted+draft coexistence case; 17/17 pass |
| `frontend/src/routes/_app/admin.index.tsx` | New fields consumed on both tabs | VERIFIED | Interfaces, "Completed Assessments"/"Next Assessment" column, expanded-row "Completed assessments"/"Next assessment" fields all present |
| `frontend/src/lib/theme.ts` | `Button`/`Card` `borderRadius: 0` | VERIFIED | Root token/Input deliberately unchanged; both component overrides at 0 |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `TopNav.tsx` | `POST /initiatives/{id}/retake` | `useStartOrRetakeAssessment()`'s `Modal.confirm` `onOk` handler | WIRED | Confirmed in `retake.ts`; shared by both TopNav and Dashboard |
| `retake.ts` | `frontend/src/lib/api.ts` | `api.get('/initiatives/me')` / `api.post(...retake)` | WIRED | Both calls present |
| `assessments.tsx` | `report.tsx` | `<Link to="/report" search={{ assessment_id: record.id }}>` | WIRED | Confirmed; `report.tsx`'s `useReportSearchParams` reads `assessment_id` from the URL and `handleDownload` forwards it |
| `admin.py` | `assessment.status` | `COUNT(DISTINCT a.id) FILTER (WHERE a.status = 'submitted')` / `EXISTS (...WHERE status='draft')` | WIRED | Present in both `list_initiatives()` and `list_users()` |
| `admin.index.tsx` | `GET /admin/initiatives`, `GET /admin/users` | `AdminInitiativeRow`/`AdminUserRow` `.completed_assessment_count`/`.has_draft_in_progress` | WIRED | Interfaces match backend response shape; both consumed in render |
| `theme.ts` | antd `ConfigProvider` | `mamiTheme.components.Button/Card` overrides | WIRED | `main.tsx` applies `mamiTheme` via `ConfigProvider`; overrides confirmed present (not independently re-verified `main.tsx` wiring, but this is pre-existing infrastructure unmodified by this phase) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| BUG-01 | 16.3-01 | Retake-save fix | SATISFIED | See truths 1-3 |
| BUG-02 | 16.3-02 | Questionnaire text blue-to-black | SATISFIED | See truths 4-6 |
| BUG-03 | 16.3-03 | View Report per-version fix | SATISFIED | See truths 7-9 |
| BUG-04 | 16.3-04 | PDF "For the initiative:" label | SATISFIED | See truth 10 |
| BUG-05A | 16.3-04 | PDF score-dot clipping | SATISFIED (code-level); visual confirmation pending | See truth 11 / Human Verification |
| BUG-05B | 16.3-04 | PDF "Data Spaces Support Centre" wording | SATISFIED | See truth 12 |
| BUG-06 | 16.3-06 | App-wide square button/card corners | SATISFIED | See truths 18-21 |
| BUG-07 | 16.3-05 | Admin panel completed-assessment count + indicator | SATISFIED | See truths 14-17 |

Note: BUG-01 through BUG-07/05A/05B are phase-local pseudo-IDs declared in plan frontmatter (`requirements:` field), not formal `.planning/REQUIREMENTS.md` entries. `.planning/REQUIREMENTS.md` has no `BUG-*` or `16.3`-scoped entries, and `.planning/ROADMAP.md`'s Phase 16.3 entry explicitly states `**Requirements**: TBD (phase-local pseudo-IDs BUG-01/02/03/04/05A/05B/06/07 used in plan frontmatter, mapped 1:1 to the 7 bugs above)`. This is expected and correct for this urgent inserted bug-fix phase type — not treated as a gap or orphaned-requirement finding, per this verification's explicit instructions.

### Anti-Patterns Found

None blocking. Scanned all files modified across the 6 plans for `TODO`/`FIXME`/`PLACEHOLDER`, empty-implementation returns, and console.log-only handlers — none found. All colors, template text, SQL, and style edits are substantive, non-stub changes with no dead-code or placeholder markers introduced.

One informational note: `frontend/src/components/layout/Sidebar.tsx` (not touched by this phase) still contains a `borderRadius: "var(--border-radius-sm)"` on its own Logout button, and is not wired into any route (confirmed via repo-wide `grep -rn "Sidebar"` — the only match is the component's own definition file; `TopNav.tsx` is the actual rendered nav). This is a pre-existing dead/unused component, not a live bug in the running app, and out of this phase's declared file scope — flagged for awareness only, not a gap.

### Automated Verification Run This Session

- `cd frontend && npx tsc -b --noEmit` — clean, zero errors
- `cd frontend && npx eslint .` — clean, zero errors/warnings
- `cd frontend && npx vitest run src/components/layout/TopNav.test.tsx` — 1/1 passed
- `cd backend && uv run ruff check app/api/v1/admin.py` — all checks passed
- `cd backend && uv run mypy app/api/v1/admin.py --ignore-missing-imports` — no issues
- `cd backend && uv run pytest tests/api/test_admin.py -q` — 17/17 passed
- `cd backend && uv run pytest tests/api/test_retake_flow.py -q` — 2/2 passed
- `cd backend && uv run pytest tests/api/test_reports.py -k specific_assessment_id -q` — 1/1 passed (proves Bug #3's backend contract)
- `cd backend && uv run pytest tests/api/test_reports.py -q` — 4 failures, all in PDF-generation tests (`test_mail_report_generates_pdf_and_sends_email`, `test_mail_report_dev_mode_skips_resend_send`, `test_download_report_pdf_returns_pdf_content_type`, `test_download_report_pdf_no_submitted_assessment_returns_404`), root-caused to `OSError: cannot load library 'libgobject-2.0-0'` — WeasyPrint's native library is unavailable in this local sandbox. Pre-existing, standing environment limitation (see CLAUDE.md/STATE.md's Phase 16-05 precedent), not a regression introduced by this phase's `report.html` edits.
- `cd frontend && grep -rn "borderRadius" src` — full repo sweep, every line individually checked against the plan's in-scope/out-of-scope classification (see Observable Truths #18-21)

### Human Verification Required

1. **PDF status-dot clipping fix (Bug #5a)**
   **Test:** Generate a report PDF (via the Railway Integration deployment once this phase's PR is live, or any environment with working WeasyPrint) for a submitted assessment.
   **Expected:** Every submitted-answer row's colored status dot renders as a full, complete circle next to its score (1.00–5.00) — not a clipped sliver. Also confirm the "For the initiative:" label and "Data Spaces Support Centre" wording render correctly, and no regression in the radar chart/priority list/legend.
   **Why human:** WeasyPrint is unavailable in this local sandbox (confirmed this session via a direct `OSError`). The code-level fix exactly replicates the already-proven `.priority-row`/`.legend` WeasyPrint-safe pattern from Phase 16-05, but only a real render can confirm the specific clipping symptom is resolved. This is by design — the user gave a conditional/provisional checkpoint approval in `16.3-04-SUMMARY.md`, explicitly deferring this check to a post-deploy pass.

2. **Square-corners visual sanity pass (Bug #6)**
   **Test:** Open the homepage, dashboard, wizard, a report page, and the Admin Panel in a browser (dev server or Railway).
   **Expected:** Every button and card-style panel shows square (0px) corners matching dssc.eu; status dots/radio circles/pills/Inputs/Tables/Alerts remain visually unchanged (still rounded where appropriate).
   **Why human:** Code-level verification (theme.ts + full repo grep sweep of every `borderRadius` occurrence) is complete and 100% consistent with the plan's scope, but no rendered-browser check was performed in this environment. Not a blocking checkpoint in the plan itself — recommended as a quick sanity pass, not a required gate.

### Gaps Summary

No code-level gaps found. All 8 bugs (BUG-01 through BUG-07, with #5 split into 5A/5B) are implemented, wired, and covered by passing automated checks (typecheck, lint, and the relevant backend test suites) where those checks are meaningful. The one item not fully closed — Bug #5a's PDF status-dot clipping fix — is a deliberate, user-approved deferral to a post-deploy visual check, not an implementation gap: the template edit is code-reviewed, matches an already-proven pattern in the same file, and the plan's own checkpoint documented this exact deferral rather than skipping verification silently. This phase should be considered functionally complete pending that one visual sign-off (plus the optional square-corners sanity pass).

---

_Verified: 2026-08-05T05:29:02Z_
_Verifier: Claude (gsd-verifier)_
