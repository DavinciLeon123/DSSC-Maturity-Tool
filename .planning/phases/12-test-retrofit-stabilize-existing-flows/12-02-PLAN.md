---
phase: 12-test-retrofit-stabilize-existing-flows
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - backend/tests/api/test_auth.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Registration rejects a duplicate email (409) and succeeds (201) with the UserRead shape"
    - "Login returns 401 with the SAME message for both wrong-password and non-existent-email (anti-enumeration / timing-equalization path exercised)"
    - "Account lockout returns 423 after the configured failed-attempt threshold"
    - "forgot-password always returns 202 regardless of whether the email exists, and enforces the 60s cooldown (429)"
    - "reset-password is one-time-use (400 on token reuse) and rejects expired tokens (400)"
  artifacts:
    - "backend/tests/api/test_auth.py"
  key_links:
    - "Tests run against the real-Postgres lifespan client fixture from conftest.py (Plan 01)"
    - "Rate limiter disabled via conftest autouse fixture so lockout assertions never collide with a 429 (Pitfall 4)"
---

<objective>
Add regression coverage for the auth subsystem (`backend/app/api/v1/auth.py`) — registration, login, account lockout, and the forgot/reset password flow — satisfying ROADMAP.md Phase 12 success criterion #1. These are characterization tests (D-04): they pin the CURRENT behavior (even anti-enumeration quirks) as the baseline so Phases 13-18 cannot silently change auth behavior.

Purpose: Auth is a subsystem the v2.0 rebuild does NOT touch; this is the safety net that fails loudly if a later phase breaks login, lockout, or password reset.
Output: `backend/tests/api/test_auth.py` covering register/login/lockout/reset, green against real Postgres.

NOTE: must_haves derive from ROADMAP.md Phase 12 success criterion #1 (no REQ-IDs exist for this phase; spec-less probe fallback skipped per Plan 01's note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md
@.planning/phases/12-test-retrofit-stabilize-existing-flows/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Registration + login characterization tests</name>
  <read_first>
    - backend/app/api/v1/auth.py lines 28-92 — `register` (201 + UserRead, 409 duplicate) and `login` (dummy-hash timing branch lines 56-64, lockout check 66-72, failed-attempt increment 74-84, success reset 86-92)
    - backend/app/schemas/auth.py — `UserCreate` (password ≥12 chars, common-password rejection), `UserRead`, `Token`
    - backend/app/core/security.py — `hash_password`/`verify_password`/`create_access_token` (do NOT re-test bcrypt internals; V6 characterization only)
    - backend/tests/conftest.py (Plan 01) — `client`, `session`, factory fixtures
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md — "Pattern 2" (characterization test shape) and Security Domain "Login timing attack" row
  </read_first>
  <action>
    Start `backend/tests/api/test_auth.py` (the `tests/api/__init__.py` package marker already exists from Plan 01). Write tests asserting: (a) POST `/api/v1/auth/register` with a valid ≥12-char password returns 201 and a body matching the UserRead shape (id, email, role defaulting to "USER", participant_type, created_at); (b) a second register with the same email returns 409 with detail "Email already registered"; (c) POST `/api/v1/auth/login` (form-encoded `username`/`password` via OAuth2PasswordRequestForm) with correct credentials returns 200 and a bearer `access_token`; (d) login with a wrong password returns 401; (e) login for a non-existent email ALSO returns 401 with the SAME detail message — assert both paths yield identical status+detail to characterize the timing-equalization/anti-enumeration behavior structurally (do NOT write a wall-clock timing assertion — those are flaky in CI, per the Security Domain note). Use the Plan 01 factory to create the registered user where a pre-existing user is needed. Per D-04, assert the current status codes/messages as the baseline; if any surprising behavior surfaces, log it as a backlog item in the SUMMARY rather than fixing it here.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_auth.py -x -q -k "register or login"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/api/test_auth.py -k "register or login"` exits 0
    - A test asserts non-existent-email login and wrong-password login return the same status (401) and same detail string
    - Register duplicate-email test asserts 409
    - No wall-clock timing assertion is present (structural equivalence only)
  </acceptance_criteria>
  <done>Registration and login (including the anti-enumeration equal-path behavior) are characterized and green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Account lockout + forgot/reset password tests</name>
  <read_first>
    - backend/app/api/v1/auth.py lines 20-25 (`_MAX_FAILED_ATTEMPTS = 5`, `_LOCKOUT_MINUTES = 15`), lines 66-92 (lockout → 423), lines 106-160 (`_send_reset_email` dev-mode skip line 110-112 + Resend send line 127; `forgot_password` always-202 line 160, 60s cooldown 429 lines 141-147), lines 163-185 (`reset_password`: 400 invalid/unknown token, 400 expired, one-time-use clears token line 181-182)
    - backend/tests/conftest.py (Plan 01) — `client`, `session`, factory fixtures, and the autouse rate-limiter-disable fixture (Pitfall 4)
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md — "Pattern 2" (lockout + forgot-password examples), "Pitfall 4" (rate limiter), Open Question 2 (test BOTH the dev-mode-skip and mocked-Resend paths), Security Domain "Password reset token replay" row
  </read_first>
  <action>
    Extend `backend/tests/api/test_auth.py`. Lockout: create a registered user, POST `/api/v1/auth/login` with a wrong password 5 times asserting 401 each, then assert the 6th attempt returns 423 with a "locked" detail; keep all login attempts for this behavior within one test so they stay under the rate-limit window even though the limiter is disabled in tests (documents the intent, per Pitfall 4). Also assert a successful login after a partial (<5) failure streak resets `failed_login_attempts` to 0. Password reset — cover BOTH email paths per Open Question 2: (a) with `RESEND_API_KEY` empty (dev-mode), assert `forgot-password` returns 202 and does NOT call `resend.Emails.send` (patch `resend.Emails.send` and assert not called — dev-mode logs instead); (b) with a key set via `monkeypatch.setenv`/settings override, assert the mocked `resend.Emails.send` IS called. Assert `forgot-password` returns 202 for an UNKNOWN email too (anti-enumeration — intentional per D-04). Assert the 60-second cooldown returns 429 on an immediate second request for the same existing user. reset-password: drive a real token by calling forgot-password for a factory user then reading `user.password_reset_token` from the test `session`; assert a valid token + valid new password returns 200; assert reusing the now-cleared token returns 400 (one-time-use); assert a manually-expired token (set `password_reset_expires` in the past via the session) returns 400. Patch `resend.Emails.send` directly (no lazy import here — unlike WeasyPrint).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/api/test_auth.py -x` exits 0 (full auth file green, including register/login from Task 1)
    - Lockout test asserts 423 on the 6th attempt after 5x401
    - forgot-password test asserts 202 for both a known and an unknown email, and 429 on the 60s cooldown
    - Two distinct email-path tests exist: one asserting `resend.Emails.send` NOT called (empty key), one asserting it IS called (key set)
    - reset-password test asserts 200 on first use, 400 on token reuse, 400 on expired token
    - Any bug discovered is logged in the SUMMARY as a backlog item (D-04), not fixed inline
  </acceptance_criteria>
  <done>Lockout and the full forgot/reset password flow (both email paths, token replay, expiry) are characterized and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth API | Untrusted credentials, registration input, and reset tokens cross here; the auth endpoints are the primary defended surface |
| /auth → Resend (email) | Reset emails leave the trust boundary; mocked in tests, never sent live |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-ENUM | Information Disclosure | `login` / `forgot-password` username enumeration | high | mitigate | Regression test asserts non-existent-email and wrong-password login return identical status+detail, and forgot-password returns 202 for unknown emails — locks the existing anti-enumeration controls so a later phase cannot regress them. |
| T-12-02-BF | Elevation of Privilege | `login` brute force | high | mitigate | Regression test asserts 423 lockout after 5 failed attempts, pinning `_MAX_FAILED_ATTEMPTS` behavior; rate limiter is disabled ONLY in the test session (Pitfall 4), the production limit is unchanged. |
| T-12-02-RST | Tampering | `reset_password` token replay | high | mitigate | Regression test asserts one-time-use (400 on reuse) and expiry (400 on expired), pinning the token-invalidation control. |
| T-12-02-VAL | Tampering | `UserCreate`/`ResetPasswordRequest` password validators | low | accept | Length/common-password validators are exercised indirectly by register/reset tests; not the primary focus (V5 partial). |
</threat_model>

<verification>
- `uv run pytest tests/api/test_auth.py -x` green against real Postgres
- Both the isolated (`-k`) and full-file runs pass — no rate-limiter 429 flakiness
- SUMMARY logs any D-04 backlog items discovered while writing these tests
</verification>

<success_criteria>
ROADMAP.md Phase 12 success criterion #1 is met: registration, login, account lockout, and password reset are covered by automated tests that fail if their behavior changes. Anti-enumeration, brute-force-lockout, and token-replay controls are pinned as baseline.
</success_criteria>

## Artifacts this phase produces (Plan 02)

- `backend/tests/api/test_auth.py` — new; tests for register (201/409), login (200/401 equal-path), lockout (423), forgot-password (202/429, both email paths), reset-password (200/400 reuse/400 expired)

<output>
Create `.planning/phases/12-test-retrofit-stabilize-existing-flows/12-02-SUMMARY.md` when done. Include a "Backlog items discovered (D-04)" section (may be empty).
</output>
