---
phase: 12-test-retrofit-stabilize-existing-flows
plan: 05
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - .github/workflows/test.yml
  - frontend/vitest.config.ts
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/test/setup.ts
  - frontend/src/components/layout/TopNav.test.tsx
autonomous: false
requirements: []
user_setup:
  - service: github-actions
    why: "First CI in this repo (D-02). No dashboard config is strictly required — the workflow triggers on push/pull_request automatically once merged — but the developer should confirm GitHub Actions is enabled for the repository and watch the first run go green."
    dashboard_config:
      - task: "Confirm the first Actions run for `Test Suite` passes both jobs (backend-tests, frontend-tests) on the PR"
        location: "GitHub repo → Actions tab"

must_haves:
  truths:
    - "A GitHub Actions workflow runs backend pytest (real Postgres via testcontainers on the Docker-preinstalled runner) and frontend Vitest on every push and pull_request (D-02)"
    - "Both CI jobs must pass for the merge check to be green — the safety net blocks bad merges automatically"
    - "`npx vitest run` executes at least one deterministic passing frontend test locally and in CI"
  artifacts:
    - ".github/workflows/test.yml"
    - "frontend/vitest.config.ts"
    - "frontend/src/components/layout/TopNav.test.tsx"
  key_links:
    - "backend-tests job runs on ubuntu-latest where Docker is preinstalled — testcontainers manages Postgres, no GH services: block (avoids two DB-provisioning code paths)"
    - "frontend Vitest config reuses vite.config.ts's plugin/proxy conventions (no duplicated TanStackRouter/proxy config)"
---

<objective>
Stand up CI from scratch (D-02) — a `.github/workflows/test.yml` with a `backend-tests` job (uv + pytest against a testcontainers Postgres) and a `frontend-tests` job (Vitest) — and wire the frontend Vitest harness (config + one deterministic smoke test) so the frontend CI job actually runs and reports pass/fail rather than no-opping. This satisfies ROADMAP.md Phase 12 success criterion #4: the suite runs automatically to gate merges throughout the Phases 13-18 rebuild.

Purpose: The whole point of the safety net is to block bad merges automatically. This plan makes the Wave-2 backend suite (plus a minimal frontend suite) run on every PR.
Output: A green two-job GitHub Actions workflow and a working `npx vitest run`.

NOTE: This plan depends on Plans 02/03/04 so the backend CI job runs REAL tests (not an empty suite that passes trivially) — that is what makes success criterion #4 genuinely verifiable. must_haves derive from ROADMAP.md Phase 12 success criterion #4 (no REQ-IDs; spec-less probe fallback skipped per Plan 01's note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md
@.planning/phases/12-test-retrofit-stabilize-existing-flows/12-PATTERNS.md
@frontend/vite.config.ts
@frontend/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Frontend package legitimacy verification before npm install (T-12-SC)</name>
  <read_first>
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md — "Package Legitimacy Audit" table (frontend rows: vitest, @vitest/coverage-v8, jsdom, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event) and "Anti-Patterns to Avoid" (ONE combined gate, not per-package)
  </read_first>
  <what-built>
    A single combined legitimacy gate covering the frontend dev packages installed in Task 2: `vitest`, `@vitest/coverage-v8`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`. A few are checker-flagged SUS purely for a recent latest-publish date despite tens of millions of weekly downloads (false-positives per the audit).
  </what-built>
  <action>
    STOP before running `npm install`. Present the RESEARCH.md "Package Legitimacy Audit" frontend rows to the human and wait for explicit approval that each package's version and canonical GitHub org match the audit. Do NOT auto-approve (this gate ignores auto_advance). Only after "approved" does Task 2's install run.
  </action>
  <how-to-verify>
    1. Open the RESEARCH.md "Package Legitimacy Audit" table (frontend rows).
    2. Confirm canonical repos: `vitest`/`@vitest/coverage-v8` → `github.com/vitest-dev/vitest`; `jsdom` → `github.com/jsdom/jsdom`; the three `@testing-library/*` → `github.com/testing-library/*`.
    3. Confirm the pinned versions match the audit (vitest 4.1.10, @vitest/coverage-v8 4.1.10, jsdom 29.1.1, @testing-library/react 16.3.2, @testing-library/jest-dom 7.0.0, @testing-library/user-event 14.6.1).
    4. After Task 2's `npm install` runs, confirm `package-lock.json` resolved these same packages/versions — no unexpected substitute.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or describe any package that does not match the audit.</resume-signal>
  <done>Human has confirmed the frontend package set matches the RESEARCH.md audit; install may proceed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend Vitest config + deterministic smoke test</name>
  <read_first>
    - frontend/vite.config.ts — the existing `defineConfig` with `TanStackRouterVite` + `react()` plugins and the `/api` proxy (the Vitest config must reuse these, NOT duplicate them into a diverging file)
    - frontend/package.json — existing `scripts` (add a `test` script) and devDependencies
    - frontend/src/components/layout/TopNav.tsx — the smoke-test target; uses `useQuery` (`@tanstack/react-query`) to call `/auth/me` and `Link`/`useNavigate` (`@tanstack/react-router`) — a bare render without a QueryClientProvider and router mocks throws
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md — Standard Stack (jsdom + testing-library), Open Question 1 (CI-wiring smoke test is sufficient this phase; real frontend behavior tests are Phase 17)
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-PATTERNS.md — "frontend/vitest.config.ts" and "frontend/src/components/layout/TopNav.test.tsx" (provider-wrapping requirements)
  </read_first>
  <action>
    From `frontend/`, install the audited dev deps: `npm install -D vitest @vitest/coverage-v8 jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event`. Create `frontend/vitest.config.ts` that re-uses the same plugin array as `vite.config.ts` (import and compose, or re-declare the identical `TanStackRouterVite` + `react()` plugins — do NOT let the two configs diverge) and adds a `test` block with `environment: 'jsdom'`, `globals: true`, and `setupFiles: ['./src/test/setup.ts']`. Create `frontend/src/test/setup.ts` importing `@testing-library/jest-dom` matchers. Add a `test` script to `package.json` (`"test": "vitest run"`). Create `frontend/src/components/layout/TopNav.test.tsx` as a deterministic smoke test: wrap `<TopNav />` in a `QueryClientProvider` (new `QueryClient`) and mock the `@tanstack/react-router` hooks (`useNavigate`, `Link`) so render does not throw, then assert the component renders (a nav element or a known label is present). The goal per Open Question 1 is a green, deterministic test that gives the CI frontend job something real to run — keep it a render/presence smoke check; deep interaction is Phase 17 (TEST-02). If wiring the TopNav providers proves heavier than a render smoke warrants, a minimal deterministic render test of a stable presentational element is an acceptable fallback — the hard requirement is exactly one reliably-green test, never a flaky one.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with at least one passing test and zero failures
    - `frontend/vitest.config.ts` exists and reuses the vite plugin/proxy conventions (no duplicated diverging TanStackRouter config)
    - `package.json` has a `"test"` script running vitest in run (non-watch) mode
    - `frontend/src/test/setup.ts` imports jest-dom matchers
  </acceptance_criteria>
  <done>Frontend Vitest harness runs a deterministic green smoke test locally.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: GitHub Actions workflow (backend + frontend jobs)</name>
  <read_first>
    - .planning/phases/12-test-retrofit-stabilize-existing-flows/12-RESEARCH.md — "Code Examples → GitHub Actions workflow (.github/workflows/test.yml)" (full draft: backend-tests uses astral-sh/setup-uv + `uv run pytest`; frontend-tests uses actions/setup-node@v7 Node 20 + `npm ci` + `npx vitest run`; no GH `services:` Postgres block because ubuntu-latest has Docker for testcontainers)
    - docker-compose.yml line 3 — `postgres:16-alpine` pin (testcontainers uses the same image tag; keep parity)
    - backend/pyproject.toml (after Plan 01) — the dev extras group `uv sync --all-extras` installs
    - frontend/package.json (after Task 2) — the `test` script CI invokes
  </read_first>
  <action>
    Create `.github/workflows/test.yml` named `Test Suite`, triggered on `push` (branches: [main]) and `pull_request`. Job `backend-tests` on `ubuntu-latest` with `defaults.run.working-directory: backend`: checkout (`actions/checkout@v7`), install uv (`astral-sh/setup-uv@v7` with cache enabled, `cache-dependency-glob: "backend/uv.lock"`), `uv sync --locked --all-extras`, then `uv run pytest --cov=app --cov-report=term-missing`. Do NOT add a GH Actions `services:` Postgres block — testcontainers manages its own `postgres:16-alpine` container via the runner's preinstalled Docker daemon (RESEARCH.md Architecture / Alternatives — one provisioning code path, not two). Job `frontend-tests` on `ubuntu-latest` with `working-directory: frontend`: checkout, `actions/setup-node@v7` (node-version 20, `cache: npm`, `cache-dependency-path: frontend/package-lock.json`), `npm ci`, `npx vitest run --coverage`. Do NOT put any real secret (RESEND_API_KEY, SECRET_KEY) in the workflow — the report tests mock Resend and use the dev-mode fallback, so no live email credential is needed in CI; rely on the settings defaults. Validate the YAML parses before finishing.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import yaml; d=yaml.safe_load(open('../.github/workflows/test.yml')); j=d['jobs']; assert 'backend-tests' in j and 'frontend-tests' in j; assert 'services' not in j['backend-tests']; print('workflow-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parses and contains both `backend-tests` and `frontend-tests` jobs
    - `backend-tests` has NO `services:` block (testcontainers provisions Postgres, not a GH service container)
    - backend job runs `uv run pytest` with coverage; frontend job runs `npx vitest run`
    - No real secret (RESEND_API_KEY / SECRET_KEY) is hardcoded in the workflow
    - Workflow triggers on both `push` (main) and `pull_request`
  </acceptance_criteria>
  <done>`.github/workflows/test.yml` exists with two passing-capable jobs; the safety net runs on every PR/push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build/CI | Frontend dev dependencies fetched during `npm install`; a malicious package would run in build/CI |
| CI runner → external services | The backend job spins up a Docker Postgres and (mocked) never calls Resend live; no production secret should enter CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-SC | Tampering | `npm install` of vitest/jsdom/testing-library | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; pin audited versions; verify `package-lock.json` matches the RESEARCH.md audit. Never auto-approvable. |
| T-12-05-SECRET | Information Disclosure | CI workflow env / secrets | medium | mitigate | Workflow hardcodes NO real secret; report tests mock Resend and rely on the dev-mode fallback + settings defaults, so no live credential is placed in CI YAML. |
| T-12-05-CI | Tampering | GitHub Actions runner integrity | low | accept | Pinned major action versions (`@v7`); runner is GitHub-hosted ephemeral. Supply-chain of the actions themselves is accepted at low severity for this internal CI. |
</threat_model>

<verification>
- `npx vitest run` green locally (frontend job will pass in CI)
- `.github/workflows/test.yml` parses; both jobs present; no `services:` block on backend job
- Phase gate: the first GitHub Actions run of `Test Suite` passes both `backend-tests` (real Wave-2 pytest suite) and `frontend-tests` — this is where success criterion #4 is truly proven end to end
</verification>

<success_criteria>
ROADMAP.md Phase 12 success criterion #4 is met: the full backend + frontend suite runs automatically on every PR/push via GitHub Actions and gives a clear pass/fail merge signal throughout the Phases 13-18 rebuild.
</success_criteria>

## Artifacts this phase produces (Plan 05)

- `.github/workflows/test.yml` — new; `Test Suite` workflow, jobs `backend-tests` (uv + pytest + testcontainers Postgres) and `frontend-tests` (Node 20 + Vitest)
- `frontend/vitest.config.ts` — new; jsdom environment, globals, setupFiles, reusing vite plugin/proxy config
- `frontend/src/test/setup.ts` — new; imports `@testing-library/jest-dom`
- `frontend/src/components/layout/TopNav.test.tsx` — new; deterministic render smoke test
- `frontend/package.json` — modified; new `test` script + Vitest/testing-library devDependencies
- `frontend/package-lock.json` — modified; resolved frontend dev dependency tree

<output>
Create `.planning/phases/12-test-retrofit-stabilize-existing-flows/12-05-SUMMARY.md` when done.
</output>